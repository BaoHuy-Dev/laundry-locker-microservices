# Theo Dõi Tiến Độ Dự Án

> Cập nhật lần cuối: 2026-06-21
> Workspace: `G:\BigProject`
> Cặp tài liệu nguồn: file này + `docs/BUSINESS_FLOWS_CURRENT.md`

## Quy Tắc Cập Nhật Bắt Buộc

Mỗi khi developer hoặc AI coding agent có tiến độ mới trong dự án, file này phải được cập nhật trong cùng phiên làm việc.

Cập nhật file này sau khi:

- Implement một chức năng.
- Fix bug.
- Đổi API, route, DTO, migration database, event, scheduler, màn hình, hoặc service contract.
- Chạy thành công hoặc chạy lỗi một lệnh verification.
- Phát hiện blocker, technical debt, hoặc giả định cũ bị sai.
- Tạo/đổi demo data hoặc credential deploy dùng cho test account.

Nếu thay đổi làm đổi hành vi nghiệp vụ, phải cập nhật thêm `docs/BUSINESS_FLOWS_CURRENT.md`.

Mức cập nhật tối thiểu cho mỗi phiên làm việc:

1. Thêm một dòng vào **Nhật Ký Thay Đổi Gần Đây**.
2. Cập nhật **Bảng Tiến Độ Theo Component** nếu status thay đổi.
3. Cập nhật **Việc Còn Lại** nếu có việc đã xong, mới thêm, hoặc đổi ưu tiên.
4. Cập nhật **Nhật Ký Verification** với lệnh/kết quả.
5. Nếu docs/artifacts được di chuyển hoặc generate, cập nhật `docs/project-artifacts/README.md`.

## Cách AI Khác Nên Bắt Đầu

Đọc các file theo thứ tự:

1. `docs/PROJECT_PROGRESS_TRACKER.md`
2. `docs/BUSINESS_FLOWS_CURRENT.md`
3. `docs/CURRENT_PROJECT_STATUS.md`
4. `RUN_RESULT.md`
5. `LOCKER_FLOW_PLAN.md`
6. `docs/project-artifacts/guides/LOCKER_FLOWS_STANDARD_SPEC.md` (phân tích luồng tủ as-is + đặc tả chuẩn thực tế + gap map + backlog).
7. `docs/ARCHITECTURE_DECISIONS.md` (ADR: K8s/Kafka/CQRS/GraphQL/mesh — quyết định hoãn + điều kiện xem lại).
8. File code liên quan đến task.

Không mặc định tài liệu cũ là đúng nếu chúng mâu thuẫn với các file trên.

## Trạng Thái Tổng Quan Hiện Tại

Dự án hiện có nền tảng locker/laundry đang chạy với backend Locker Phase 2, màn hình web admin cho locker/maintenance, và Flutter routing theo role cho các flow locker.

Tóm tắt trạng thái:

- Backend Locker Phase 1: đã xong và đã test.
- Backend Locker Phase 2: đã xong và đã test.
- Backend production hardening Phase 1: đã implement correlation ID, Prometheus metrics endpoint, OpenAPI docs runtime, security workflow, và deploy verify.
- Backend production hardening Phase 2: đã implement Resilience4j/timeout cho Feign, SBOM runtime/CI, Trivy container image scan gate, Testcontainers smoke cho locker, và đã verify Maven.
- Backend production hardening Phase 3/4: đã implement gateway RBAC/access-token tests, Swagger UI/OpenAPI aggregation qua gateway, build metadata, full 12-image Trivy matrix, deploy artifact checksum, và đã verify Maven.
- Backend production hardening Phase 4 continuation: đã thêm GitHub artifact attestations/provenance cho deploy artifact, tag-based backend release workflow, release SBOM/checksum, và script verify release artifact.
- Web admin locker/maintenance: đã implement và đã verify build.
- Flutter customer/manager/maintenance locker flows: đã implement, đã verify targeted build/analyze.
- IoT backend verify-access bằng PIN/QR: đã implement.
- Mobile mở tủ qua IoT (`/api/iot/unlock`): đã implement, mô phỏng bằng script Python độc lập `smart-locker-iot/simulate_demo_cabinet.py` (chưa có hardware thật).
- Reorder ("đặt lại đơn") cho SEND/RENTAL: đã sửa đúng (trước đó tạo đơn không có ô/giá sai), đã wire mobile.
- Notification khi maintenance claim/resolve báo cáo lỗi tủ: đã implement (event mới `locker.report.claimed/resolved`), mobile có màn "Báo cáo của tôi".
- Drone fleet maintenance (trạng thái/pin nhập tay/kỹ thuật viên/nhật ký, tab "Drone" mobile): đã implement 2026-06-22 (xem dòng "Đang Làm" bên dưới). Drone delivery/assignment theo pin/tracking realtime thật: vẫn chưa implement.
- AI/RAG: chưa implement.
- `laundry-service`: thiếu source module.
- Role `PARTNER`/`partner-service`: đã gỡ khỏi backend (seed/role/permission/compose) vì không còn dùng.

## Đang Làm

| Ngày | Branch | Task | Phạm vi dự kiến sửa | Không sửa | Ảnh hưởng |
|---|---|---|---|---|---|
| 2026-07-03 | mobile `feat/mobile-delivery-push-notifications` | **Theo dõi giao drone cho NGƯỜI NHẬN — feature chuyên biệt + timeline (Phase 1, push-only).** Nâng cấp từ luồng noti-only 2026-06-28 (chỉ deep-link `order_detail`) lên **feature `drone_delivery` riêng đủ 4 layer** (mirror `logistics_send`) + **trang timeline `DroneDeliveryTrackingPage(orderId)`** (4 mốc dispatched→approaching→arrived→delivered, mốc hiện tại nổi bật, ETA, banner delayed=amber/failed=red) + sheet nhắc ra nhận khi `approaching`. FCM router mở rộng: 6 type `drone_*` tap → `context.go('/drone-delivery', extra: orderId)` (thay vì `order_detail`). Provider Riverpod 3 nghe FCM stream lọc theo `orderId` rồi refetch. Fetch `GET /api/orders/{orderId}/drone-delivery` **tắt sau flag `droneDeliveryEnabled`+mock** (BE chưa có endpoint). CHƯA live map/websocket/MAVLink. | Mobile: 🆕 `lib/features/drone_delivery/**` (12 file, 4 layer + `.g.dart`); ✏️ `core/services/firebase_messaging_service.dart` (set `droneDeliveryTypes` + route tap), `core/routing/app_router.dart` (route `droneDeliveryTracking`), `core/config/feature_flags.dart` (`droneDeliveryEnabled`). Docs: `merge-status.md` + 2 file sống BE. | Không viết lại `FirebaseMessagingService` (chỉ mở rộng); không đụng `drone_telemetry`/`drone_mission` (pilot/MAVLink); không tạo channel FCM mới (dùng `aisl_high_importance_channel`); không thêm live map/websocket; không sửa `.g.dart` tay; không consolidate Provider/BLoC. | Mobile: thêm trang timeline theo dõi drone + deep-link 6 type `drone_*`. BE cần bổ sung `GET /api/orders/{orderId}/drone-delivery` + FCM data payload `{type:drone_*, orderId, status, eta, deliveryId, ...}` (xem BUSINESS_FLOWS mục 11.4). Không đổi DB. Verify: `flutter analyze` feature + core PASS (0 issue); build apk debug (đang chạy). |
| 2026-07-03 | BE `feat/order-remove-laundry-endpoints` + mobile `feat/mobile-remove-courier-laundry` | **PA5 — Gỡ giặt ủi + courier/staff-registration khỏi scope; hoàn thiện tính năng dở.** (1) BE: gỡ 5 endpoint laundry `PUT /api/orders/{id}/collect|weight|process|ready|return` + methods `collect/updateWeight/process/ready/returnOrder` (giữ trạng thái cũ ở complete/checkout cho dữ liệu lịch sử). (2) Mobile: xóa features `courier_delivery/courier_dispatch/courier_registration/staff_application/logistics_send/user_laundry/staff`, courier mode ở Home/Profile, các courier use case/model/route/provider, trang legacy `order_page/locker_action_page/locker_otp_page`, bộ lọc "Giặt ủi"; chuyển `user_gateway_client` → `core/network`. (3) Bổ sung: khóa app sinh trắc học (`local_auth` + `FlutterFragmentActivity` + `USE_BIOMETRIC` + toggle Bảo mật + gate splash); nút Mở tủ parse `accepted/message` (hiển thị đúng kết quả phần cứng); Trợ giúp/Liên hệ nội dung thật. | BE: `order-service` controller+service. Mobile: ~25 file sửa + ~90 file xóa (xem git). Docs: 2 file sống + mirror. | Không sửa migration cũ; không đổi gateway route/RBAC; không đụng `/api/staff/**` (STAFF ops read-only giữ); không đụng `main.py` IoT; không commit secret. | API: 5 endpoint laundry bị gỡ (không client nào dùng). Mobile: courier/laundry UI biến mất; thêm biometric app-lock. Verify: BE `mvn -pl order-service -am compile` PASS; mobile `flutter analyze` 0 error/0 warning + build apk debug (đang chạy). |
| 2026-06-30 | mobile `feat/maintenance-box-anomalies` (+docs BE `feat/maintenance-box-anomalies-mobile`) | **Cảnh báo phần cứng toàn cục cho MAINTENANCE (slice mobile UI)**. Wire `GET /api/maintenance/box-anomalies` (slice backend trước) vào tab "Sự cố" của `MaintenanceHomePage`: section "Cảnh báo phần cứng" đầu tab gom mọi ô cửa-mở-bất-thường mọi tủ, có nút Chỉ đường. | Mobile: `lib/features/locker_ops/data/locker_ops_service.dart` (+`boxAnomalies()`), `lib/features/locker_ops/presentation/pages/maintenance_home_page.dart` (+state `_anomalies`, load best-effort trong `_load`, `_boxAnomaliesSection` ở đầu `_buildQueue`). Docs BE: `BUSINESS_FLOWS_CURRENT.md` (mục 21) + `PROJECT_PROGRESS_TRACKER.md` (+mirror). | Không đổi backend/API/contract (endpoint đã có ở slice trước); không đổi route/role mobile; không đụng tab khác hay flow customer; không thêm dependency; dùng lại `_openDirections`. | Chỉ thêm UI mobile MAINTENANCE (1 section read-only đầu tab Sự cố, ẩn khi không có ô bất thường). Best-effort. Không đổi API/DB/event/role. Verify: `flutter analyze` 2 file — No issues. **Còn lại:** chưa smoke emulator. |
| 2026-06-30 | BE `feat/maintenance-box-anomalies` | **Tổng quan cảnh báo phần cứng toàn cục cho MAINTENANCE (slice backend)**. Box-health đang theo từng tủ; thêm endpoint gom **mọi ô cửa-mở-bất-thường trên TẤT CẢ tủ** vào 1 danh sách cho tab tổng quan ca trực (KTV không phải chọn từng tủ). Read-only, không side-effect (không tự tạo report/notify). | locker-service: `dto/BoxAnomalyResponse.java` (MỚI), `service/LockerService.java` (+`boxAnomalies()` — fetch all hardware từ iot-service best-effort, lọc door OPEN && ≠ OCCUPIED, enrich locker metadata như `openFaults`; +import `ArrayList`), `controller/LockerController.java` (+`GET /api/maintenance/box-anomalies`). Docs + mirror. | KHÔNG sửa `LockerBox.status`/state machine; không tự reconcile/tạo report/notify; không đổi gateway/RBAC (`/api/maintenance/**` sẵn có); không DB/migration/event; chưa đụng mobile (slice sau). | Endpoint mới `GET /api/maintenance/box-anomalies` (MAINTENANCE/ADMIN). Best-effort (iot-service chết → rỗng). Không đổi DB/event/role. Verify: `mvn -pl locker-service -am compile`. **Còn lại:** slice mobile (section "Cảnh báo phần cứng" trên tab tổng quan ca trực). |
| 2026-06-30 | mobile `feat/maintenance-box-health` (+docs BE `feat/maintenance-box-health-mobile`) | **Box-health cho MAINTENANCE (follow-up GAP 2, slice mobile UI)**. Wire endpoint backend (slice trước) vào UI bảo trì: tab "Kiểm tra tủ" của `MaintenanceHomePage` hiện card "Tình trạng phần cứng ô" — trạng thái logic theo đơn cạnh trạng thái cửa phần cứng, nổi bật ô cần chú ý (cửa mở mà không OCCUPIED). | Mobile: `lib/features/locker_ops/data/locker_ops_service.dart` (+`boxHealth(lockerId)`), `lib/features/locker_ops/presentation/pages/maintenance_home_page.dart` (+state `_boxHealth`, load best-effort trong `_selectLocker`, `_boxHealthCard`/`_boxHealthRow` trong tab Kiểm tra tủ). Docs BE: `BUSINESS_FLOWS_CURRENT.md` (mục 21) + `PROJECT_PROGRESS_TRACKER.md` (+mirror). | Không đổi backend/API/contract (endpoint đã có ở slice trước); không đổi route/role mobile; không đụng các tab khác (Sự cố/Việc của tôi/Định kỳ/Drone) hay flow customer; không thêm dependency. | Chỉ thêm UI mobile MAINTENANCE (1 card read-only trong tab Kiểm tra tủ). Best-effort: BE/IoT chưa có dữ liệu → card hiện hint, không vỡ trang. Không đổi API/DB/event/role. Verify: `flutter analyze` 2 file — No issues. **Còn lại:** chưa smoke emulator; chưa job đối chiếu mismatch tự động. |
| 2026-06-30 | BE `feat/maintenance-box-health` | **Box-health cho MAINTENANCE (follow-up GAP 2, slice backend)**. GAP 2 mới expose box-status cho Manager/Admin; nay cho **bảo trì** xem trạng thái phần cứng cửa đặt cạnh trạng thái logic theo đơn để soi bất thường (cửa mở trên ô không OCCUPIED). iot-service thêm endpoint nội bộ; locker-service ghép logic + hardware. (Slice mobile UI làm task sau.) | iot-service: `controller/IotController.java` (+`GET /internal/iot/box-status`). locker-service: `client/IotClient.java` (+`boxStatus`+record `BoxHardwareStatus`), `dto/BoxHealthResponse.java` (MỚI), `service/LockerService.java` (+`boxHealth`+`fetchHardwareStatuses`), `controller/LockerController.java` (+`GET /api/maintenance/lockers/{lockerId}/box-health`). Docs + mirror. | KHÔNG sửa `LockerBox.status`/state machine; không tự reconcile/ghi đè; không đổi gateway route/RBAC (`/api/maintenance/**` + `/internal/**` đã có); không sửa migration/DB/event; chưa đụng mobile (slice sau). | Endpoint mới `GET /api/maintenance/lockers/{lockerId}/box-health` (MAINTENANCE/ADMIN) + nội bộ `GET /internal/iot/box-status`. Hardware lookup best-effort (iot-service chết → hwState=null). Không đổi DB/event/role. Verify: `mvn -pl iot-service,locker-service -am compile`. **Còn lại:** slice mobile UI (tab bảo trì hiện box-health + cảnh báo). |
| 2026-06-30 | BE `feat/iot-hardware-status-reconcile` + IoT `feat/iot-hardware-status-reconcile` | **Cabinet → backend: trạng thái phần cứng ô (GAP 2, bản an toàn)**. Cabinet báo trạng thái cửa/cảm biến thật về backend; trước đây `IotService.updateBoxStatus` chỉ publish event rồi vứt đi, không lưu gì. Nay **lưu riêng** trạng thái phần cứng (`iot_schema.box_hardware_status`, V3) **tách khỏi** `LockerBox.status` (order-driven, locker-service sở hữu) — KHÔNG ghi đè trạng thái theo đơn, chỉ để ops đối chiếu phát hiện lệch. Expose `GET /api/manage/iot/box-status` (Manager/Admin). Simulator báo cửa OPEN→CLOSED sau khi mở. | iot-service: `db/migration/V3__box_hardware_status.sql` (MỚI), `model/BoxHardwareStatus.java` (MỚI), `repository/BoxHardwareStatusRepository.java` (MỚI), `dto/BoxHardwareStatusResponse.java` (MỚI), `dto/BoxStatusUpdateRequest.java` (+`lockerId` optional), `service/IotService.java` (upsert + `listBoxHardwareStatuses`), `controller/IotController.java` (+`GET /api/manage/iot/box-status`), `service/LockerMqttService.java` (status handler truyền lockerId từ topic + helper `parseLongOrNull`). IoT: `simulate_demo_cabinet.py` (báo `cabinet/{lockerId}/locker/{boxId}/status` OPEN/CLOSED). Docs + mirror. | KHÔNG sửa `LockerBox.status`/locker-service (giữ nguyên state machine theo đơn); không tự reconcile/ghi đè trạng thái logic; không đổi gateway route/RBAC (`/api/manage/iot/**` đã có); không sửa migration cũ V1/V2; không sửa `main.py`/serial/setup. | Bảng mới `iot_schema.box_hardware_status` (V3, additive). Endpoint mới `GET /api/manage/iot/box-status` (Manager/Admin). MQTT topic `cabinet/{lockerId}/locker/{boxId}/status` giờ được persist. Không đổi API public khác/event/role/UI mobile. Verify: `mvn -pl iot-service -am compile`; `python -m py_compile` + smoke. **Còn lại:** chưa có surface cho MAINTENANCE (mobile/`/api/maintenance/...`) — follow-up; chưa job đối chiếu mismatch tự động. |
| 2026-06-30 | IoT `feat/iot-simulator-heartbeat` (+docs BE cùng branch) | **Simulator phát heartbeat → device-health (GAP 3)**. Backend đã có sẵn handler `cabinet/{id}/heartbeat` → `IotService.updateStatus` (dashboard `GET /api/manage/iot/device-status`) nhưng KHÔNG ai publish heartbeat ⇒ dashboard rỗng, thiết bị luôn "offline". Cho simulator tự học cabinet từ traffic (lệnh open/sync) + seed qua env, rồi định kỳ publish `{status:ONLINE}` để cabinet hiện ONLINE. Thuần simulator, backend không đổi. | IoT: `smart-locker-iot/simulate_demo_cabinet.py` (thêm `_publish_heartbeat`/`_learn_cabinet`/`_heartbeat_loop`, env `SIM_HEARTBEAT_SECONDS`/`SIM_HEARTBEAT_CABINETS`, thread heartbeat trong `main()`). Docs: `PROJECT_PROGRESS_TRACKER.md` + `BUSINESS_FLOWS_CURRENT.md` (mục 19) + mirror. | Không sửa backend Java/API/DB/migration/event (handler heartbeat đã có sẵn); không sửa `main.py`/serial/setup (hardware track); không đổi UI mobile/web. | Chỉ ảnh hưởng dữ liệu device-health (Manager/Admin web `GET /api/manage/iot/device-status` giờ có cabinet ONLINE khi simulator chạy). Không đổi API contract/DB/event/role. Verify: `python -m py_compile` + smoke logic stub client. |
| 2026-06-29 | BE `feat/iot-booking-state-sync` + IoT `feat/iot-booking-state-sync` | **Đồng bộ booking → IoT (GAP 1)**. Khi khách đặt/giữ/chiếm/giải phóng ô (SEND/RENTAL), backend hiện chỉ đổi DB chứ không báo cabinet. Thêm đường đồng bộ trạng thái ô xuống tủ qua MQTT để "đặt đơn → tủ mô phỏng đồng bộ": iot-service publish `cabinet/{lockerId}/command/sync` `{boxId,state,orderId?}`; locker-service gọi best-effort khi reserve/occupy/release/fault/clear; simulator log lệnh sync. | iot-service: `dto/BoxStateSyncRequest.java` (MỚI), `service/LockerMqttService.java` (+`publishBoxStateSync`), `service/IotService.java` (+`syncBoxState`), `controller/IotController.java` (+`POST /internal/iot/box-sync`). locker-service: `client/IotClient.java` (+`syncBoxState`+record), `service/LockerService.java` (+helper `syncBoxStateQuietly`, gọi trong reserveBox/occupyBox/releaseBox/markFault/clearFault). IoT: `smart-locker-iot/simulate_demo_cabinet.py` (+subscribe `cabinet/+/command/sync`). | Không sửa `main.py`/serial/setup-handler (hardware track); không đổi gateway route/RBAC (`/internal/**` đã chặn sẵn); không sửa migration/DB/schema; không đổi UI nghiệp vụ mobile/web; không chặn luồng đặt đơn nếu IoT lỗi (nuốt lỗi). | Endpoint nội bộ MỚI `POST /internal/iot/box-sync` (service-to-service, chặn qua gateway). MQTT topic mới `cabinet/{lockerId}/command/sync`. Không đổi API public/DB/event RabbitMQ/role. Verify: `mvn -pl iot-service,locker-service -am compile`; `python -m py_compile simulate_demo_cabinet.py`. |
| 2026-06-28 | mobile `feat/mobile-delivery-push-notifications` + BE `feat/notification-delivery-status` | **Push notification trạng thái giao hàng (drone delivery) — phase noti-only** (CHƯA làm live map/websocket/MQTT). Người nhận nhận noti ở 6 mốc (dispatched/approaching/arrived/delivered/delayed/failed), tap → deep-link chi tiết đơn theo `orderId`; nhận ở cả 3 trạng thái app; lịch sử noti; tự refresh trạng thái đơn. **Tái dùng hạ tầng FCM sẵn có**. Quyết định đã chốt: nhắm theo **userId người nhận** (reuse `fcm_tokens`+`sendToUser`); kích hoạt backend bằng **internal endpoint `POST /internal/notifications/delivery-status`** + định nghĩa event `delivery.status.changed`; mobile **mở rộng `FirebaseMessagingService`** + tách helper. | Mobile: 🆕 `features/notifications/domain/entities/delivery_notification.dart`, 🆕 `core/services/notification_permission_helper.dart`, ✏️ `core/services/firebase_messaging_service.dart`, ✏️ `notification_list_page.dart`. BE `notification-service`: 🆕 `dto/DeliveryStatusNotificationRequest.java`, 🆕 `service/DeliveryNotificationService.java`, ✏️ `controller/NotificationController.java`; `common-lib` (+routing key `delivery.status.changed`). | Không sửa migration cũ; không đổi DB `fcm_tokens` (giữ theo user); không đổi luồng order/locker/payment; không thêm live map/websocket/MQTT; không expose `/internal/**` qua gateway; không commit secret. | Endpoint mới `POST /internal/notifications/delivery-status`, event mới `delivery.status.changed`. Mobile deep-link `order_detail` từ noti + lịch sử + auto-refresh. Không đổi DB schema. Verify: `flutter analyze` + `mvn -pl notification-service -am compile`. Luồng drone bắn status thật vẫn TODO (test bằng gọi internal endpoint). |
| 2026-06-28 | mobile `feat/mobile-drone-mission-planner` | **Telemetry & điều khiển real-time MAVLink (Flight Data, Mảng 2)**. Kết nối flight controller ArduPilot/PX4 qua **MAVLink** (tự viết codec thuần Dart v1+v2, CRC-16/MCRF4XX + CRC_EXTRA chuẩn — không thêm dependency), transport **UDP + TCP** (`dart:io`, chạy với drone thật qua WiFi/bridge + ArduPilot SITL để test). Hiển thị vị trí drone live trên bản đồ (marker xoay theo heading + vệt đường bay), **HUD** (chân trời nhân tạo + độ cao/tốc độ/pin/GPS/mode/armed/link), gửi lệnh ARM/DISARM/RTL/LOITER/AUTO/GUIDED + REQUEST_DATA_STREAM. USB-serial/Bluetooth để abstraction sẵn, làm sau (cần plugin native). | Mobile (`smart-laundry-locker-mobile`): feature mới `lib/features/drone_telemetry/**` (data `mavlink_crc.dart`/`mavlink_protocol.dart`/`mav_connection.dart`; domain `mav_messages.dart`/`drone_telemetry.dart`/`flight_modes.dart`; presentation `flight_data_page.dart`/`flight_data_controller.dart`/widgets `attitude_indicator.dart`); route mới + nút entry trong tab Drone của `maintenance_home_page.dart`; test `test/features/drone_telemetry/mavlink_protocol_test.dart`. | Không sửa backend/API/DB/migration/event; không đụng Mảng 1 đã làm (drone_mission) ngoài việc thêm entry; không thêm dependency mới (dùng `dart:io`/`flutter_map`); không expose secret. | Chỉ thêm UI + protocol mobile offline cho vai trò MAINTENANCE (mở từ tab Drone). Không đổi API contract/DB/event/role. Tương thích ArduPilot/PX4 thật qua UDP/TCP. Verify: `flutter analyze` + unit test codec MAVLink round-trip. |
| 2026-06-28 | mobile `feat/mobile-drone-mission-planner` | **Mission Planner — Lập kế hoạch bay (Flight Plan, Mảng 1)**. Thuần frontend + data model, không cần drone thật. Vẽ/sửa/xóa/sắp xếp waypoint trên bản đồ `flutter_map`, set độ cao + lệnh MAVLink cho từng điểm (TAKEOFF/WAYPOINT/LOITER_TIME/LOITER_TURNS/LOITER_UNLIM/RTL/LAND/CHANGE_SPEED), nối thành đường bay (polyline đánh số), set HOME, tính tổng quãng đường (haversine) + ETA theo cruise speed, lưu library cục bộ (`shared_preferences`), **export/import file mission chuẩn QGC WPL 110** (`.waypoints`, tương thích Mission Planner/QGroundControl) **+ JSON nội bộ** qua `file_picker`. Mảng 2 (telemetry MAVLink real-time) để phiên sau. | Mobile (`smart-laundry-locker-mobile`): feature mới `lib/features/drone_mission/**` (domain `mav_command.dart`/`mission_item.dart`/`flight_mission.dart`; data `mission_codec.dart`/`mission_repository.dart`; presentation `mission_planner_page.dart`); route mới trong `core/routing/app_router.dart`; nút entry trong tab Drone của `features/locker_ops/presentation/pages/maintenance_home_page.dart`; test `test/features/drone_mission/mission_codec_test.dart`. | Không sửa backend/API/DB/migration/event; không đụng drone fleet maintenance đã có (domain "con drone vật lý" trạng thái/pin — khác hẳn); không đổi flow customer/locker; không sửa migration; không thêm dependency mới (dùng `flutter_map`/`latlong2`/`file_picker`/`shared_preferences` đã có). | Chỉ thêm UI mobile + data model offline cho vai trò MAINTENANCE (mở từ tab Drone). Không đổi API contract/DB/event/role. File export tương thích GCS thật (ArduPilot/PX4). Verify: `flutter analyze` trên file mới + unit test codec round-trip. |
| 2026-06-22 | BE `feat/locker-drone-fleet-maintenance` + mobile `feat/mobile-drone-fleet-maintenance` | **Bảo trì đội drone (fleet)** — domain mới "con drone vật lý" (pin/trạng thái bay/kỹ thuật viên/nhật ký), khác ô tủ `cellType=DRONE`. BE: bảng `drone_units`/`drone_maintenance_logs` (migration V10, seed 3 drone demo vào CAB-DEMO-01); endpoint `/api/maintenance/drones**` (list/claim/status/battery/logs) + `/api/admin/lockers/drones` (tạo mới) — dùng lại route/RBAC `/api/maintenance/**`+`/api/admin/lockers/**` có sẵn, không đổi gateway. Mobile: tab thứ 5 "Drone" trong `MaintenanceHomePage` (danh sách + bottom sheet nhận xử lý/đổi trạng thái/cập nhật pin/nhật ký). | BE: `locker-service` (`model/Drone*`, `repository/Drone*`, `dto/Drone*`, sửa `LockerService.java`/`LockerController.java`, migration `V10__drone_units.sql`). Mobile: `locker_ops_service.dart` (+method drone), `maintenance_home_page.dart` (+tab Drone, `_DroneStatusChip`, `_DroneLogSheet`). | Không sửa migration cũ (V1-V9); không đổi gateway route/RBAC; không đụng `TechnicianHomePage`/`/api/technician/**` (UI generic IoT device cũ, không có backend, ngoài scope). | Endpoint mới `/api/maintenance/drones/**`, `/api/admin/lockers/drones`; bảng mới `drone_units`/`drone_maintenance_logs`; mobile MAINTENANCE có thêm tab "Drone". Pin/trạng thái bay vẫn nhập tay (chưa có telemetry thật từ drone). Verify: BE `mvn -pl locker-service -am compile` BUILD SUCCESS; mobile `flutter analyze` trên 2 file sửa — 0 issue. **Còn lại**: chưa test runtime E2E (cần deploy migration V10 + JWT MAINTENANCE thật để gọi `GET /api/maintenance/drones`); chưa có UI admin tạo drone trên web (chỉ có API, dùng seed demo để test mobile); gán drone cho 1 đơn/chuyến giao hàng cụ thể vẫn chưa làm. |
| 2026-06-22 | `develop` (WIP chưa branch riêng) | **Mobile UI/UX nâng cấp** (2026-06-22): (1) `VoucherDetailPage` — ticket card gradient, copyable code chip, bảng thông tin; `MyVouchersPage` dẫn vào từng voucher qua tap. (2) `PromotionDetailPage` — banner ảnh CachedNetworkImage + gradient overlay, ticket banner gradient, code copy, bảng thông tin, steps "Cách dùng"; wired từ cả `PromotionsPage` (tap card) và `_FlashSaleCard` trên home (rootNavigator: true để phủ bottom nav). (3) `AppEventBus` (`app_event_bus.dart`, MỚI): typed events `OrderChangedEvent/PaymentCompletedEvent/WalletUpdatedEvent/PaymentFailedEvent/ReportUpdatedEvent`; `NotificationProvider` parse `actionType` từ STOMP payload + FCM data và emit; `OrderPage` + `CustomerOrderDetailPage` subscribe → tự reload; `WalletProvider` subscribe → tự refresh balance. (4) 10 mock ô tủ/locker qua `_enrichLayout` (7 STANDARD + 3 DRONE, grid 2×5) trong `store_lockers_page.dart`. (5) Fix nav bar overlap: 4 điểm điều hướng trong ShellRoute đổi sang `rootNavigator: true` / thêm `padding.bottom` cho bottom sheet. | Mobile: `voucher_detail_page.dart` (MỚI), `my_vouchers_page.dart`, `promotion_detail_page.dart` (MỚI), `promotions_page.dart`, `promotion_model.dart` (thêm `isExpired` getter), `home_page.dart`, `app_event_bus.dart` (MỚI), `notification_provider.dart`, `order_page.dart`, `customer_order_detail_page.dart`, `wallet_provider.dart`, `store_lockers_page.dart`. | Không sửa backend/API/DB/migration; không xoá route/page cũ; không đổi STOMP/FCM infra; không sửa model cũ (chỉ thêm getter). | App tự refresh màn Đơn hàng + Chi tiết đơn + Số dư ví khi nhận event từ STOMP/FCM; trang chi tiết voucher/promotion hoạt động khi tap; lưới ô tủ luôn hiện đủ 10 ô demo. |
| 2026-06-21 | `feat/payment-wallet-multimethod` | **Thanh toán đa hình thức**: ví nội bộ (nạp→cộng dư→trừ khi trả), checkout đơn qua Ví/VNPay/MoMo/Tiền mặt, order tự đánh dấu PAID qua event, MoMo thật (gated env), admin điều chỉnh ví. | BE payment-service (`model/{Wallet,WalletTransaction}`, `repository/Wallet*`, `service/{WalletService,MomoService}`, sửa `PaymentService`/`PaymentController`, `dto/*`, `V3__wallet.sql`); order-service (`LockerOrder.payment_status`, `RabbitConfig`, `OrderPaymentEventListener`, `V5__order_payment_status.sql`); `api-gateway` route ví; `docker-compose` env MoMo/VNPay. Mobile: `feature_flags`, `wallet_provider`. Web: `api-paths`, `apis/admin/wallet.ts`+`index.ts`, `baseAPi` tag, `WalletModal.tsx`, `UserTable.tsx`. | Không sửa migration cũ; không bắt buộc thanh toán (chưa chặn cấp PIN); không commit secret (creds qua env). | Endpoint mới `POST /api/payments/checkout`, `GET /api/wallet`(+transactions), `GET/POST /api/admin/wallet/{userId}*`; MoMo callback/IPN thật. order-service consume `PAYMENT_COMPLETED`. Migration V3(payment)+V5(order). Verify: BE compile PASS, mobile analyze PASS, web build PASS. **Còn lại**: nút "thanh toán đơn" trong mobile locker_ops (chưa wire, tránh đụng legacy `orders`); E2E sandbox. |
| 2026-06-21 | (WIP chưa commit, đang ở `develop`/`main`) | Hoàn tất tính năng đăng ký user mobile qua Firebase (phone OTP + Google) + admin web cấp role ADMIN/MANAGER/MAINTENANCE đăng nhập được, theo `docs/project-artifacts/guides/MOBILE_SELF_REGISTER_PLAN.md`. Tiếp nối WIP dang dở + fix lỗi: duplicate import `Slf4j` (`FirebaseConfig`), duplicate key `logging:` (`application.yml`), `FirebaseAuthService` mobile sang google_sign_in v7 + route Google qua Firebase (trả Firebase idToken đúng để backend verify), mở rộng `RoleNameSchema` (web), xóa file rác `payload.json`. | BE: `auth-service` (`AuthService`/`AuthController`/`FirebaseConfig`/`SocialIdentity`/`SocialIdentityRepository`/`CreateAccountRequest`/`FirebaseLoginRequest`/`V2__auth_social_identities.sql`/`pom.xml`/`application.yml`), `user-service` (`UserController`/`AuthClient`/`AdminCreateUserRequest`). Mobile: `firebase_auth_service.dart`, `social_login_use_case.dart`, `auth_bottom_sheet.dart`, `login_provider.dart`, `auth_injection.dart`, data source/repository auth, `pubspec.yaml`, `android/app/build.gradle.kts`. Web: `CreateUserModal.tsx`, `UserTable.tsx`, `admin.schemas.ts`. | Không sửa migration cũ (chỉ thêm V2); không commit secret (Firebase JSON/`google-services.json`/mobile `.env`); không đụng flow locker/order/payment. | Endpoint mới `POST /api/auth/firebase` (public), `POST /internal/auth/accounts` (internal); đổi hành vi `POST /api/auth/register` (ép CUSTOMER) + `POST /api/admin/users` (tạo cả auth_account). Migration V2 mới. Verify: BE `mvn compile` BUILD SUCCESS; mobile `flutter analyze` 0 error; web `npm run build` PASS. **Còn lại:** test runtime E2E cần Firebase credential thật + native config (google-services.json); Facebook chưa bật. |
| 2026-06-19 | `chore/seed-100-records` | Phân tích service rác (`partner-service`, `staff-service`, `laundry-service`) và sinh 100 dòng dữ liệu mẫu thực tế cho mỗi bảng của 9 service đang dùng. | Thêm mới `scripts/generate_100_seed_data.py` và `scripts/seed-100-realistic-data.sql` | Không sửa schema DB hay logic backend. | Tạo file SQL ~580KB để insert dữ liệu thực tế giúp UI có data hiển thị; đã thêm hướng dẫn xoá containers thừa trên droplet. |
| 2026-06-19 | `feat/mobile-hide-broken-and-rebrand-logo` (repo `smart-laundry-locker-mobile`) | (1) Kiểm tra toàn bộ mobile: `flutter analyze` 0 error/0 warning (406 info lint cũ), `flutter build apk --debug` PASS — không có lỗi báo đỏ. File `android/build/reports/problems/problems-report.html` chỉ là 15 WARNING deprecation Groovy DSL từ plugin (không phải lỗi project, không chặn build). (2) Thay logo app bằng logo tủ khóa navy người dùng cung cấp (`~/Downloads/logo512.png`). (3) Tạm ẩn các tính năng backend chưa có endpoint (gọi vào 404, sinh lỗi đỏ + UI rỗng): ví/số dư, nạp tiền/giao dịch, gói dịch vụ (subscription), kho voucher cá nhân, quảng cáo + blog trang chủ. | Mobile: thêm `lib/core/config/feature_flags.dart` (cờ tắt UI); `home_repository.dart` + `wallet_provider.dart` (short-circuit không gọi mạng); `home_page.dart` (ẩn card số dư + section ví courier); `profile_page.dart` (ẩn menu Gói dịch vụ/Ưu đãi&Quà tặng/Giao dịch). Logo: `assets/images/logo.png`, `android/.../mipmap-*/ic_launcher.png`, `ios/.../AppIcon.appiconset/*`, `pubspec.yaml` (`flutter_launcher_icons.android: true`). | Không sửa backend/API/DB/migration/event; không xóa route/page (chỉ ẩn entry-point bằng cờ, dễ bật lại); không đụng flow SEND/RENTAL locker_ops thật (không phụ thuộc ví); không sửa legacy `confirm_rent_page`/`locker_action_page` (code chết, không reachable); không đụng warning KGP/Gradle của plugin. | Chỉ ảnh hưởng UI mobile khách hàng: ẩn ví/topup/subscription/voucher/ads/blog cho tới khi backend có service tương ứng. Trang "Ưu đãi" (`/api/promotions/active`) vẫn hoạt động bình thường. Đổi icon launcher + logo trong app. Không đổi API contract/DB/event/role. |
| 2026-06-18 | `feat/payment-vnpay-wallet-topup` | Implement VNPay wallet topup flow cho mobile app: tạo endpoint `POST /api/payments/topup/create` và alias callback `GET /payments/vnpay/callback` trong payment-service; fix path mobile `/payments/topup/create` → `/api/payments/topup/create`; thêm proper exception handling cho `NotFoundException` và các exception khác trong repository mobile. | BE: `payment-service/.../dto/CreateTopupRequest.java`, `TopupResponse.java`, `PaymentRepository.java`, `PaymentService.java`, `PaymentController.java`, `V2__allow_topup_payments.sql`; `api-gateway/.../application.yml`, `JwtGatewayFilter.java`. Mobile: `top_up_remote_data_source_impl.dart`, `top_up_repository_impl.dart`. | Mobile: hết lỗi "Not Found" khi bấm "Nạp ngay". Chưa test E2E thật với VNPay sandbox credential; chưa implement wallet/balance service để cộng số dư sau topup. |
| 2026-06-17 | `fix/auth-async-email-login` | Sửa lỗi đăng nhập cực chậm do hệ thống chặn đợi gửi email OTP (bằng cách sửa proxy `@Async` trên interface) | `auth-service/.../EmailService.java` | Không đổi logic nghiệp vụ, không đổi contract | Khắc phục đăng nhập treo vài giây; API trả về kết quả ngay lập tức. |
| 2026-06-17 | `feat/auth-beautiful-otp-email` | Nâng cấp email OTP sang HTML chuyên nghiệp, đẹp mắt | `auth-service/.../EmailOtpService.java`, `auth-service/.../SmtpEmailService.java` | Không đổi logic nghiệp vụ, không đổi contract | Thay đổi diện mạo email OTP, thêm `@Async` cho HTML mailer để không block thread gửi OTP. |
| 2026-06-16 | `feat/maintenance-ui-redesign` (repo `smart-laundry-locker-mobile`) | Làm lại UI `MaintenanceHomePage` (mobile only) — giao diện cũ/rối (AppBar màu mận đỏ cứng, không dùng design kit) chuyển sang đồng bộ `AISLShadcnTheme`/`ops_widgets.dart` như các màn customer đã revamp trước đó. Giữ nguyên 100% chức năng (4 tab, claim/resolve/work-log/SLA/lịch định kỳ/mở khẩn cấp/rating/lưới ô tủ). | `maintenance_home_page.dart` (sửa chính), `ops_widgets.dart` (+`OpsSheetAction` public), `my_locker_orders_page.dart` (đổi `_SheetAction` private → `OpsSheetAction`). | Không đổi tên method/state/API call nào; không đổi backend/contract; không đổi 4 tab hay nhãn/đếm của chúng. | Chỉ ảnh hưởng UI mobile vai trò MAINTENANCE; không đổi API/DB/event. |
| 2026-06-16 | `feat/maintenance-iot-sync-hardening` (3 repo: backend, mobile, web FE) | Audit + đóng backlog P0/P1/P2 từ phân tích "IoT↔User↔Maintenance còn thiếu/chưa đồng bộ": force-open thật + audit log mở ô, PIN/QR brute-force lockout, TTL backstop cho ô RESERVED, contact info khách trên report, rating report sau khi RESOLVED, web admin parity (lifecycle actions đã có ở `layout-view.tsx`, bổ sung thêm lịch bảo trì định kỳ), enforce size khi reserve, device health dashboard. | BE: `iot-service` (migration V2, `BoxAccessLog`/`AccessAttempt`, force-unlock, device-status list), `locker-service` (migration V8/V9, `IotClient`/`UserClient`, force-open, TTL sweep, size fallback, report rating), `common-lib` không đổi, `api-gateway` (+1 path predicate). Mobile: `locker_ops_service.dart`, `maintenance_home_page.dart`, `my_reports_page.dart`, `send_parcel_page.dart`. Web FE: `lockerOps.ts`, `layout-view.tsx`, `BoxSettingModal.tsx`, `Admin/maintenance/index.tsx`. | Bỏ qua P2 #9 (sync trạng thái ô→MQTT, chờ hardware thật) và P2 #10 (payment gating, giữ miễn phí cho demo) theo quyết định user; không đụng `smart-locker-iot` (force-open dùng lại đúng cơ chế unlock/simulator đã có); không sửa `docker/postgres/init-databases.sql`. | Endpoint mới `/api/maintenance/boxes/{id}/force-open`, `/api/lockers/reports/{id}/rate`, `/api/manage/iot/device-status`, `/internal/iot/force-unlock`; migration mới (không sửa cũ); UI mobile + web admin + gateway route. |
| 2026-06-16 | FE `feat/point-clients-to-deployed-server` + mobile `feat/point-clients-to-deployed-server` + BE docs `docs/point-clients-to-deployed-server` | Trỏ FE + mobile (và do đó cả database vì client đi qua gateway) về backend đã deploy trên DigitalOcean `http://146.190.84.136:8080`. Chốt chính sách: cả nhóm dùng server + DB trên droplet, không bật docker local trừ khi sửa code. Xác nhận auto-deploy backend khi merge vào nhánh chính (`develop`) đã có sẵn (`deploy-droplet.yml` → `docker compose up -d --build` → Flyway migrate DB lúc service khởi động). | FE: `fe/.env`, `fe/vite.config.ts`, `fe/src/constants/api-paths.ts`. Mobile: `.env.example` (committed default `env_config.g.dart`/`env_config.dart` đã sẵn server). BE: chỉ living docs + mirror. | Không sửa code Java/migration/compose/CI; không sửa gateway CORS (origin `http://localhost:3000` của FE dev đã được phép); không commit mobile `.env` (private, không track); không động `iot/tablet-web` (sub-app cabinet, ngoài scope). | Ảnh hưởng base URL của web admin + mobile (đều trỏ server). DB của client đi gián tiếp qua gateway server. Không đổi API contract/DB schema/event. **Lưu ý vận hành:** lúc kiểm tra, gateway `:8080` trên droplet đang KHÔNG listen (SSH 22 OK, 8080 "connection refused") → stack backend cần được bật lại trên droplet để client kết nối được. |
| 2026-06-16 | `feat/booking-iot-reorder-maintenance-loop` (3 repo: backend, mobile, `smart-locker-iot`) | Đồng bộ luồng booking tủ từ mobile: (1) mô phỏng kết nối IoT↔mobile để mở tủ (vì hardware thật chưa có); (2) bổ sung "đặt lại đơn" (reorder) hoạt động đúng cho SEND/RENTAL; (3) customer↔maintenance "qua lại" qua notification khi claim/resolve report + màn "Báo cáo của tôi". | BE: `order-service/.../OrderService.java` (`reorder()`), `common-lib/.../DomainEventNames.java`, `locker-service/.../LockerService.java`, `notification-service/.../NotificationService.java`. Mobile: `locker_ops_service.dart`, `my_locker_orders_page.dart`, `my_reports_page.dart` (mới), `app_router.dart`. IoT: `simulate_demo_cabinet.py` (mới, file độc lập), `README.md`. | Không sửa `main.py`/`serial_manager.py`/`setup_handler.py`/hardware-track của `smart-locker-iot` (track thật của người khác); không sửa migration cũ; không đụng `report_list_page.dart`/`create_report_page.dart` legacy; không expose `/internal/**`. | Ảnh hưởng API `/api/orders/{id}/reorder` (sửa logic), event mới `locker.report.claimed/resolved`, notification consumer, mobile UI luồng tủ (mở tủ/đặt lại đơn/báo cáo của tôi); không đổi migration/DB schema. |
| 2026-06-15 | `chore/full-demo-seed` | Tạo seed demo đầy đủ (idempotent) cho toàn bộ MS DB: 4 tài khoản đặt sẵn (admin/customer/maintenance/manager, pw `12345678`) + 100 bản ghi/bảng nghiệp vụ, để test luồng A→Z. | Thêm `scripts/seed-full-demo-ms.sql`; cập nhật living docs + mirror. | Không sửa migration/schema, không sửa code service, không đụng `docker/postgres/init-databases.sql`. | Chỉ thêm DỮ LIỆU demo (dải id ≥9001/≥90001, marker `*-DEMO-*`); không đổi schema/API/UI. Áp lên DB đã chạy (local PASS; droplet do user tự chạy vì agent bị chặn ghi prod DB). |
| 2026-06-15 | `fix/mobile-token-refresh-and-startup` (repo `smart-laundry-locker-mobile`) | Khắc phục lỗi/cảnh báo khi `flutter run`: (1) 401 token hết hạn → app logout ngay; (2) Firebase duplicate-app nuốt lỗi làm bỏ qua messaging init; (3) build chậm/giật do Gradle heap 8G trên máy 16GB chạy kèm Docker+emulator. | `lib/core/network/auth_interceptor.dart` (refresh-on-401), `lib/main.dart` (guard Firebase init), `android/gradle.properties` (heap 8G→3G + parallel/caching). | Không sửa backend/contract API, không đổi UI flow nghiệp vụ, không migrate KGP (chỉ là cảnh báo forward-compat), không đụng emulator/hardware. | Ảnh hưởng vòng đời phiên đăng nhập mobile (tự refresh thay vì đá ra) + khởi tạo Firebase messaging + tốc độ build local; backend/DB/web không đổi. |
| 2026-06-15 | FE `fix/admin-login-2fa-response-normalization` + BE `fix/gateway-cors-globalcors-prefix` | Sửa crash trang `/admin/dashboard` (`Cannot read properties of undefined (reading 'toString')`): dashboard đọc 11 field nhưng backend overview chỉ trả `totalOrders`+`byStatus`. | FE `fe/src/pages/Admin/dashboard/hooks/useDashboard.ts` (normalize overview đủ field, default 0); BE `order-service/.../OrderService.statistics()` (thêm `ordersToday/pendingOrders/totalRevenue/revenueToday`). | Không gọi chéo service trong order-service (cross-service KPI vẫn để FE default 0), không sửa migration/DB, không đổi route/RBAC. | Ảnh hưởng response `/api/admin/dashboard/overview` (thêm key, backward-compatible) + render dashboard web; DB/event/mobile không đổi. |
| 2026-06-15 | `fix/admin-login-2fa-response-normalization` (repo `laundry-locker-frontend`) | Sửa crash sau khi nhập OTP admin trên web: `Cannot read properties of undefined (reading 'id')` do FE đọc `data.user.id` trong khi backend trả payload phẳng (không có key `user`). | `laundry-locker-frontend/fe/src/context/auth-context.tsx` (chuẩn hoá response phẳng của `verify-2fa`, giữ email bước 1). | Không sửa backend/DTO `verify-2fa`, không đổi route/RBAC, không đụng `.env`/`vite.config.ts` (config local của user), không đổi luồng partner OTP. | Chỉ ảnh hưởng chuẩn hoá response phía web admin login; API contract backend không đổi. |
| 2026-06-15 | `fix/gateway-cors-globalcors-prefix` | Sửa lỗi web admin không đăng nhập được: preflight CORS bị chặn (`No 'Access-Control-Allow-Origin'`) do `globalcors` đặt sai prefix YAML của Spring Cloud Gateway 4.3.x. | `api-gateway/src/main/resources/application.yml` (chuyển `globalcors` về `spring.cloud.gateway.server.webflux.globalcors`, thêm method `PATCH`). | Không sửa route/RBAC/JWT filter, không đổi service contract/DB/migration/event, không đụng code FE/mobile. | Chỉ ảnh hưởng cấu hình CORS của gateway cho web FE `http://localhost:3000`; API contract/DB/UI không đổi. |
| 2026-06-14 | `fix/notification-runtime-smoke` | Hoàn tất phần còn dang dở của realtime notification: tách/kiểm thử STOMP JWT principal, bổ sung smoke verification có thể chạy lại khi môi trường ổn định, và cập nhật trạng thái rủi ro. | Dự kiến chạm `notification-service/src/main/java/**`, `notification-service/src/test/**`, có thể thêm `scripts/**`; cập nhật 2 file sống + mirror. | Không sửa migration cũ, không commit secret/mobile `.env`, không expose `/internal/**`, không đổi FE/mobile UI flow. | Ảnh hưởng verification/WebSocket notification; REST API/database/mobile UI không đổi. |
| 2026-06-14 | `fix/realtime-notification-smoke` | Xử lý rủi ro sau merge: harden realtime notification end-to-end, thêm/sửa client WebSocket nếu khả thi, và bổ sung smoke verification cho notification/maintenance directions. | Dự kiến chạm `notification-service`, `smart-laundry-locker-mobile/lib/features/notifications/**`, có thể thêm test/smoke script/docs; cập nhật 2 file sống + mirror. | Không sửa migration cũ, không commit secret/mobile `.env`, không expose `/internal/**`, không đụng partner/laundry-service legacy. | Ảnh hưởng API realtime notification/WebSocket/mobile notification; FCM push thật vẫn phụ thuộc Firebase credential/thiết bị deploy. |
| 2026-06-14 | `feat/maintenance-user-realtime-flows` | Triển khai vertical slice nâng cấp trải nghiệm role theo hướng production: ưu tiên Maintenance chi tiết trên mobile/web, bổ sung đường đi tới tủ/lỗi cho user/maintenance, và khảo sát/nối realtime chat/thông báo nếu backend hiện có cho phép. | Dự kiến chạm `smart-laundry-locker-mobile/lib/features/**`, `laundry-locker-frontend/fe/src/**`, có thể chạm `notification-service`/docs nếu cần API realtime; cập nhật living docs và mirror. | Không sửa migration cũ đã chạy, không đụng `laundry-service`/`partner-service` legacy, không đổi deploy infra/secret/local env, không expose `/internal/**` qua gateway. | Có thể ảnh hưởng mobile UI, web admin/ops UI, API/notification/WebSocket; chỉ thêm migration mới nếu thật sự cần đổi DB. |
| 2026-06-14 | `docs/locker-flows-standard-spec` | Ghi chính thức 5 quyết định kiến trúc (K8s/Helm/GitOps, Kafka, CQRS/ES, GraphQL, service mesh) dưới dạng ADR — đều là "hoãn/chưa áp dụng now" kèm điều kiện xem lại + phác thảo cho CodeX. Chỉ tài liệu. | `docs/ARCHITECTURE_DECISIONS.md` (mới); pointer trong 2 file sống; mirror. | Không sửa code/compose/CI/migration; không deploy K8s/Kafka/mesh. Chỉ tài liệu governance. |
| 2026-06-14 | `docs/locker-flows-standard-spec` | Phân tích luồng tủ (as-is, bám code locker/order/iot) + đặc tả toàn bộ luồng nghiệp vụ tủ khóa chuẩn thực tế (to-be) + gap map + backlog. Tài liệu blueprint, chưa code. | `docs/project-artifacts/guides/LOCKER_FLOWS_STANDARD_SPEC.md` (mới), `docs/BUSINESS_FLOWS_CURRENT.md`, `docs/PROJECT_PROGRESS_TRACKER.md`, mirror artifact backend/docs. | Không sửa code Java/migration/business logic, không đổi API/DB/event/UI runtime, không stage `docker/postgres/init-databases.sql`, không commit file private/secret. | Chỉ tài liệu; không đổi hành vi runtime. Định hướng cho các giai đoạn implement L1–L7 sau. |
| 2026-06-13 | `chore/backend-production-phase1` | Production hardening Phase 1 đã implement, verify, commit và push trên branch riêng; chờ merge nếu cần. | Backend Maven modules, CI workflow, cấu hình runtime service, living docs. | Không sửa nghiệp vụ locker/order/payment hiện có, không sửa migration cũ đã chạy, không stage `docker/postgres/init-databases.sql` đang bị xoá local từ trước, không commit file private/secret. | Ảnh hưởng kỹ thuật vận hành backend; không đổi API nghiệp vụ/database/event/UI/mobile. |
| 2026-06-13 | `chore/backend-production-phase2` | Production hardening Phase 2 đã implement, verify, commit và push trên branch riêng; chờ merge nếu cần. | Backend Maven modules, service `application.yml`, GitHub Actions, docs. | Không sửa nghiệp vụ locker/order/payment hiện có, không sửa migration cũ đã chạy, không stage `docker/postgres/init-databases.sql` đang bị xoá local từ trước, không commit file private/secret. | Ảnh hưởng kỹ thuật vận hành/backend quality; không đổi API nghiệp vụ/database/event/UI/mobile. |
| 2026-06-13 | `chore/backend-production-phase3-4` | Production hardening Phase 3/4 đã implement, verify, commit và push trên branch riêng; chờ merge nếu cần. | `api-gateway` tests/config, root Maven config, GitHub Actions, docs/artifact mirrors. | Không sửa nghiệp vụ locker/order/payment hiện có, không sửa migration cũ đã chạy, không stage `docker/postgres/init-databases.sql` đang bị xoá local từ trước, không commit file private/secret. | Ảnh hưởng kỹ thuật vận hành/API docs/CI; không đổi API nghiệp vụ/database/event/UI/mobile. |
| 2026-06-13 | `chore/backend-production-phase4-provenance` | Production hardening Phase 4 continuation đã implement, verify, commit và push trên branch riêng; chờ merge nếu cần. | GitHub Actions release/deploy workflow, `scripts/`, living docs/artifact mirrors. | Không sửa nghiệp vụ locker/order/payment hiện có, không sửa migration cũ đã chạy, không stage `docker/postgres/init-databases.sql` đang bị xoá local từ trước, không commit file private/secret. | Ảnh hưởng CI/CD/release supply-chain; không đổi API nghiệp vụ/database/event/UI/mobile. |

## Bản Đồ Repository

| Khu vực | Đường dẫn | Branch quan sát gần nhất | Trạng thái |
|---|---|---|---|
| Backend | `laundry-locker-microservices/` | `chore/backend-production-phase4-provenance` | Source backend chính; Phase 4 provenance/release hardening đang ở branch riêng, có một xoá local ngoài scope tại `docker/postgres/init-databases.sql` không được stage. |
| Web frontend | `laundry-locker-frontend/` | `main` | Có admin locker/maintenance và các fix TypeScript. |
| Flutter mobile | `smart-laundry-locker-mobile/` | `develop` | Có module locker ops và role routing. |
| IoT runtime | `smart-locker-iot/` | chưa check trong pass gần nhất | Python runtime/simulation cho cabinet. |
| Workspace root | `G:\BigProject` | không phải git repo | Chứa docs/scripts/local secret notes liên project. |

Không commit:

- `env.txt`
- `pro.txt`
- `Application.txt`
- local host/secret notes
- mobile `.env` nếu chưa sanitize có chủ đích

## Bảng Tiến Độ Theo Component

Ý nghĩa status:

- `DONE`: đã implement và verify đủ cho demo hiện tại.
- `PARTIAL`: có code nhưng chưa verify đầy đủ, còn mismatch legacy, hoặc phụ thuộc service/integration đang thiếu.
- `TODO`: chưa implement.
- `BLOCKED`: không thể tiếp tục nếu thiếu credential, server access, hoặc quyết định product.

| Component / Flow | Status | Bằng chứng | Bước tiếp theo |
|---|---|---|---|
| Auth register/login/JWT | DONE | `auth-service`, login smoke với customer/manager/maintenance pass. **2026-06-21**: self-register ép `CUSTOMER` (bỏ honor `roles` client); thêm `POST /api/auth/firebase` verify Firebase ID token (phone OTP + Google qua 1 endpoint, bảng mới `social_identities`, migration V2); `POST /internal/auth/accounts` + `POST /api/admin/users` giờ tạo cả `auth_account` để admin cấp role MANAGER/MAINTENANCE/ADMIN login được. Verify tĩnh: BE `mvn compile` BUILD SUCCESS, mobile `flutter analyze` 0 error, web `npm run build` PASS. **2026-06-21 (chiều)**: thêm đăng nhập **Facebook** (`flutter_facebook_auth` + native config), build apk PASS; đã commit 3 nhánh feature + push; merge develop/main chờ user (qua PR). | Test runtime E2E cần Firebase credential thật trên server; Facebook cần bật provider trong Firebase + redirect URI. |
| User profile/roles | DONE | `user-service`, `user_profiles`, admin user APIs. | Đảm bảo deployed seed roles có `MANAGER`/`MAINTENANCE` nếu cần demo. |
| Gateway routing/RBAC | DONE | Guard cho `/api/admin/**`, `/api/manage/**`, `/api/maintenance/**`, `/internal/**`; gateway chỉ authorize JWT `tokenUse=access`; có unit tests cho internal block, refresh-token reject, admin/manage/maintenance RBAC, public OpenAPI/catalog read. CORS cho web FE (`http://localhost:3000`) cấu hình tại `spring.cloud.gateway.server.webflux.globalcors` (fix 2026-06-15, trước đó đặt sai prefix `deprecated` nên web login fail). | Cập nhật route table khi thêm service; bổ sung end-to-end gateway tests khi có harness chạy đủ service. |
| Locker physical cell model | DONE | V2/V3 migrations, `DRONE/STANDARD/XL`, layout endpoint. | Chỉ thêm cell status `EXPIRED` nếu product cần. |
| Locker fault/maintenance flow | DONE | V4 migration, API claim/resolve report, FE/mobile pages. **2026-06-16 (sáng)**: customer giờ được notify (event `locker.report.claimed/resolved`) khi maintenance claim/resolve báo cáo của mình; mobile có màn "Báo cáo của tôi" (`GET /api/lockers/my-reports`). **2026-06-16 (chiều)**: report giờ có `reporterName`/`reporterPhone` (tra qua `user-service`, best-effort) cho maintenance xem; customer đánh giá report sau khi RESOLVED (`POST/GET /api/lockers/reports/{id}/rate|rating`), maintenance xem điểm trung bình (`GET /api/maintenance/my-rating-average`). | Chưa expose `repair_logs` (work-log nội bộ KTV) cho customer; chưa có ảnh đính kèm work-log; chưa có role TECHNICIAN riêng. |
| Audit log mở ô (access log) | DONE | **2026-06-16**: bảng mới `iot_schema.box_access_logs` (`iot-service` V2) ghi mọi lần mở ô (actor, credential PIN_OR_QR/MASTER, kết quả) từ cả `unlock()` (khách) và `forceUnlock()` (maintenance). | Chưa có UI xem lại log (chỉ ghi DB); cân nhắc thêm trang admin xem audit trail nếu cần. |
| PIN/QR brute-force lockout | DONE | **2026-06-16**: bảng `iot_schema.access_attempts` (`iot-service` V2); `verifyAccess()` khóa box tạm (mặc định 15 phút sau 5 lần sai, cấu hình `app.iot.lockout.*`). | Chưa có cảnh báo/notify khi 1 box bị khóa nhiều lần (dấu hiệu tấn công). |
| Force-open khẩn cấp (maintenance) | DONE | **2026-06-16**: `POST /api/maintenance/boxes/{id}/force-open` (locker-service) → Feign `IotClient` → `POST /internal/iot/force-unlock` (iot-service, không cần PIN khách, luôn ghi audit log MASTER). Web admin: nút "Mở khẩn cấp" ở `layout-view.tsx` (mọi trạng thái ô) + đã nối `BoxSettingModal.tsx` (trước đó gọi route chết `/api/admin/lockers/boxes/{id}/force-open`). Mobile: action "Mở tủ khẩn cấp" trong bottom-sheet hành động ô (mọi trạng thái). | Trang `Admin/lockers/detail.tsx` + `BoxForceOpenModal.tsx` vẫn là code chết (không route tới, chỉ giả lập toast) — không sửa, không xóa, chỉ ghi nhận. |
| Device health dashboard | DONE | **2026-06-16**: `GET /api/manage/iot/device-status` (role MANAGER/ADMIN, route gateway `/api/manage/iot/**`) liệt kê toàn bộ `DeviceStatus` (trước đó có ghi nhưng không endpoint đọc, không UI). Web admin: section "Sức khỏe thiết bị" trong `Admin/maintenance/index.tsx` (badge online/offline + lastSeenAt). **2026-06-30**: `simulate_demo_cabinet.py` giờ **phát heartbeat** (học cabinet từ traffic open/sync + seed `SIM_HEARTBEAT_CABINETS`, định kỳ `{status:ONLINE}`) ⇒ dashboard có dữ liệu thiết bị ONLINE thật khi simulator chạy — trước đó luôn rỗng vì không ai publish `cabinet/{id}/heartbeat`. | Chưa tiêu thụ event `iot.device.status.changed` cho push realtime (đang là poll qua REST); chưa có UI mobile. |
| Cell-level RESERVED TTL backstop | DONE | **2026-06-16**: cột `reserved_until` (`locker-service` V8, mặc định 24h = cùng cửa sổ `app.order.auto-cancel-hours`), job `LockerScheduler.sweepExpiredReservations` (cron mỗi giờ) chỉ là backstop — order-service's sweep (mỗi 15 phút) đã release ô khi auto-cancel đơn nên đường này hiếm khi cần kích hoạt. | Không lưu `reserved_order_id` (box được reserve trước khi order có id — xem ghi chú trong migration V8); nếu cần đối soát chặt hơn, phải đổi thứ tự tạo order trong `OrderService.create()`. |
| Enforce size khi reserve | DONE | **2026-06-16**: `findAvailableBox` giờ fallback sang size lớn hơn theo thứ tự SMALL→MEDIUM→LARGE→XL khi không có ô đúng size (trước đó throw `NO_AVAILABLE_BOX` ngay). Mobile SEND có chọn kích thước (SMALL/MEDIUM/LARGE). | Demo seed (`V3__seed_demo_cabinet.sql`) chỉ có size MEDIUM/XL nên đường fallback chưa có dữ liệu thật để minh họa SMALL/LARGE; không sửa seed cho việc này. |
| SEND parcel flow | DONE | `POST /api/orders/send`, PIN hai giai đoạn, QR, receiver flow. **2026-06-16**: `reorder()` sửa để tái tạo đúng đơn SEND (gọi lại `createSend`) thay vì tạo đơn không có ô; mobile có nút "Đặt lại đơn" + "Mở tủ" (gọi `/api/iot/unlock`) trong chi tiết đơn. | Thêm payment UX nếu SEND là tính năng có phí. |
| RENTAL flow | DONE | `POST /api/orders/rental`, extend/end, tính giá theo giờ. **2026-06-16**: `reorder()` sửa để tái tạo đúng đơn RENTAL (gọi lại `createRental` với `cellType`/`hours` suy ra từ đơn cũ) thay vì sai giá/không có hạn thuê. | Nối provider payment nếu yêu cầu. |
| PIN/QR verify-access | DONE | `QrTokenService`, `POST /api/iot/verify-access`. **2026-06-16**: mobile gọi trực tiếp `POST /api/iot/unlock` (nút "Mở tủ" trong chi tiết đơn) — mô phỏng bằng `smart-locker-iot/simulate_demo_cabinet.py` vì chưa có hardware thật; đã verify round-trip MQTT thật qua `broker.hivemq.com`. | Build tablet-web/cabinet UX; thay simulator bằng `main.py` + setup handshake thật khi có Raspberry Pi/Arduino. |
| Laundry order legacy flow | PARTIAL | Core endpoints tồn tại và smoke cũ pass; chưa regression đầy đủ gần nhất. | Re-test full lifecycle sau mỗi thay đổi `order-service`. |
| Staff operations | DONE | **PA4 (2026-06-20)**: `StaffController` (locker-service) + `StaffOrderController` (order-service) — read-only; gateway RBAC `/api/staff/**` yêu cầu `STAFF\|ADMIN`; seed account `staff@lockr.test` (user_id 9005). Không có `staff-service` riêng, không có DB riêng. | Không có mobile/FE shell cho STAFF hiện tại. |
| Technician IoT operations | DONE | **PA4 (2026-06-20)**: `TechnicianController` (iot-service) — 5 endpoints quản lý device; gateway RBAC `/api/technician/**` yêu cầu `TECHNICIAN\|ADMIN`; seed account `tech@lockr.test` (user_id 9006); `DeviceStatusRepository.findByLockerId` và `BoxAccessLogRepository.findByLockerIdOrderByCreatedAtDesc` đã thêm. | Không có mobile/FE shell cho TECHNICIAN hiện tại. |
| Admin web dashboard | PARTIAL | Main admin routes tồn tại; build pass. Đăng nhập admin (CORS + 2FA + dashboard render) đã thông sau fix 2026-06-15: `/admin/dashboard` không còn crash; KPI đơn/doanh thu lấy thật từ `order-service`. | KPI cross-service (users/stores/lockers/boxes/services) đang hiển thị 0 — cần wire aggregation chéo service cho `/api/admin/dashboard/overview`. Browser smoke các trang admin còn lại. |
| Admin locker list/layout | DONE | `/admin/lockers`, `/admin/lockers/:lockerId`, RTK lockerOps. | Giữ UI khớp `LockerLayoutResponse`. |
| Admin maintenance page | DONE | `/admin/maintenance`, action report/fault; hiển thị tổng quan backlog, địa chỉ/toạ độ locker và nút chỉ đường cho fault/report. **2026-06-16**: thêm contact khách (SĐT) trên report, section "Bảo trì định kỳ" (tạo/đã-kiểm-tra/xóa lịch — trước đó chỉ mobile có) và section "Sức khỏe thiết bị". Action lifecycle ô (out-of-service/cleaning/return-to-service/force-open) đã có sẵn ở `/admin/lockers/:lockerId` (`layout-view.tsx`) từ trước, không phải thiếu như audit ban đầu tưởng — chỉ thiếu force-open thật (đã nối). | Browser smoke với admin token thật trên deploy. |
| Partner portal | Đã gỡ | Role `PARTNER`/permission `PARTNER_MANAGE`/account `partner.seed`/`partner-service` đã gỡ khỏi seed + docker-compose (2026-06-13). | FE partner routes là deprecated; dọn UI partner khi có thời gian. |
| Services/laundry catalog | PARTIAL | FE/admin docs tồn tại; backend `laundry-service` thiếu. | Build lại `laundry-service` hoặc route catalog qua service khác. |
| Payment/refund | PARTIAL | `payment-service` endpoints tồn tại; provider phụ thuộc environment; production profile đã fail-fast nếu còn config demo/sandbox/localhost. **2026-06-18**: thêm `POST /api/payments/topup/create` (tạo VNPay URL nạp ví), `GET /payments/vnpay/callback` (alias public cho mobile WebView detect), Flyway index V2; mobile path đã fix (`/api/payments/topup/create`). Chưa có wallet/balance service — số dư chưa thực sự được cộng sau topup. | (1) Verify VNPay/MoMo với credential sandbox/real. (2) **[BẮT BUỘC]** Implement wallet service: cộng balance sau VNPay callback COMPLETED (xem mục TODO trong BUSINESS_FLOWS_CURRENT.md mục 26). (3) Implement `GET /api/wallet/balance` để mobile đọc số dư. |
| Notifications/FCM/WebSocket | PARTIAL | Service endpoints/events tồn tại; public JWT endpoint `/api/notifications/fcm-tokens` hoạt động; notification-service xác thực cả WebSocket handshake header và STOMP `CONNECT` bằng JWT access token, bind principal `userId` cho `/user/queue/notifications`; runtime smoke local qua gateway PASS: list/count notification, FCM token save/delete, admin send -> private STOMP `MESSAGE`; RabbitMQ `DomainEvent` listener có allow-list converter và log conversion sạch. **2026-06-22**: `NotificationProvider` (mobile) giờ parse field `actionType` từ cả STOMP payload (`notification.dataPayload?.actionType` + `referenceId`) và FCM data (`message.data['type']` + `referenceId`), emit typed events lên `AppEventBus` singleton (`OrderChangedEvent/PaymentCompletedEvent/WalletUpdatedEvent/PaymentFailedEvent/ReportUpdatedEvent`) — các screen (`OrderPage`, `CustomerOrderDetailPage`, `WalletProvider`) subscribe để tự reload mà không cần pull thủ công. **2026-06-28 (push trạng thái giao hàng drone, phase noti-only)**: BE thêm `DeliveryNotificationService` + `POST /internal/notifications/delivery-status` + event `delivery.status.changed` — gửi noti 6 mốc (dispatched/approaching/arrived/delivered/delayed/failed) tới `receiverUserId` qua `sendToUser`, data payload `{orderId,status,eta,message,type:ORDER_STATUS_CHANGED,referenceId:orderId}`, lưu lịch sử. Mobile: `FirebaseMessagingService` thêm onTokenRefresh + deep-link `order_detail` theo `orderId` (cả 3 trạng thái app + tap local-notif foreground qua payload JSON), model `DeliveryStatus`, helper quyền + hướng dẫn Settings, lịch sử noti deep-link `ORDER_STATUS_CHANGED`. Xem `docs/BUSINESS_FLOWS_CURRENT.md` mục 16.1. | Verify FCM push thật trên thiết bị/Firebase credential production; luồng giao drone thật bắn `delivery.status.changed` vẫn TODO (hiện test bằng gọi internal endpoint). |
| Loyalty | PARTIAL | `loyalty-service` endpoints tồn tại. | Verify event integration và FE/mobile usage. |
| Store management | DONE | `store-service` endpoints và route admin/public. | Browser smoke admin stores nếu cần. |
| Flutter customer locker ops | DONE | Login/home quick actions/rental/send/my-orders smoke pass. UI 3 màn revamp về design system shadcn navy + action gate theo state machine; chi tiết đơn tủ có nút chỉ đường tới locker qua Google Maps/address fallback. **2026-06-15**: Lưới ô tủ 2D (`StoreLockerGridPage`) — lazy-load layout theo store, DRONE cell icon + màu indigo + non-tappable; booking bỏ picker khi đến từ lưới ô (skip API load, hiện card read-only). `flutter analyze` 0 error. **2026-06-16**: chi tiết đơn tủ thêm action "Mở tủ" (gọi `/api/iot/unlock`, primary action khi đơn còn active) và "Đặt lại đơn" (gọi `/api/orders/{id}/reorder`, khi COMPLETED/CANCELED); thêm màn mới "Báo cáo của tôi" (`/locker/my-reports`, đọc `GET /api/lockers/my-reports`) + link từ snackbar sau khi báo lỗi thành công. `flutter analyze` 0 error trên các file đã sửa/thêm. Màn "Báo cáo của tôi" giờ có thêm chấm sao đánh giá khi report `RESOLVED` (`POST/GET /api/lockers/reports/{id}/rate|rating`). SEND thêm chọn kích thước hàng (SMALL/MEDIUM/LARGE) gửi kèm `size` cho `/api/orders/send`. **2026-06-18**: Redesign home page: (a) section "Popular Lockers" horizontal scroll; (b) section "Flash Sale" horizontal scroll từ `GET /api/promotions/active` thật (thay thế 3 service cards cứng trước đó); (c) chip "Ưu đãi" giờ navigate tới `/promotions` (PromotionsPage, dùng API thật) thay vì `/my-vouchers` (gọi endpoint chết `/promotions/vouchers/my`); (d) tạo toàn bộ Promotions feature (model/repo/provider/page). `flutter analyze` 0 issue. **2026-06-22**: (e) `VoucherDetailPage` (MỚI) — ticket gradient (`UNUSED` có màu, `USED`/`EXPIRED` xám), notch divider, copyable code chip, bảng thông tin; `MyVouchersPage` dẫn vào từng voucher qua `GestureDetector` + `rootNavigator: true`. (f) `PromotionDetailPage` (MỚI) — banner ảnh 180px (`CachedNetworkImage` + overlay), ticket banner gradient (6 màu theo `id % 6`), copyable code, bảng thông tin (dùng `isExpired` getter mới trên `PromotionModel`), steps "Cách dùng"; wired từ `PromotionsPage` (tap card, regular push) và `_FlashSaleCard` (home, `rootNavigator: true` để phủ bottom nav); fix cũ: `_FlashSaleCard` trước đó gọi `context.push(AppRouter.promotions)` thay vì chi tiết. (g) `AppEventBus` (MỚI `app_event_bus.dart`): `NotificationProvider` emit typed events từ STOMP + FCM; `OrderPage` + `CustomerOrderDetailPage` subscribe → tự reload; `WalletProvider` subscribe → tự refresh balance. (h) `_enrichLayout` 10 mock ô tủ (row 0: 5 STANDARD; row 1: 2 STANDARD + 3 DRONE) khi API trả <10 ô hoặc lỗi. (i) Fix nav bar overlap: `rootNavigator: true` / `padding.bottom` cho 4 điểm điều hướng trong ShellRoute. | Smoke trên emulator với deploy cho toàn bộ UI (create/confirm/complete/extend/delegate/report/directions + voucher detail + promotion detail + realtime event refresh + lưới 10 ô + nav bar không bị phủ); chưa chạy trên emulator phiên này, chỉ verify tĩnh. |
| Flutter stores (customer) | DONE | Feature `lib/features/stores/**`: list (search + nearby), detail (info + ratings + directions), entry "Khám phá cửa hàng" từ home; gọi `/api/stores`, `/api/stores/{id}`, `/api/stores/{id}/ratings`. `flutter analyze` 0 error. | Manual smoke trên emulator với deploy có store data. |
| Flutter manager home | PARTIAL | Code và backend role login đã verify; manual UI smoke bị giới hạn. | Test manager UI trên emulator/device ổn định. |
| Flutter maintenance home | DONE | Màn hình `MaintenanceHomePage` đã hoàn tất luồng: chọn tủ, xem layout ô, báo hỏng, clear fault. Code đã analyze sạch; UI hoạt động với endpoint thật. **2026-06-16 (chiều)**: thêm action "Mở tủ khẩn cấp" (mọi trạng thái ô, có dialog xác nhận), hiện contact khách (SĐT) trên report card, banner điểm đánh giá trung bình của KTV. **2026-06-16 (tối) — làm lại UI**: chuyển từ AppBar màu mận đỏ + `Card`/`Container` tự vẽ tay sang `BrandHeroHeader` + `OpsCard`/`OpsSectionLabel`/`OpsEmptyState`/`OpsBanner` (đồng bộ design kit với các màn customer đã revamp); đổi locker picker từ `DropdownButtonFormField` sang `LockerPickerField` dùng lại từ Gửi hàng/Thuê tủ; cell-action bottom sheet đổi từ `ListTile` rời sang `OpsSheetAction` (widget mới promote lên `ops_widgets.dart`, dùng chung với `my_locker_orders_page.dart`); 3 dialog (force-open/report-fault/out-of-service) đồng bộ bo góc 20. **Không đổi tên method/state/API call nào** — chỉ đổi widget render. **2026-06-30**: tab "Kiểm tra tủ" thêm card "Tình trạng phần cứng ô" (`_boxHealthCard`/`_boxHealthRow`) gọi `GET /api/maintenance/lockers/{lockerId}/box-health` — trạng thái logic theo đơn cạnh trạng thái cửa phần cứng (GAP 2), nổi bật ô `needsAttention` (cửa mở mà không OCCUPIED); best-effort, rỗng → hint. Tab "Sự cố" thêm section "Cảnh báo phần cứng" (`_boxAnomaliesSection`, `GET /api/maintenance/box-anomalies`) gom ô bất thường mọi tủ + nút Chỉ đường. `flutter analyze` No issues. | Test trên thiết bị thật/emulator nếu cần — phiên này chỉ verify tĩnh (`flutter analyze` 0 lỗi), không có emulator chạy sẵn và cần đăng nhập tương tác nên chưa xem trực quan được. |
| Flutter legacy courier/logistics | PARTIAL | Nhiều route tồn tại; chưa align backend hiện tại trong pass gần nhất. | Audit hoặc remove/mark legacy. |
| Python IoT runtime | PARTIAL | `main.py` (hardware-track, chờ Raspberry Pi/Arduino + setup handshake — chưa có code BE gửi handshake này) vẫn PARTIAL. **2026-06-16**: thêm `simulate_demo_cabinet.py` (script độc lập, không đụng `main.py`) trả lời đúng payload Java đang gửi thật (`box_id`, không phải `lockerId`/`slotIndex` như `main.py` mong đợi) — đã verify round-trip MQTT thật. **2026-06-29**: simulator subscribe thêm `cabinet/+/command/sync` (booking→IoT sync, GAP 1) — log `[SIM] SYNC` khi `locker-service` báo ô đổi trạng thái lúc reserve/occupy/release/fault/clear. **2026-06-30**: simulator phát heartbeat `cabinet/{id}/heartbeat` (GAP 3) — học cabinet từ traffic + seed env, định kỳ `{status:ONLINE}` để device-health dashboard có dữ liệu. **2026-06-30 (GAP 2)**: sau khi mở thành công, simulator báo trạng thái cửa `cabinet/{lockerId}/locker/{boxId}/status` OPEN→CLOSED → iot-service persist vào `box_hardware_status` (đọc qua `GET /api/manage/iot/box-status`). | Đồng bộ broker và setup handshake thật khi có hardware; cho tới đó dùng simulator cho demo. |
| Tablet-web cabinet UI | TODO | Mới được nhắc trong docs. | Build dựa trên layout + verify-access. |
| Mission Planner — Lập kế hoạch bay (Flight Plan, Mảng 1) | DONE (mobile, offline) | **2026-06-28**: feature mobile mới `lib/features/drone_mission/**` — vẽ/sửa/xóa/sắp xếp waypoint trên `flutter_map`, set độ cao + lệnh MAVLink (TAKEOFF/WAYPOINT/LOITER_TIME/LOITER_TURNS/LOITER_UNLIM/RTL/LAND/CHANGE_SPEED), nối đường bay (polyline đánh số), set HOME, tổng quãng đường (haversine) + ETA, lưu library `shared_preferences`, **export/import QGC WPL 110 `.waypoints`** (tương thích Mission Planner/QGroundControl) **+ JSON nội bộ** qua `file_picker`. Entry từ tab "Drone" trong `MaintenanceHomePage`, route `/drone/mission-planner`. `flutter analyze` 0 issue; codec test 7/7 PASS. Xem `docs/BUSINESS_FLOWS_CURRENT.md` mục 11.2. | Mảng 2 (telemetry MAVLink real-time: kết nối UDP/TCP/Serial/BT, HUD, gửi lệnh) chưa làm. Chưa smoke trên emulator (chỉ verify tĩnh + unit test). Drag marker trực tiếp trên map chưa có (dùng chế độ "di chuyển" + chạm lại). |
| Drone fleet maintenance | PARTIAL | **2026-06-22**: domain mới `DroneUnit`/`DroneMaintenanceLog` trong `locker-service` (migration V10) — trạng thái IDLE/CHARGING/IN_FLIGHT/MAINTENANCE/FAULT, % pin, kỹ thuật viên phụ trách, nhật ký bảo trì; endpoint `/api/maintenance/drones/**` + `/api/admin/lockers/drones`; mobile tab "Drone" trong `MaintenanceHomePage`. Pin/trạng thái bay nhập tay, chưa có telemetry thật. Xem `docs/BUSINESS_FLOWS_CURRENT.md` mục 11.1. | Verify runtime E2E qua gateway sau khi deploy migration V10; UI admin web tạo drone (hiện chỉ có API). |
| Real drone delivery service | TODO | Thư mục drone có prototype docs/code (precision-landing Raspberry Pi/MAVLink), chưa có backend điều khiển bay/giao hàng thật. | Thiết kế `drone-service` cho điều khiển bay + gán nhiệm vụ giao hàng. |
| Battery-aware assignment | TODO | Drone fleet đã có field pin/trạng thái (xem trên) nhưng chưa có logic gán drone cho 1 đơn/chuyến giao hàng cụ thể theo pin. | Implement sau khi có drone delivery service. |
| Realtime drone telemetry & điều khiển (Flight Data, Mảng 2) | DONE (mobile, MAVLink trực tiếp) | **2026-06-28**: feature mobile mới `lib/features/drone_telemetry/**` — codec **MAVLink** v1/v2 tự viết (CRC-16/MCRF4XX khớp check-value chuẩn `0x6F91`, CRC_EXTRA common.xml), transport **UDP+TCP** (`dart:io`), `FlightDataPage`: bản đồ vị trí drone live (marker xoay theo heading + vệt bay), HUD (chân trời nhân tạo + alt/speed/climb/heading/pin/GPS/mode/armed/link), lệnh ARM/DISARM/RTL/LOITER/GUIDED/AUTO/TAKEOFF + heartbeat 1Hz + REQUEST_DATA_STREAM. Entry từ tab "Drone" → route `/drone/flight-data`. `flutter analyze` 0 issue; codec test 9/9 PASS. Tương thích ArduPilot/PX4 thật qua WiFi/bridge + SITL. Xem `docs/BUSINESS_FLOWS_CURRENT.md` mục 11.3. | Chưa smoke với SITL/drone thật phiên này (kết nối cần peer/simulator). USB-serial + Bluetooth chưa làm (abstraction `MavConnection` đã sẵn — cần plugin native Android). Chưa vẽ mission đã lập (Mảng 1) chồng lên map Flight Data. |
| ~~Realtime drone tracking map~~ → thay bằng dòng trên | — | Trước là TODO "chưa có map/telemetry service"; nay đã làm trực tiếp qua MAVLink ở mobile (không cần WebSocket service trung gian). | — |
| AI/RAG support | TODO | Chưa có `ai-service`. | Chọn provider/storage rồi scaffold. |
| CI/CD | PARTIAL | Backend CI `mvn -B test`, backend security workflow Dependency Review/CodeQL, SBOM artifact, Trivy image scan gate cho 12 image có Dockerfile, build-info metadata, deploy workflow `mvn -B clean verify`, deploy artifact checksum + GitHub provenance attestation, tag-based release workflow, release SBOM/checksum, rollback script và verify-release script đã có. | Theo dõi CI trên remote; thêm SLSA policy enforcement, immutable release setting, blue-green/zero-downtime nếu cần production nghiêm ngặt hơn. |

## Chi Tiết Tiến Độ Backend

### Đã Xong

- Maven multi-module backend đã có.
- Database và schema theo service đã có.
- Gateway route table đã có.
- JWT identity propagation đã có.
- Gateway chặn `/internal/**` từ bên ngoài.
- Gateway chỉ cho token `tokenUse=access` truy cập API nghiệp vụ; refresh token chỉ dùng ở auth refresh flow.
- Secret policy dùng chung trong `common-lib` cho JWT/QR/payment production guard:
  - Không tự pad HMAC secret ngắn.
  - JWT/QR secret phải có tối thiểu 32 UTF-8 bytes.
  - Profile `prod`/`production` fail-fast nếu còn giá trị demo/dev/change-me/default/localhost/sandbox.
- GitHub Actions backend CI chạy `mvn -B test` cho PR/push vào các nhánh làm việc chính.
- Production hardening Phase 1 đã implement:
  - Gateway và servlet services gắn/forward `X-Correlation-Id`, trả lại header response, và đưa vào MDC log ở servlet services.
  - Tất cả app có actuator expose thêm `/actuator/metrics` và `/actuator/prometheus`; metrics có tag `application`.
  - Các servlet service và gateway có OpenAPI runtime endpoint `/v3/api-docs`, bật/tắt bằng `SPRINGDOC_API_DOCS_ENABLED`.
  - GitHub Actions có workflow security cho Dependency Review và CodeQL.
  - Deploy workflow build bằng `mvn -B clean verify`, không còn package với `-DskipTests`.
- Production hardening Phase 2 đã implement:
  - Các service dùng OpenFeign có `spring-cloud-starter-circuitbreaker-resilience4j`.
  - Bật OpenFeign circuit breaker theo env `SPRING_CLOUD_OPENFEIGN_CIRCUITBREAKER_ENABLED`, có group mode, default connect timeout `APP_FEIGN_CONNECT_TIMEOUT_MS`, read timeout `APP_FEIGN_READ_TIMEOUT_MS`.
  - Thêm default Resilience4j circuit breaker config bằng env `APP_RESILIENCE4J_CB_*`.
  - Actuator expose thêm `/actuator/sbom`; Maven build generate CycloneDX SBOM dưới `target/classes/META-INF/sbom/application.cdx.json`.
  - GitHub Actions security workflow generate SBOM artifact và scan container image bằng Trivy cho `api-gateway`, `auth-service`, `order-service`, `locker-service`, `payment-service`, `iot-service`.
  - Thêm Testcontainers smoke test cho `locker-service` để verify Flyway seed demo cabinet và mapping maintenance report trên PostgreSQL thật khi Docker khả dụng.
- Production hardening Phase 3/4 đã implement:
  - Gateway có Swagger UI aggregation, gồm `/swagger-ui/index.html` và các route `/v3/api-docs/<service>` cho 10 service servlet đang có source.
  - Gateway public filter cho phép OpenAPI/Swagger UI public, nhưng vẫn chặn API nghiệp vụ mutating nếu không có JWT.
  - Thêm `JwtGatewayFilterTest` cho các contract quan trọng: chặn `/internal/**`, reject refresh token ở business API, RBAC admin/manage/maintenance, forward identity headers, public OpenAPI/catalog GET.
  - Spring Boot Maven `build-info` chạy cho các app module để `/actuator/info` có metadata build khi actuator đọc `META-INF/build-info.properties`.
  - Backend security workflow mở rộng Trivy scan matrix từ 6 image lên 12 image có Dockerfile; không thêm `laundry-service`/`partner-service` vì thiếu source.
  - Deploy workflow tạo/upload SHA-256 checksum cho deploy artifact; deploy script verify checksum nếu file `.sha256` tồn tại.
  - Deploy script bỏ mặc định `LAUNDRY-SERVICE`/`PARTNER-SERVICE` khỏi expected Eureka apps, có thể override bằng env `EUREKA_EXPECTED_APPS`.
- Production hardening Phase 4 continuation đã implement:
  - Deploy workflow dùng GitHub artifact attestation cho deploy tarball và checksum.
  - Thêm `backend-release.yml`: khi push tag `v*`, workflow build/test bằng `mvn -B clean verify`, package release tarball, copy root CycloneDX SBOM, tạo checksum, attest provenance và publish GitHub Release.
  - Release workflow upload release artifacts với retention 30 ngày.
  - Thêm `scripts/verify-release-artifact.sh` để verify SHA-256 và, nếu có GitHub CLI, verify attestation bằng `gh attestation verify`.
- Locker cell model đã implement:
  - `cell_type`
  - `row_index`
  - `col_index`
  - `fault_reason`
  - `landing_pad`
  - `landing_marker_id`
- Demo cabinet đã seed:
  - `CAB-DEMO-01`
  - 3 `DRONE`
  - 6 `STANDARD`
  - 1 `XL`
- Locker Phase 2 đã implement:
  - SEND.
  - RENTAL.
  - QR token.
  - Manager endpoints.
  - Maintenance endpoints.
  - Scheduler reminder/cleanup.

### Partial / Cần Chú Ý

- `docker-compose.yml` vẫn khai báo `laundry-service` đang thiếu; `docker-compose.override.yml` skip nó khi chạy local. `partner-service` đã được gỡ hẳn khỏi compose (2026-06-13).
- Một số docs cũ và FE pages vẫn giả định có service catalog/partner APIs.
- Seed data role hiện còn `USER`, `STAFF`, `ADMIN` (đã gỡ `PARTNER`), trong khi role routing mới dùng thêm `CUSTOMER`, `MANAGER`, `MAINTENANCE`.
- Password của deployed seed account không biết từ seed hash; có thể cần reset cho demo.

### Chưa Bắt Đầu

- Real drone backend service.
- Real laundry catalog source service.
- AI/RAG service.

## Chi Tiết Tiến Độ Frontend

### Đã Xong

- React admin route config có:
  - `/admin/lockers`
  - `/admin/lockers/:lockerId`
  - `/admin/maintenance`
- Admin sidebar có Lockers và Maintenance.
- RTK Query locker ops API slice đã có.
- Lỗi TypeScript trong examples/loyalty/order timeline đã fix.
- `npm.cmd run build` pass trong verification gần nhất.

### Partial / Cần Chú Ý

- Partner portal routes tồn tại nhưng phụ thuộc service backend đang thiếu/partial.
- Một số admin screens có thể vẫn gọi API theo assumption cũ và cần browser smoke.
- Admin auth/2FA cần verify theo environment.

### Chưa Bắt Đầu

- Drone fleet/tracking dashboard.
- Tablet/cabinet web UI.
- AI support UI.

## Chi Tiết Tiến Độ Flutter Mobile

### Đã Xong

- Login screen thật gọi `/api/auth/login`.
- Lưu token và routing theo role.
- `role_routes.dart`:
  - Manager/Admin -> `/manager`
  - Maintenance -> `/maintenance-home`
  - Role còn lại -> `/home`
- Home quick actions đã trỏ lại:
  - Thuê tủ.
  - Gửi hàng.
  - Đơn tủ của tôi.
- Module `locker_ops` mới:
  - API service.
  - Shared widgets.
  - Send parcel page.
  - Rent locker page.
  - My locker orders page.
  - Manager home.
  - Maintenance home.
- Render QR qua `qr_flutter`.
- Targeted analyze/build đã pass.
- Customer UI smoke pass trên emulator cho route rendering.
- Module `stores` mới (2026-06-13): màn danh sách cửa hàng (tìm kiếm + gần tôi), màn chi tiết cửa hàng (thông tin + đánh giá + chỉ đường), entry "Khám phá cửa hàng" trên home. `flutter analyze` 0 error.
- **2026-06-22**: `VoucherDetailPage` (MỚI) — ticket card gradient (UNUSED có màu / USED+EXPIRED xám), notch divider, copyable code chip, bảng thông tin, mô tả; `MyVouchersPage` bọc card trong `GestureDetector` → `Navigator.of(context, rootNavigator: true).push(VoucherDetailPage)`.
- **2026-06-22**: `PromotionDetailPage` (MỚI) — banner ảnh `CachedNetworkImage` 180px + gradient overlay, chip "Flash Sale", badge giảm giá, ticket banner gradient (6 màu chọn theo `promo.id % 6`), copyable code chip, bảng thông tin (`isExpired` getter mới trên `PromotionModel`), 3 steps "Cách dùng"; wired từ `PromotionsPage` (regular `Navigator.push`) và `_FlashSaleCard` trên home (`rootNavigator: true` phủ bottom nav).
- **2026-06-22**: `AppEventBus` — singleton `StreamController<AppEvent>.broadcast()` tại `lib/core/services/app_event_bus.dart`; `NotificationProvider` parse `actionType` từ STOMP + FCM, emit `OrderChangedEvent/PaymentCompletedEvent/WalletUpdatedEvent/PaymentFailedEvent/ReportUpdatedEvent`; `OrderPage` + `CustomerOrderDetailPage` + `WalletProvider` subscribe để tự refresh mà không cần pull thủ công.
- **2026-06-22**: 10 mock ô tủ/locker qua `_enrichLayout` trong `store_lockers_page.dart` — grid 2×5: row 0 là 5 STANDARD (mix AVAILABLE/OCCUPIED/RESERVED), row 1 là 2 STANDARD + 3 DRONE; bổ sung khi API trả <10 ô hoặc trả lỗi.
- **2026-06-22**: Fix nav bar overlap — 4 điểm điều hướng trong ShellRoute đổi sang `Navigator.of(context, rootNavigator: true).push()` / thêm `MediaQuery.of(context).padding.bottom` cho bottom sheet trong booking.
- Xác minh `develop` đã là superset (đã merge `ThaiBinh_NewUI_v1` qua PR #1) nên không cần merge thủ công; UI Đội bảo trì (MAINTENANCE) đã có sẵn và wire route đầy đủ.

### Partial / Cần Chú Ý

- Manager và maintenance UI cần manual smoke trên emulator/device ổn định.
- Một số file generated `.g.dart` và `.env` đang modified; cần cẩn thận khi commit.
- Các màn hình cũ vẫn tồn tại:
  - Old locker action.
  - QR login scanner.
  - Courier/logistics.
  - Wallet/transactions.
  - Calls `/advertisements`, `/blogs`, `/wallet/balance` trên home.

### Chưa Bắt Đầu

- Tablet-web/device-facing UI tương đương mobile.
- Realtime drone tracking mobile UI.
- Full payment UX cho SEND/RENTAL.

## Chi Tiết Tiến Độ IoT

### Đã Xong

- Backend IoT service hỗ trợ verify-access bằng PIN và QR.
- Endpoint device status và box-status tồn tại.
- MQTT facade code có trong backend.
- Python runtime tồn tại ở `smart-locker-iot`.

### Partial / Cần Chú Ý

- Broker config phải khớp giữa backend và Python runtime.
- Máy PC không có hardware chỉ nên chạy simulation mode.
- Sensor cửa/weight thật chưa tự động lái vòng đời ô (chưa auto occupy/release). **2026-06-30 (GAP 2)**: trạng thái cửa/cảm biến cabinet báo lên giờ ĐÃ được persist riêng (`iot_schema.box_hardware_status`, đọc qua `GET /api/manage/iot/box-status`) — tách khỏi `LockerBox.status` order-driven, chỉ để đối chiếu, KHÔNG tự ghi đè trạng thái logic.

### Chưa Bắt Đầu

- Tự động occupy/release từ sensor thật.
- Flow kết quả biometric verification thật.
- Drone deposit hardware integration.

## Ghi Chú Deploy / Database

### Chính Sách Server Dùng Chung (chốt 2026-06-16)

Từ 2026-06-16, **cả nhóm dùng chung backend + database đã deploy trên droplet**, không chạy local nữa (trừ khi đang sửa code backend thì mới cần bật docker local để test):

- **Entrypoint cho client**: `http://146.190.84.136:8080` (API Gateway). Đây là port duy nhất (cùng SSH 22) được DigitalOcean cloud firewall `smartlocker-firewall` mở inbound.
- **Web FE** (`laundry-locker-frontend/fe`): `VITE_API_BASE_URL`, default trong `api-paths.ts`, và proxy `vite.config.ts` đều trỏ `http://146.190.84.136:8080`. `fe/.env` **được track trong git** nên đổi 1 lần áp dụng cho cả nhóm.
- **Mobile** (`smart-laundry-locker-mobile`): `env_config.g.dart` (generated, track) + `defaultValue` trong `env_config.dart` đã = `http://146.190.84.136:8080`; `.env.example` cập nhật theo. Mobile `.env` là file private, không track — member tự copy từ `.env.example`.
- **Database**: client KHÔNG nối thẳng DB; chúng đi qua gateway server, nên trỏ FE/mobile về server là tự động dùng DB trên server. Nếu cần nối DB trực tiếp (DBeaver/psql hoặc chạy 1 service local trỏ DB server) thì dùng `146.190.84.136:15432` — **nhưng port 15432 hiện KHÔNG mở trên cloud firewall**, cần thêm rule inbound (nên giới hạn theo IP nhóm) trước khi nối được từ ngoài.
- **Auto-deploy khi merge nhánh chính**: `.github/workflows/deploy-droplet.yml` đã chạy on `push: develop` (+ `workflow_dispatch`): `mvn -B clean verify` → đóng gói tarball → SCP/SSH vào droplet (secrets `DROPLET_HOST/DROPLET_USER/DROPLET_SSH_KEY/DROPLET_PORT`) → `scripts/deploy-from-artifact.sh` chạy `docker compose up -d --build --remove-orphans`. **DB tự cập nhật theo**: mỗi service chạy Flyway migration lúc khởi động, nên migration mới được áp tự động khi deploy; dữ liệu giữ qua docker volume Postgres. ⇒ Yêu cầu "merge vào nhánh chính → tự deploy code + DB mới nhất" **đã được đáp ứng sẵn**.

### Lưu Ý Port Gateway (quan trọng) — đã fix bền vững 2026-06-16

- `docker-compose.yml` map gateway `"${API_GATEWAY_PORT:-18080}:8080"` → **mặc định host 18080** (cho local dev, vì host 8080 thường bị chiếm). Nhưng cloud firewall DO chỉ mở `22` + `8080` inbound, nên client ngoài **phải** vào `:8080`.
- **Fix bền vững**: `scripts/deploy-from-artifact.sh` giờ `export API_GATEWAY_PORT="${API_GATEWAY_PORT:-8080}"` ⇒ **mọi lần deploy lên droplet, gateway luôn được publish ở host 8080** mà không cần sửa env tay (deploy ghi đè thư mục nên `.env` set tay sẽ mất). Local vẫn giữ mặc định 18080 vì dev chạy `docker compose` trực tiếp, không qua deploy script. Health-check trong script dò `http://127.0.0.1:8080/actuator/health` nên khớp.
- **Chẩn đoán sự cố 2026-06-16 (root cause thật)**: lúc user báo "mobile không kết nối được", `docker compose ps` trên droplet cho thấy **tất cả service Up** nhưng `ll-ms-api-gateway` map `0.0.0.0:18080->8080/tcp` — tức gateway chạy ở host **18080** (firewall chặn) trong khi `:8080` không có listener ⇒ client ngoài "connection refused". Không phải OOM (RAM thật 7.8Gi/8GB, đã thêm swap 4GB). Fix tức thời: thêm `API_GATEWAY_PORT=8080` vào `.env` droplet + `docker compose up -d api-gateway`. Fix bền vững: như trên (trong deploy script).

### Tham Chiếu DigitalOcean Droplet đã quan sát

- Public IP: `146.190.84.136`
- PostgreSQL expose port `15432`
- Password superuser `postgres` đã được reset thủ công thành `postgres` khi troubleshoot.
- Deploy DB seed accounts đã quan sát:
  - `customer.seed@laundry.test`
  - `staff.seed@laundry.test`
  - `admin.seed@laundry.test`
  - `customer.vip@laundry.test`
  - (`partner.seed@laundry.test` đã gỡ khỏi seed file 2026-06-13; deploy DB cũ có thể còn record cho tới khi re-seed.)

Quan trọng:

- `POSTGRES_PASSWORD` trong `docker inspect` không chắc là password DB hiện tại nếu volume PostgreSQL đã init trước đó.
- Nếu demo account cần password biết trước, reset `auth_schema.auth_accounts.password_hash` có chủ đích và document password demo đã chọn tại đây.
- Không commit private server notes.

## Nhật Ký Verification

| Ngày | Khu vực | Lệnh / Kiểm tra | Kết quả | Ghi chú |
|---|---|---|---|---|
| 2026-07-14 | Mobile (drone order foundation) | `flutter test test/features/locker_ops/locker_ops_service_test.dart test/features/drone_delivery/drone_booking_sheet_test.dart` + `flutter analyze lib/features/locker_ops/data/locker_ops_service.dart lib/features/drone_delivery/presentation/widgets/drone_booking_sheet.dart test/features/locker_ops/locker_ops_service_test.dart test/features/drone_delivery/drone_booking_sheet_test.dart` + `flutter build apk --debug` | PASS | 23 tests PASS, analyze `No issues found!`, build ra `build/app/outputs/flutter-apk/app-debug.apk`. Có chạy `flutter pub get` trước mỗi lệnh nên output liệt kê nhiều package outdated nhưng không có lỗi. |
| 2026-07-02 | BE (G3/G4 + manager status) | `mvn -q compile -pl order-service,locker-service -am -DskipTests` sau khi thêm `releaseOverdueOrders`/`reconcileBoxStates`/`PATCH /api/manage/orders/{id}/status` + `GET /internal/boxes` | PASS | BUILD SUCCESS (exit 0). Verify tĩnh; E2E chạy stack cục bộ trong cùng phiên (xem nhật ký thay đổi). |
| 2026-07-02 | Mobile (forgot password + manager orders) | `flutter analyze` toàn project sau khi thêm luồng Quên mật khẩu + bottom sheet đổi trạng thái đơn Manager | PASS | 0 error/0 warning (409 info lint cũ pre-existing, các file mới không dính lint). |
| 2026-06-30 | Mobile (box-anomalies UI) | `flutter analyze lib/features/locker_ops/presentation/pages/maintenance_home_page.dart lib/features/locker_ops/data/locker_ops_service.dart` | PASS | **No issues found!** (ran ~169s). Thêm `boxAnomalies()` + section "Cảnh báo phần cứng" đầu tab Sự cố (state `_anomalies` load best-effort, `_boxAnomaliesSection` + nút Chỉ đường). Verify tĩnh; chưa smoke emulator. |
| 2026-06-30 | BE (maintenance box-anomalies) | `mvn -pl locker-service -am compile` sau khi thêm `BoxAnomalyResponse`, `LockerService.boxAnomalies` (fetch all hardware + lọc OPEN&&≠OCCUPIED + enrich locker), `GET /api/maintenance/box-anomalies` (fix: thêm import `java.util.ArrayList`) | PASS | `BUILD SUCCESS` (exit 0; lần đầu fail vì thiếu import ArrayList, đã thêm). Verify tĩnh; chưa E2E (cần JWT MAINTENANCE + dữ liệu `box_hardware_status` có ô OPEN). |
| 2026-06-30 | Mobile (maintenance box-health UI) | `flutter analyze lib/features/locker_ops/presentation/pages/maintenance_home_page.dart lib/features/locker_ops/data/locker_ops_service.dart` | PASS | **No issues found!** (ran 65s). Thêm `boxHealth(lockerId)` + card "Tình trạng phần cứng ô" (state `_boxHealth` load best-effort khi chọn tủ, `_boxHealthCard`/`_boxHealthRow`). Verify tĩnh; chưa smoke emulator phiên này. |
| 2026-06-30 | BE (maintenance box-health) | `mvn -pl iot-service,locker-service -am compile` sau khi thêm `GET /internal/iot/box-status`, `IotClient.boxStatus`, `BoxHealthResponse`, `LockerService.boxHealth` (ghép logic + hardware, cờ `doorOpen`/`needsAttention`), `GET /api/maintenance/lockers/{lockerId}/box-health` | PASS | `BUILD SUCCESS` (exit 0). Verify tĩnh; chưa E2E runtime (cần deploy + JWT MAINTENANCE + dữ liệu `box_hardware_status`). Hardware lookup best-effort (try/catch → Map rỗng). |
| 2026-06-30 | BE+IoT (hardware status, GAP 2) | `mvn -pl iot-service -am compile` (V3 migration + `BoxHardwareStatus` entity/repo/DTO + `updateBoxStatus` upsert + `GET /api/manage/iot/box-status` + `BoxStatusUpdateRequest.lockerId` + MQTT handler) **và** `python -m py_compile` + smoke door-state | PASS | BE `BUILD SUCCESS` (exit 0). Smoke simulator: `_report_box_hw_state` publish đúng `cabinet/{lockerId}/locker/{boxId}/status` (slotIndex+hwState); mở thành công → result + OPEN ngay + CLOSED qua timer; mở fail → KHÔNG báo trạng thái cửa; client mất kết nối → không publish. Verify tĩnh; chưa E2E runtime (cần deploy V3 + broker + gọi `GET /api/manage/iot/box-status`). |
| 2026-06-30 | IoT (simulator heartbeat) | `python -m py_compile simulate_demo_cabinet.py` + smoke logic với stub MQTT client (không cần broker) | PASS | `PY_COMPILE_OK` + `SMOKE_OK`. Smoke verify: học cabinet từ traffic → heartbeat ngay tới `cabinet/{id}/heartbeat` `{status:ONLINE}`; không heartbeat lại cabinet đã biết; lệnh sync học cabinet + không gửi reply; client mất kết nối → không publish. Chưa round-trip thật với broker + `GET /api/manage/iot/device-status` phiên này (cần deploy + chạy simulator). |
| 2026-06-29 | Backend (booking→IoT sync) | `mvn -pl iot-service,locker-service -am compile` sau khi thêm `BoxStateSyncRequest`/`LockerMqttService.publishBoxStateSync`/`IotService.syncBoxState`/`IotController POST /internal/iot/box-sync` + `IotClient.syncBoxState` + 5 lời gọi `syncBoxStateQuietly` | PASS | `BUILD SUCCESS` (exit 0). Verify tĩnh; chưa E2E runtime (cần deploy + simulator chạy để thấy log `[SIM] SYNC`). MQTT publish fire-and-forget, best-effort nuốt lỗi nên không cần broker khi compile. |
| 2026-06-29 | IoT (simulator) | `python -m py_compile simulate_demo_cabinet.py` sau khi subscribe `cabinet/+/command/sync` + nhánh `_on_message` cho sync | PASS | `PY_COMPILE_OK`. Chưa chạy round-trip thật với broker phiên này (chỉ syntax-check). |
| 2026-06-28 | Backend (delivery push) | `mvn -pl notification-service -am compile -DskipTests` | PASS | **BUILD SUCCESS** (notification-service + common-lib). Gồm `DomainEventNames.DELIVERY_STATUS_CHANGED`, `NotificationService.create(req, extraData)` overload, `DeliveryNotificationService` (enum 6 status + notify + notifyFromEvent), `DeliveryStatusNotificationRequest`, endpoint `POST /internal/notifications/delivery-status`, binding RabbitMQ + listener routing. Chưa chạy runtime/Firebase thật phiên này. |
| 2026-06-28 | Mobile (delivery push) | `flutter analyze` `firebase_messaging_service.dart` + `notification_permission_helper.dart` + `delivery_notification.dart` + `notification_list_page.dart` | PASS | "No issues found!". Gồm onTokenRefresh re-register, deep-link `order_detail` theo `orderId` (onMessageOpenedApp/getInitialMessage/tap local-notif qua payload JSON), model `DeliveryStatus` (icon/màu/title/body 6 mốc), helper quyền + hướng dẫn Settings, lịch sử noti tap `ORDER_STATUS_CHANGED`→order detail + icon truck. Chưa smoke trên emulator/Firebase thật phiên này. |
| 2026-06-28 | Mobile (Flight Data / MAVLink) | `flutter test test/features/drone_telemetry/mavlink_protocol_test.dart` | PASS | **9/9 test pass**. Quan trọng: test **CRC-16/MCRF4XX khớp check-value chuẩn `0x6F91`** cho `"123456789"` ⇒ codec CRC đúng spec MAVLink (tương thích ArduPilot/PX4 thật, không chỉ tự nhất quán). Phủ: decode GLOBAL_POSITION_INT/HEARTBEAT(armed+mode)/SYS_STATUS(pin), reassemble frame bị cắt giữa 2 lần đọc, drop frame sai CRC, round-trip COMMAND_LONG/HEARTBEAT/SET_MODE/REQUEST_DATA_STREAM qua parser. |
| 2026-06-28 | Mobile (Flight Data / MAVLink) | `flutter analyze lib/features/drone_telemetry lib/core/routing/app_router.dart .../maintenance_home_page.dart test/features/drone_telemetry` | PASS | "No issues found!" — 0 lỗi trên feature mới `drone_telemetry` + router + maintenance home + test. Đã bỏ import thừa `flight_modes.dart` trong page. Chưa chạy emulator/SITL phiên này — chỉ verify tĩnh + unit test (kết nối UDP/TCP cần peer thật/SITL). |
| 2026-06-28 | Mobile (Mission Planner) | `flutter analyze lib/features/drone_mission lib/core/routing/app_router.dart lib/features/locker_ops/.../maintenance_home_page.dart test/features/drone_mission` | PASS | "No issues found!" — 0 lỗi trên feature mới + 2 file đã sửa (router, maintenance home) + test. (Đã sửa 2 cảnh báo ban đầu: unused import `mav_command.dart`; `onReorder` deprecated → `onReorderItem`.) Chưa chạy emulator phiên này — chỉ verify tĩnh + unit test. |
| 2026-06-28 | Mobile (Mission Planner) | `flutter test test/features/drone_mission/mission_codec_test.dart` | PASS | **7/7 test pass**. Phủ: round-trip QGC WPL 110 (home/lệnh/toạ độ/độ cao/loiter param), reject thiếu header, fallback WAYPOINT cho command code lạ, round-trip JSON nội bộ lossless, throw JSON hỏng, tính `totalDistanceMeters`/`estimatedSeconds`/`waypointCount` (chỉ đếm leg có toạ độ). |
| 2026-06-22 | Backend (drone fleet) | `mvn -pl locker-service -am compile` sau khi thêm `DroneUnit`/`DroneMaintenanceLog`/`DroneStatus`, repository, DTO, service method, controller endpoint, migration V10 | PASS | `BUILD SUCCESS`. Chưa chạy Flyway thật (không có Docker/Postgres local đang bật phiên này) — migration V10 chưa được verify runtime, chỉ verify cú pháp SQL bằng đọc lại. |
| 2026-06-22 | Mobile (drone fleet) | `flutter analyze` trên `locker_ops_service.dart` + `maintenance_home_page.dart` sau khi thêm tab "Drone" + `_DroneStatusChip`/`_DroneLogSheet` | PASS | "No issues found". Chưa chạy emulator phiên này (không có thiết bị/emulator sẵn) — chỉ verify tĩnh. |
| 2026-06-22 | Mobile (UI/UX nâng cấp) | `flutter analyze` (toàn project) sau khi thêm `VoucherDetailPage`, `PromotionDetailPage`, `AppEventBus`, `_enrichLayout` 10 mock cells, fix nav bar overlap, `NotificationProvider` emit domain events; thêm `isExpired` getter vào `PromotionModel` | PASS | 0 error. Verify tĩnh; chưa chạy emulator phiên này. |
| 2026-06-21 | Backend (payment) | `mvn compile` toàn reactor sau khi thêm ví + checkout + MoMo + order-paid listener | PASS | `BUILD SUCCESS`. Gồm `WalletService`/`MomoService`/`PaymentService.checkout`/`OrderPaymentEventListener` + migration V3(payment)/V5(order). |
| 2026-06-21 | Mobile (ví) | `flutter analyze` feature_flags + features/wallet | PASS | "No issues found". Bật `walletEnabled/transactionsEnabled`; `WalletProvider` đọc đúng `GET /api/wallet` (trước trỏ sai `/wallet/balance`). |
| 2026-06-21 | Web (admin ví) | `npm run build` (`tsc -b && vite build`) | PASS | Built ~14s, 0 lỗi TS. RTK `wallet.ts` + `WalletModal` + nút "Ví" trong `UserTable` + tag `Wallet`. |
| 2026-06-21 | Mobile (Facebook) | `flutter analyze` 3 file + `flutter build apk --debug` sau khi thêm `flutter_facebook_auth` + native config | PASS | `√ Built app-debug.apk`. App ID `1767424237196097` + Client Token nhúng qua `strings.xml`/AndroidManifest; `signInWithFacebook()` → Firebase credential. Phải thêm `kotlin.jvm.target.validation.mode=warning` vào `gradle.properties` vì plugin facebook compile Kotlin jvmTarget=21 còn Java 1.8 (mismatch chặn build). Runtime cần bật Facebook provider trong Firebase + OAuth redirect URI. |
| 2026-06-21 | Mobile (Firebase config thật) | `flutter analyze` 3 file + `flutter build apk --debug` sau khi user bật Google provider + thay `google-services.json` mới (project `laundry-locker-19a9d`) | PASS | `√ Built app-debug.apk` (180s, chỉ warning KGP cũ). Đã wire **Web client ID** `1007589685877-...apps.googleusercontent.com` vào `EnvConfig.googleWebClientId` + `GoogleSignIn.initialize(serverClientId:)`; **fix `firebase_options.dart`** android entry từ `db-wsb-project` → `laundry-locker-19a9d` (trước đó app init nhầm project ≠ google-services.json). iOS entry vẫn `db-wsb-project` (chưa có app iOS trong project, chỉ ảnh hưởng khi build iOS). **Chưa test runtime**: cần user thêm SHA-1/256 vào Firebase + đặt service account JSON cho backend. |
| 2026-06-21 | Backend | `mvn -pl auth-service,user-service -am compile -DskipTests` | PASS | `BUILD SUCCESS`. Gồm `firebase-admin 9.4.3` (pull mới), `social_identities`/`SocialIdentityRepository`, `firebaseLogin`, `createAccount`, `/internal/auth/accounts`, `adminCreate` tạo auth_account. Sau khi fix duplicate import `Slf4j` (`FirebaseConfig`) + duplicate key `logging:` (`application.yml`) — 2 lỗi sẽ chặn compile/boot nếu không sửa. |
| 2026-06-21 | Mobile | `flutter analyze` (toàn project) | PASS | **0 error** (410 info lint cũ). `firebase_auth_service.dart` "No issues found" sau khi migrate google_sign_in v7 (`GoogleSignIn.instance.authenticate()` thay `signIn()`) + route Google qua `signInWithCredential` (trả Firebase idToken đúng để backend `verifyIdToken`). UI Google/phone-OTP trong `auth_bottom_sheet` analyze sạch. Chưa chạy emulator/build apk phiên này. |
| 2026-06-21 | Web FE | `npm run build` (`tsc -b && vite build`) | PASS | Built ~15s, 0 lỗi TypeScript. Gồm `CreateUserModal`/`UserTable` đổi role list sang CUSTOMER/ADMIN/MANAGER/MAINTENANCE + `RoleNameSchema` mở rộng (nếu không sẽ bị Zod chặn khi tạo MANAGER/MAINTENANCE). Cảnh báo chunk-size cũ, không chặn. |
| 2026-06-19 | Mobile | `flutter analyze` + `flutter build apk --debug` sau khi ẩn nhận diện khuôn mặt | PASS | **0 error, 0 warning** (406 info cũ); APK **√ Built**. Thêm cờ `faceRecognitionEnabled=false`: `ProfileProvider._getFaceRegistrationStatus` short-circuit (hết gọi `GET /api/auth/ai/registered/{id}` → trước trả **500**) + ẩn menu "Đăng ký khuôn mặt" ở Profile. |
| 2026-06-19 | Mobile (web) | `flutter analyze` + `flutter build apk --debug` sau khi fix lỗi đỏ khi chạy web | PASS | `flutter analyze` **0 error, 0 warning** (406 info cũ); `flutter build apk --debug` → **√ Built app-debug.apk**. Đã reproduce bằng `flutter run -d chrome` (không có `web/` dir track → Flutter tự scaffold ephemeral): xác nhận RenderFlex overflow **không xuất hiện ở bề rộng mặc định** ⇒ overflow chỉ xảy ra ở viewport hẹp (DevTools chia đôi màn hình của user) tại Row "Đăng nhập nhanh (DEV)" — đã bọc text vào `Flexible`+ellipsis. Firebase web: guard `!kIsWeb` (hết lỗi "DefaultFirebaseOptions have not been configured for web"). Dio logger: tắt `requestHeader/requestBody/responseBody` (hết đổ accessToken/refreshToken/body ra console). Launcher icon đã là logo navy đúng (verify pixel góc = navy); máy user hiện Flutter-default vì là APK build cũ → cần build/cài lại. |
| 2026-06-19 | Mobile | `flutter analyze` + `flutter build apk --debug --target-platform android-arm64` (sau khi thêm feature flags + ẩn UI + đổi logo) | PASS | `flutter analyze`: **0 error, 0 warning**, 406 info lint (toàn bộ là lint cũ pre-existing, không tăng sau thay đổi). `flutter build apk --debug` → `√ Built app-debug.apk` (~128s). Cảnh báo còn lại chỉ là KGP/Gradle deprecation của plugin (camera/file_picker/...) và 15 WARNING Groovy DSL trong `problems-report.html` — **không phải lỗi đỏ, không chặn build**. Đã chặn các call 404 cũ (`/wallet/balance`, `/advertisements`, `/blogs`) ngay tại nguồn (short-circuit) thay vì chỉ catch. Chưa chạy trên emulator phiên này (chỉ verify tĩnh + build). |
| 2026-06-16 | Mobile | `flutter analyze --no-pub` trên `maintenance_home_page.dart`, `ops_widgets.dart`, `my_locker_orders_page.dart` | PASS | "No issues found!" — 0 lỗi trên cả 3 file sau khi làm lại UI bảo trì + promote `OpsSheetAction`. `flutter devices` không có emulator/thiết bị nào đang chạy (chỉ Windows desktop + Chrome/Edge web) và cần đăng nhập tương tác để vào đúng role MAINTENANCE nên **chưa xem được trực quan** — chỉ verify tĩnh phiên này. |
| 2026-06-16 | Backend | `mvn -B test` (toàn bộ reactor 14+ module) | PASS | Exit code 0. Docker không khả dụng phiên này nên 2 test Testcontainers Postgres của `locker-service` skip (đúng thiết kế `disabledWithoutDocker=true`, không phải lỗi). Bao gồm thay đổi `api-gateway/application.yml` (+1 path predicate), `iot-service` (audit log + lockout + force-unlock + device-status list), `locker-service` (force-open + TTL sweep + size fallback + reporter contact + rating). |
| 2026-06-16 | Mobile | `flutter analyze --no-pub` trên `locker_ops_service.dart`, `maintenance_home_page.dart`, `my_reports_page.dart`, `send_parcel_page.dart` | PASS | "No issues found!" trên cả 4 file đã sửa (force-open, rating, size selector, contact info). |
| 2026-06-16 | Web FE | `npm.cmd install` (lần đầu, `node_modules` chưa có trong môi trường này) rồi `npm.cmd run build` (`tsc -b && vite build`) | PASS | 0 lỗi TypeScript; build xong ~12s. Cảnh báo chunk-size/browserslist cũ không liên quan, không chặn build. Gồm `lockerOps.ts` (6 endpoint mới), `layout-view.tsx` (force-open), `BoxSettingModal.tsx` (sửa route chết), `Admin/maintenance/index.tsx` (schedules + device health + contact info). |
| 2026-06-16 | Backend / Auth | Cô lập login 500 bằng curl + `mvn -pl common-lib,user-service -am compile` | PASS | Login sai-pass `400 AUTH_INVALID` / đúng-pass `500 INTERNAL_ERROR` / register `500` ⇒ lỗi ở Feign sang user-service. Droplet log: user-service Flyway "checksum mismatch version 1" (V1 bị sửa ở `a6b5eb3`). Thêm `FlywayRepairConfig` (common-lib) + flyway-core optional; compile **PASS** (exit 0). **VERIFIED runtime:** user chạy `UPDATE user_schema.flyway_schema_history SET checksum=1261895981 WHERE version='1'` (bảng ở `user_schema`) + recreate user-service → login/register/profile/admin-login probe ngoài **đều 200**. |
| 2026-06-16 | Mobile E2E (emulator) | `flutter build apk --debug` + `adb install` + chạy trên emulator-5554 (Android 14) + đọc logcat + screenshot | PASS | Build PASS 104s. Trước: APK cũ gọi `https://api-dev.aisl.io.vn` (chết) → "Lỗi server". Sau rebuild + network-security-config: app gọi đúng `http://146.190.84.136:8080/api/lockers` (cleartext OK), màn "Danh sách tủ" load tủ thật từ server. `/api/lockers`,`/api/stores` 200; `/api/user/profile` 503 nhất thời (user-service vừa restart) → probe ngoài 401 = service up. Legacy `/wallet/balance`,`/advertisements`,`/blogs` (thiếu prefix `/api`, feature Revoland cũ) vẫn fail nhưng được catch, không chặn UI. |
| 2026-06-20 | Backend PA4 | `mvn clean package -DskipTests` toàn bộ 13 module sau khi thêm STAFF/TECHNICIAN roles | PASS | BUILD SUCCESS 13/13 modules (common-lib → loyalty-service). Đồng thời fix pre-existing issue: Lombok `@Slf4j` không hoạt động với Java 25 do thiếu `annotationProcessorPaths` trong root `pom.xml` → đã thêm. Thêm: `StaffController` (locker-service), `StaffOrderController` (order-service), `TechnicianController` + `BoxAccessLogResponse` (iot-service), RBAC `/api/staff/**` + `/api/technician/**` trong gateway, seed accounts 9005+9006, 2 repository query methods. |
| 2026-06-16 | Deploy script | `bash -n scripts/deploy-from-artifact.sh` (Git Bash) sau khi thêm `export API_GATEWAY_PORT=8080` | PASS | Syntax OK. Chẩn đoán trên droplet (`docker compose ps`): api-gateway map `0.0.0.0:18080->8080` → root cause client ngoài không vào được `:8080`. `free -h`: Mem 7.8Gi (8GB, resize đã apply), Swap 4.0Gi (đã thêm). Fix tức thời `.env`+recreate gateway → 8080; fix bền vững trong deploy script. |
| 2026-06-16 | Web FE | `npm run build` (`tsc -b && vite build`) trong `laundry-locker-frontend/fe` sau khi trỏ về server | PASS | `✓ built in ~25s`, 0 lỗi type. Chỉ còn warning chunk-size cũ (>500kB) không liên quan. Đã đổi `fe/.env` `VITE_API_BASE_URL`, default `api-paths.ts`, và proxy `vite.config.ts` sang `http://146.190.84.136:8080`. |
| 2026-06-16 | Server / reachability | `curl http://146.190.84.136:8080/actuator/health` + probe `:22`, đối chứng `curl https://api.github.com` | PARTIAL/FAIL | Outbound OK (GitHub 200). SSH `:22` trả banner `SSH-2.0-OpenSSH_9.6p1` (droplet sống). Gateway `:8080` → "connection refused" (không có listener) ⇒ stack backend **đang không chạy** trên droplet; cần bật lại (`docker compose up -d` / chạy lại deploy). Client đã cấu hình đúng `:8080` (port firewall mở). |
| 2026-06-16 | Mobile config | Đọc `env_config.dart`/`env_config.g.dart`/`api_constants.dart`; cập nhật `.env.example` | PASS | `env_config.g.dart` (track) = `http://146.190.84.136:8080`, `defaultValue` cũng = server ⇒ mobile đã trỏ server cho cả nhóm dù không có `.env`. `.env.example` đã đổi sang server. Mobile `.env` (private, không track) local đã = server từ trước. Không sửa code Dart nên không cần `flutter analyze`. |
| 2026-06-16 | Backend | `mvn -pl common-lib,order-service,locker-service,notification-service -am test` | PASS | Exit code 0. `locker-service` Testcontainers PostgreSQL smoke chạy thật (Docker khả dụng phiên này) thay vì skip như các lần trước; boot full Spring context PASS sau khi thêm `publishReportNotification`. |
| 2026-06-16 | Mobile | `flutter analyze --no-pub` trên `locker_ops_service.dart`, `my_locker_orders_page.dart`, `my_reports_page.dart` (mới), `app_router.dart` | PASS | "No issues found!" — 0 error/warning trên 4 file đã sửa/thêm. |
| 2026-06-16 | IoT simulator | Round-trip MQTT thật: chạy `simulate_demo_cabinet.py` (background), publish payload đúng format Java thật gửi (`{commandId, box_id, action, timeout}`) lên `cabinet/9999-test/command/open` qua `broker.hivemq.com:1883`, nhận lại `cabinet/9999-test/command/open/result` | PASS | `status:"SUCCESS"`, `commandId` khớp, `boxId` khớp, trong ~1.5s. Xác nhận đúng contract mà `IotService.unlock()` cần (`node.has("status") && !"FAILED".equals(...)`). Dùng `pip install paho-mqtt` tạm trên Python 3.11 system (môi trường không có `uv`/Python 3.13) chỉ để test kết nối — không đổi `pyproject.toml`/`uv.lock`. |
| 2026-06-16 | Chưa chạy phiên này | Docker-compose full stack (gateway + order-service + iot-service + locker-service + notification-service) + tạo đơn SEND/RENTAL thật + gọi `/api/iot/unlock` qua gateway + emulator click-through "Mở tủ"/"Đặt lại đơn"/"Báo cáo của tôi" + xác nhận push notification thật khi claim/resolve | CHƯA CHẠY | Docker Desktop trên máy này giới hạn ~6.8GB RAM (thấy qua Testcontainers log) — không đủ an toàn để bật đồng thời 12+ JVM service cho E2E click-through trong phiên này. Mức tin cậy hiện tại dựa trên: Maven test pass (gồm Postgres thật), MQTT round-trip thật, và đọc code xác nhận khớp contract end-to-end. Khuyến nghị chạy lại theo `RUN_RESULT.md`/`HANDOFF_CODEX.md` khi cần demo thật. |
| 2026-06-15 | DB / Full demo seed | Chạy `scripts/seed-full-demo-ms.sql` trên Postgres local (×2) + login 4 account qua gateway `:18080` | PASS | Run #1 áp đủ: user_profiles 104, auth_accounts 104, stores 100, lockers 100, boxes 900, reports 100, schedules 100, repair_logs 100, promotions 100, orders 100, order_details 100, status_history 200, ratings 100, complaints 100, payments 100, refunds 100, notifications 100, fcm_tokens 100, loyalty_accounts 104, point_transactions 100, device_statuses 100. Run #2 **idempotent** (0 error, không trùng key). `POST /api/auth/login` với pw `12345678` cho cả 4 email → `AUTH_LOGIN_OK` role ADMIN/CUSTOMER/MAINTENANCE/MANAGER đúng. **Droplet `146.190.84.136:15432` CHƯA áp**: harness chặn agent ghi vào prod DB → user tự chạy script (lệnh trong báo cáo). |
| 2026-06-15 | Mobile / Auth + Startup | `flutter analyze --no-pub lib/core/network/auth_interceptor.dart lib/main.dart` sau khi sửa | PASS (0 error) | `auth_interceptor.dart` 0 issue; `main.dart` chỉ còn 8 info-level cũ (withOpacity/BuildContext across async gaps) không liên quan thay đổi. Chẩn đoán từ log `flutter run` maintenance: token `exp` Jun 15 15:01 < now 15:51 → 401 (token hết hạn thật, không phải lỗi backend); `AuthInterceptor` cũ logout thẳng. Đã thêm refresh-on-401 (gọi `POST /api/auth/refresh-token`, serialize 1 lần/đợt vì backend xoay vòng refresh token, retry request gốc). Firebase duplicate-app: guard `Firebase.apps.isEmpty`. Build chậm: Gradle heap 8G→3G + parallel/caching (máy 16GB chạy kèm Docker 12 JVM + emulator). |
| 2026-06-15 | Mobile UI | `flutter analyze lib/.../profile_page.dart lib/.../my_locker_orders_page.dart lib/.../location_services.dart` | PASS | 0 error mới. 3 issue info pre-existing (lucide unnecessary_import, deprecated `activeColor`/`withOpacity` từ SDK Flutter) không liên quan thay đổi. UI revamp: ProfilePage BrandHeroHeader layout, MyLockerOrdersPage header, LockerUtilitiesRow navigation fix. |
| 2026-06-15 | Web FE + Backend / Admin Dashboard | FE `npm.cmd run build` + BE `mvn -pl order-service -am package` + rebuild container + verify JSON endpoint | PASS | Crash `/admin/dashboard` (`reading 'toString'` tại `dash-board.tsx:60`) do dashboard đọc 11 field (`ordersToday/totalRevenue/...`) nhưng `OrderService.statistics()` chỉ trả `totalOrders`+`byStatus`. Sửa: FE `useDashboard` normalize overview đủ field default 0 (không crash dù backend thiếu) + BE thêm `ordersToday/pendingOrders/totalRevenue/revenueToday`. FE build `✓` 0 lỗi; BE jar build SUCCESS; rebuild `ll-ms-order-service` UP; gọi trực tiếp `GET :8083/api/admin/dashboard/overview` trả `{totalOrders:11, ordersToday:0, pendingOrders:2, totalRevenue:230000.00, revenueToday:0, byStatus:{...}}`. Cross-service KPI (users/stores/lockers/boxes/services) vẫn 0 (chưa wire aggregation chéo service — follow-up). |
| 2026-06-15 | Web FE / Admin 2FA | `npm.cmd run build` (`tsc -b && vite build`) trong `laundry-locker-frontend/fe` sau khi sửa `auth-context.tsx` | PASS | `✓ built in ~17s`, 0 lỗi type. Nguyên nhân crash sau OTP: FE đọc `data.user.id` nhưng `/api/admin/auth/verify-2fa` (qua `authMap`) trả payload **phẳng** (`accountId/userId/accessToken/refreshToken/roles/name`) — `data.user` `undefined` → `Cannot read properties of undefined (reading 'id')`. Đã chuẩn hoá đọc field phẳng (`userId`/`roles`/`name`) + giữ email bước 1; trace luồng: `adminLoginStep2` → `persistLogin(role:["ADMIN"])` → `getRedirectPath` → `/admin/dashboard`. OTP dev lấy từ log `auth-service` (`docker logs ll-ms-auth-service \| grep "Development OTP"`). Chưa chạy E2E browser thật (cần password admin của user). |
| 2026-06-15 | Backend / Gateway CORS | Rebuild gateway với config đã sửa + curl preflight/POST qua `:18080` | PASS | **Trước:** `OPTIONS /api/admin/auth/login` (Origin `http://localhost:3000`) trả `403`, **không có** `Access-Control-Allow-Origin` → trình duyệt báo "Failed to fetch". **Sau** khi chuyển `globalcors` về `spring.cloud.gateway.server.webflux.globalcors` + rebuild jar/image: preflight `200` với `Access-Control-Allow-Origin: http://localhost:3000`, `Allow-Methods GET,POST,PUT,PATCH,DELETE,OPTIONS`, `Allow-Credentials true`; POST thật cũng có ACAO header (body `AUTH_INVALID` khi cố tình sai mật khẩu → endpoint hoạt động bình thường). Bằng chứng prefix: `GlobalCorsProperties` trong `spring-cloud-gateway-server-4.3.4` chỉ bind `spring.cloud.gateway.server.webflux.globalcors`; prefix cũ `deprecated since 4.3.0`. |
| 2026-06-15 | Mobile | `flutter analyze --no-pub` trên các file đã chỉnh: `store_lockers_page.dart`, `locker_page.dart`, `rent_locker_page.dart`, `send_parcel_page.dart`, `locker_ops_service.dart` | PASS | 0 error trên các file đã sửa. Pre-existing 440+ info-level findings không liên quan không đổi. |
| 2026-06-15 | Backend / Mobile | Fix Cold Start 500 error trên app, tạo seed user, fix lỗi syntax | PASS | Mobile `flutter run` pass sau khi sửa syntax lỗi ngoặc ở `home_page.dart`. Login success sau khi tăng TimeLimiter của `auth-service` lên 10s và chờ API Gateway nhận diện service mới. SQL seed script đã insert test user thành công vào Postgres trực tiếp. |
| 2026-06-14 | Mobile fix/verify | Batch 2 (branch `fix/mobile-api-endpoints-alignment`, commit `08567d3`): repoint bottom-nav **Đơn hàng** từ `OrderPage` legacy (`/orders/me` chết) sang `MyLockerOrdersPage` (locker_ops, `GET /api/orders/my-orders`); tủ thiếu toạ độ → `NaN` để chi tiết tủ hiện màn "toạ độ không hợp lệ" thay vì pin (0,0). | PASS | On-device: tab Đơn hàng `GET /api/orders/my-orders` 200 hiện đơn thật (ORD-2026…); chi tiết CAB-DEMO-01 render bản đồ FPT HCMC. `flutter analyze` 0 error. |
| 2026-06-14 | Mobile fix/verify | Sửa lệch endpoint mobile↔backend (branch `fix/mobile-api-endpoints-alignment`): Profile `getProfile` → `/api/user/profile` + normalize `id` int→String + bọc try/catch `_getFaceRegistrationStatus` (AI 404 từng làm `loadProfile` ném lỗi → kẹt spinner Hồ sơ); Register → `/api/auth/register` với body `firstName/lastName/roles` + sau đăng ký chuyển tab Login (bỏ OTP legacy); tab Tủ (cả 3 method `getLocations*`) → `/api/lockers` map an toàn (id/address/lat/long nullable). | PASS | On-device (emulator) verify qua logcat + screenshot: tab Tủ `GET /api/lockers` 200 hiện 2 tủ; tab Hồ sơ `GET /api/user/profile` 200 hiện "Huy Nguyen"; register `AUTH_REGISTERED` qua API. `flutter analyze` 4 file = 0 error. Phát hiện method thật của tab Tủ là `/locations/customer` (không phải `/locations`) nhờ verify thật. |
| 2026-06-14 | Runtime/Mobile | Run mobile + backend cho manual test luồng Customer/Maintenance: gateway local `:18080` health `UP`; login API customer `nqbhuy2004nt@gmail.com` và maintenance `maintenance@laundry.test` qua `:18080` PASS; sửa mobile `.env`/`env_config.g.dart` trỏ `10.0.2.2:18080`; `flutter pub get` + `flutter build apk --debug` PASS; cài + login app trên emulator Pixel_4 vào đúng Customer Home (badge notification 17). Layout CAB-DEMO-01 (locker id=2): 6 STANDARD + 1 XL + 3 DRONE đều AVAILABLE. | PASS | Emulator Pixel_4 thiếu RAM nên từng ANR "System UI isn't responding" lúc boot; ổn định lại với `-gpu host -memory 2560`. Chỉ APK debug + sửa config local, không đổi code commit. |
| 2026-06-14 | Backend | `mvn -pl locker-service,notification-service -am test` | PASS | common-lib 8 tests pass; locker-service Testcontainers PostgreSQL smoke 2 tests pass; notification-service compile/test phase pass. |
| 2026-06-14 | Flutter | `flutter analyze --no-pub lib/features/locker_ops/presentation/pages/maintenance_home_page.dart lib/features/locker_ops/presentation/pages/my_locker_orders_page.dart lib/features/locker_ops/data/locker_ops_service.dart lib/features/locker_ops/presentation/utils/locker_maps.dart lib/features/notifications/domain/entities/notification_model.dart lib/features/notifications/infrastructure/data_sources/notification_remote_data_source.dart lib/core/services/firebase_messaging_service.dart` | PASS | Targeted analyze 7 items: "No issues found". Lệnh rộng hơn `flutter analyze lib/features/locker_ops lib/features/notifications lib/core/services/firebase_messaging_service.dart` timeout 5 phút không trả output. |
| 2026-06-14 | Web FE | `npm.cmd run build` trong `laundry-locker-frontend/fe` | PASS | `tsc -b && vite build` pass; còn warning Vite/Browserslist/chunk-size cũ, không chặn build. |
| 2026-06-14 | Git/Docs | `git diff --check` ở backend/mobile/frontend và quét private-file trong `docs/project-artifacts` | PASS | Không có whitespace error; không thấy `env.txt`, `pro.txt`, `Application.txt`, `Host *.txt` trong artifacts. |
| 2026-06-14 | Backend | `mvn -pl notification-service -am test` | PASS | common-lib 8 tests pass; notification-service compile/test phase pass sau khi thêm STOMP JWT auth và JJWT deps. |
| 2026-06-14 | Flutter | `flutter analyze --no-pub lib/features/notifications lib/core/services/firebase_messaging_service.dart` | PASS | Notification realtime STOMP client + provider integration analyze sạch; `flutter pub add stomp_dart_client` resolve `stomp_dart_client 3.0.1`. |
| 2026-06-14 | Runtime smoke | `docker ps`, gateway health local, deploy health probe | PARTIAL/FAIL | Kết quả cũ trước khi harden: containers local có vẻ chạy nhưng Docker Desktop/logs báo không start được; `http://localhost:8080/actuator/health` đóng connection; `https://api-dev.aisl.io.vn/actuator/health` trả 404. Đã có dòng PASS mới bên dưới cho local gateway port thay thế `18080`. |
| 2026-06-14 | Backend | `mvn -pl notification-service -am test` | PASS | common-lib 8 tests pass; notification-service 8 tests pass, gồm STOMP JWT principal, Spring bean wiring cho interceptor/handshake handler, và RabbitMQ `DomainEvent` converter allow-list. |
| 2026-06-14 | Backend package | `mvn -pl notification-service -am package -DskipTests` | PASS | Repackage jar notification-service thành công để Docker image copy đúng code mới. |
| 2026-06-14 | Runtime smoke | `docker compose up -d --no-deps --build notification-service`; `scripts/smoke-notification-stomp.ps1`; gateway/API probes | PASS | Do host port `8080` bị `AgentService.exe` chiếm, gateway local chạy bằng override tạm port `18080`. PASS: gateway/notification health UP, maintenance/admin login, `GET /api/notifications`, unread count, FCM token save/delete, subscribe `/user/queue/notifications`, admin send -> STOMP `MESSAGE`, và Rabbit conversion log không còn `unauthorized class DomainEvent`. |
| 2026-06-14 | Tài liệu | `git diff --check` cho `ARCHITECTURE_DECISIONS.md` + 2 file sống + mirror | PASS | ADR governance; docs-only, không có file private. |
| 2026-06-14 | Backend | `mvn -pl order-service -am test` trên branch `fix/locker-reservation-ttl-and-release` | PASS | BUILD SUCCESS; 8 test common-lib pass; order-service (gồm sửa auto-cancel/scheduler L1) compile sạch. Không cần Docker. |
| 2026-06-14 | Flutter | `flutter analyze lib/features/locker_ops` và `flutter analyze` toàn project | PASS | "No issues found" trên `locker_ops`; toàn project 0 error (413 info/warning debt cũ không đổi) sau revamp UI luồng tủ. |
| 2026-06-14 | Tài liệu | `git diff --check` trên branch `docs/locker-flows-standard-spec` + quét private-file (`env.txt`/`pro.txt`/`Application.txt`/`Host *.txt`) trong `docs/project-artifacts` | PASS | Chỉ thêm/sửa Markdown (spec luồng tủ + 2 file sống + mirror); không có file private bị copy vào artifacts. |
| 2026-06-13 | Flutter | `flutter analyze lib/features/stores lib/core/routing/app_router.dart lib/features/home/...` | PASS | "No issues found"; 0 lỗi trên feature stores mới + file wiring. |
| 2026-06-13 | Flutter | `flutter analyze` toàn project | PASS/PARTIAL | 0 lỗi mức error; 413 issue còn lại là info/warning debt cũ của codebase migrate (gồm `test/widget_test.dart`). |
| 2026-06-13 | Backend/Seed | `git diff --check` sau khi bỏ role `PARTNER` khỏi seed/compose | PASS | Không lỗi whitespace; seed SQL giữ cấu trúc/comma hợp lệ (`USER/STAFF/ADMIN`, permission `1001-1007`); chỉ còn cột `partner_id` ở store là schema cố ý giữ. Không đụng Java/migration. |
| 2026-06-13 | Backend | `mvn -pl api-gateway -am test` | PASS | Phase 4 continuation không đổi Java logic; targeted gateway/common tests vẫn pass: 8 common-lib tests, 2 correlation gateway tests, 7 JWT/RBAC gateway tests. |
| 2026-06-13 | Release scripts | `C:\Program Files\Git\bin\bash.exe -n scripts/deploy-from-artifact.sh` và `... verify-release-artifact.sh` | PASS | Shell syntax check pass cho deploy script và script verify release artifact. |
| 2026-06-13 | Git/Docs | `git diff --check` và quét private-file trong `docs/project-artifacts` | PASS | Không phát hiện whitespace/error; không có `env.txt`, `pro.txt`, `Application.txt`, `Host *.txt` trong artifacts. |
| 2026-06-13 | Backend | `mvn -B clean verify` | PASS/PARTIAL | 14 module backend clean/test/package/verify pass sau Phase 3/4; build-info/repackage pass; Testcontainers locker smoke skip 2 test vì Docker local không khả dụng. |
| 2026-06-13 | Backend | `mvn -B test` | PASS/PARTIAL | 14 module backend build/test pass sau Phase 3/4; `JwtGatewayFilterTest` 7 tests pass; Testcontainers locker smoke skip 2 test do Docker local không khả dụng. |
| 2026-06-13 | Backend | `mvn -pl api-gateway -am test` | PASS | Targeted gateway/common tests pass: 8 common-lib tests, 2 correlation gateway tests, 7 JWT/RBAC gateway tests. |
| 2026-06-13 | Deploy script | `C:\Program Files\Git\bin\bash.exe -n scripts/deploy-from-artifact.sh` | PASS | Shell syntax check pass bằng Git Bash; WSL `bash -n` không chạy được vì môi trường này không có `/bin/bash`. |
| 2026-06-13 | Backend | `mvn -B clean verify` | PASS/PARTIAL | 14 module backend clean/test/package/verify pass sau Phase 2; Testcontainers locker smoke được phát hiện nhưng skip 2 test vì máy local không có Docker environment, khi CI/server có Docker sẽ chạy PostgreSQL container thật. |
| 2026-06-13 | Backend | `mvn -B test` | PASS/PARTIAL | 14 module backend build/test pass sau Phase 2; CycloneDX SBOM generate cho các module; Testcontainers locker smoke skip 2 test do Docker local không khả dụng. |
| 2026-06-13 | Backend | `mvn -pl locker-service -am test` | PASS/PARTIAL | Targeted test cho locker-service pass; Testcontainers PostgreSQL smoke được cấu hình `disabledWithoutDocker=true` nên skip trên máy không có Docker daemon. |
| 2026-06-13 | Git/Docs | `git diff --check` | PASS | Kiểm tra sau khi cập nhật Phase 2, không phát hiện whitespace/error. |
| 2026-06-13 | Backend | `mvn -B clean verify` | PASS | 14 module backend clean/test/package/verify pass với dependency OpenAPI/Prometheus và filter correlation ID mới. |
| 2026-06-13 | Backend | `mvn test` | PASS | 14 module backend build/test pass sau production hardening Phase 1. |
| 2026-06-13 | Backend | `mvn -pl common-lib,api-gateway -am test` | PASS | Targeted test cho `CorrelationIdFilter`, `CorrelationIdGatewayFilter`, và `SecuritySecrets`. |
| 2026-06-13 | Backend | `mvn test` tại `laundry-locker-microservices` | PASS | 14 module backend build/test pass; thêm 6 unit tests cho `SecuritySecrets`. |
| 2026-06-13 | Backend | `mvn -pl common-lib,api-gateway,auth-service,order-service,payment-service -am test` | PASS | Targeted test cho các module hardening bảo mật. |
| 2026-06-13 | Git/Docs | `git diff --check` | PASS | Không phát hiện whitespace/error trong diff hiện tại. |
| 2026-06-13 | Tài liệu | Đổi `BUSINESS_FLOWS_CURRENT.md` và `PROJECT_PROGRESS_TRACKER.md` sang tiếng Việt có dấu, sync mirror artifact, và quét heading tiếng Anh cũ | PASS | Hai file nguồn và bản copy artifact đã được Việt hoá; không đụng secret txt. |
| 2026-06-13 | Tài liệu | `rg -n "BUSINESS_FLOWS_CURRENT|PROJECT_PROGRESS_TRACKER" ...` và quét private-file trong `docs/project-artifacts` | PASS | Living docs đã được link/copy; `env.txt`, `pro.txt`, `Application.txt`, và `Host *.txt` không bị copy vào artifacts. |
| 2026-06-13 | Backend | Gateway health `GET /actuator/health` | PASS | Trả `200`. |
| 2026-06-13 | Backend | SEND/Rental/QR/RBAC/Maintenance E2E | PASS | Đã ghi trong `RUN_RESULT.md` và `HANDOFF_CODEX.md`. |
| 2026-06-13 | Web FE | `npm.cmd run build` trong `laundry-locker-frontend/fe` | PASS | TypeScript build pass. |
| 2026-06-13 | Flutter | `flutter pub get` | PASS | Dependency `qr_flutter` đã resolve. |
| 2026-06-13 | Flutter | Targeted `flutter analyze` | PASS | Không có issue trong các file locker/auth/home đã chạm. |
| 2026-06-13 | Flutter | `flutter build apk --debug` | PASS | Debug APK build thành công. |
| 2026-06-13 | Flutter | Customer emulator smoke | PASS | Login và 3 route quick action locker render được. |
| 2026-06-13 | Flutter | Manager/Maintenance backend role login | PASS | API role login đã verify; manual UI smoke bị giới hạn bởi emulator input. |
| 2026-06-13 | Deploy DB | TCP `146.190.84.136:15432` | PASS | Port reachable. |
| 2026-06-13 | Deploy DB | Service users connect vào DB | PASS | `auth_user`, `user_user`, `order_user`, `locker_user`, ... kết nối được. |
| 2026-06-13 | Deploy DB | Reset password `postgres` trong container | PASS | `docker exec ll-ms-postgres psql ...` verify access nội bộ container. |

## Việc Còn Lại

### P0 - Cần Cho Demo Sạch Hiện Tại

- Tạo hoặc reset demo accounts cho các role hiện tại:
  - Customer.
  - Admin.
  - Manager.
  - Maintenance.
- Browser smoke các trang admin locker/maintenance với deployed backend.
- Full smoke trên emulator/device:
  - Customer tạo SEND.
  - Customer tạo RENTAL.
  - Customer mở chi tiết đơn tủ và bấm chỉ đường tới locker.
  - Customer bấm "Mở tủ" (cần chạy `smart-locker-iot/simulate_demo_cabinet.py` song song) và "Đặt lại đơn" trên đơn COMPLETED/CANCELED.
  - Customer mở "Báo cáo của tôi" sau khi báo lỗi 1 ô.
  - Manager home.
  - Maintenance claim/resolve/clear fault và bấm chỉ đường tới locker lỗi; xác nhận customer tương ứng nhận được notification claim/resolve.
  - Xem trực quan UI mới của `MaintenanceHomePage` trên emulator/thiết bị thật (4 tab + bottom sheet hành động ô + 3 dialog) — phiên 2026-06-16 chỉ verify tĩnh bằng `flutter analyze`.
  - Maintenance bấm "Mở tủ khẩn cấp" (mobile + web `layout-view.tsx` + `BoxSettingModal.tsx`) và xác nhận ghi vào `box_access_logs`.
  - Customer đánh giá report sau khi RESOLVED; maintenance xem điểm trung bình.
  - Web admin: tạo/đã-kiểm-tra/xóa lịch bảo trì định kỳ; xem section "Sức khỏe thiết bị" có dữ liệu thật khi cabinet gửi heartbeat.
  - Thử PIN sai 5 lần liên tiếp trên 1 box, xác nhận box bị khóa tạm 15 phút.
- Verify notification runtime trên deploy/emulator:
  - Local gateway smoke đã PASS cho `/api/notifications`, unread count, FCM token save/delete và STOMP private `MESSAGE` qua `/ws` -> `/user/queue/notifications`.
  - Re-run cùng smoke trên deploy/emulator sau khi `api-dev` health route sẵn sàng.
  - Push FCM thật tới thiết bị khi Firebase credential/config production sẵn sàng.
- Quyết định deployed seed roles nên dùng `CUSTOMER` thay vì `USER` không.
- Xác nhận mobile `.env` trỏ đến deployed API khi test trên thiết bị thật.

### P1 - Làm Cứng Sản Phẩm Hiện Tại

- **Luồng tủ — backlog từ `LOCKER_FLOWS_STANDARD_SPEC.md`** (xem gap map G1–G16, lộ trình L1–L7):
  - L1 (ưu tiên cao nhất): vá auto-cancel `@Scheduled` + release ô khi hủy (G1/G2), thêm TTL cho ô `RESERVED`, đối soát drift trạng thái ô↔order (G4).
  - L2: cell `EXPIRED`/move-to-storage khi quá hạn (G3), enforce `size` khi reserve (G9).
  - L3: luồng nhận hàng courier `PARCEL_RECEIVE` + courier access code (G6).
  - L4+: gate thanh toán (G5), access-log từng lần mở ô (G11), SMS/email OTP thật (G13), PIN brute-force lockout (G14).
- **[2026-06-18 — BẮT BUỘC] Implement wallet/balance service cho topup**: endpoint `POST /api/payments/topup/create` đã có, tạo được VNPay URL và ghi nhận payment COMPLETED khi callback, nhưng **số dư user KHÔNG thay đổi** vì không có wallet service. TODO: (a) thêm `balance` field vào `user_profiles` hoặc tạo `wallet-service` riêng; (b) sau VNPay callback COMPLETED, publish RabbitMQ event `TOPUP_COMPLETED {userId, amount}` để wallet service consume + cộng balance; (c) implement `GET /api/wallet/balance` để mobile đọc số dư; (d) thêm idempotency check trong callback tránh double-credit. Xem chi tiết thiết kế ở `BUSINESS_FLOWS_CURRENT.md` mục 26.
- Thêm/verify payment UX cho SEND và RENTAL.
- Re-test old laundry lifecycle sau các thay đổi Phase 2 trong order service.
- Đồng nhất role name trên backend seed data, mobile, FE permissions và docs.
- Tạo demo data script ổn định cho deploy và local.
- Hoàn thiện phần còn lại sau production hardening Phase 3/4:
  - Mở rộng Testcontainers integration tests sang RabbitMQ và các flow order/locker/payment quan trọng.
  - Thêm end-to-end gateway tests khi có harness chạy đủ service thật.
  - Theo dõi runtime Swagger UI aggregation trên deploy sau khi service đăng ký Eureka.
- Thêm automated tests cho:
  - SEND PIN hai giai đoạn.
  - RENTAL extend/end.
  - QR hợp lệ/không hợp lệ.
  - Maintenance claim/resolve.
  - Gateway RBAC.

### P2 - Service Đang Thiếu Source

- Build lại hoặc remove scope `laundry-service`.
- `partner-service`: đã remove scope (gỡ role/seed/compose 2026-06-13). Còn lại: dọn FE partner pages deprecated khi có thời gian.
- Cập nhật FE services pages theo quyết định cuối.

### P3 - Capstone / Advanced Scope

- Tablet-web locker UI.
- Tích hợp sensor thật.
- Drone service.
- Drone simulator.
- Battery-aware assignment.
- Realtime map tracking.
- AI/RAG support.
- Dựng dashboard/alert thực tế cho Prometheus/Grafana/Loki hoặc stack observability được chọn.
- CI/CD release pipeline nâng cao: SLSA policy enforcement, immutable release setting trên GitHub, rollback có kiểm chứng sâu hơn, blue-green/zero-downtime nếu cần production nghiêm ngặt hơn.

## Rủi Ro Và Lưu Ý Đã Biết

- Tài liệu cũ có thể vẫn mô tả assumption trước Phase 2. Ưu tiên file này và `BUSINESS_FLOWS_CURRENT.md`.
- Root workspace không phải git repo, nên root docs không được track cùng backend changes.
- Nhiều repo đang dirty. Không bulk reset hoặc checkout.
- Mobile `.env` đang modified local và có thể chứa base URL riêng theo environment.
- Dự án có cả flow courier/logistics legacy và flow locker ops mới; không được mặc định chúng là một quy trình.
- Việc thiếu `laundry-service` và `partner-service` có thể làm vỡ FE/API expectations cũ.
- Password/env PostgreSQL trên DigitalOcean dễ gây nhầm lẫn vì Docker env không rewrite password sau khi volume đã init.

## Nhật Ký Thay Đổi Gần Đây

| Ngày | Người thực hiện | Thay đổi | File / Khu vực | Verification |
|---|---|---|---|---|
| 2026-07-14 | OpenCode | **Drone delivery Phase 1 — order foundation + mobile cutover khỏi local mock.** Backend: `order-service` thêm `POST /api/orders/drone-deliveries` + `GET /api/orders/{orderId}/drone-delivery`, DTO `CreateDroneDeliveryOrderRequest`/`DroneDeliveryOrderResponse`, repository lookup `findByUserIdAndIdempotencyKey`, entity `LockerOrder` thêm field drone foundation (`receiver_user_id`, `destination_locker_id`, `reserved_box_id`, `parcel_weight_grams`, `delivery_stage`, `idempotency_key`), Feign `LockerClient.reserveBox(id, channel)`, service `createDroneDelivery`/`getDroneDelivery`, migration **`V7__drone_delivery_order_foundation.sql`**, cancel/release dedupe cả `reservedBoxId`. Mobile: `LockerOpsService.createDroneDeliveryOrder()` gọi `/api/orders/drone-deliveries` với header `Idempotency-Key`; `DroneBookingSheet` chờ response backend, báo lỗi thật, disable nút khi submit, **không còn** tạo `DroneDeliveryStore`/`DroneTrackingPage` bằng ID giả `DRN-*`; thêm test service + widget cho flow mới. Đồng bộ docs sống + mirror; note plan/spec ban đầu ghi V6 nhưng implementation thực tế dùng **V7** vì V6 đã tồn tại cho promotion. | BE: `order-service/src/main/java/com/huynqb/laundrylocker/order/{controller/OrderController.java,service/OrderService.java,model/LockerOrder.java,repository/LockerOrderRepository.java,client/LockerClient.java,dto/CreateDroneDeliveryOrderRequest.java,dto/DroneDeliveryOrderResponse.java}`, `order-service/src/main/resources/db/migration/V7__drone_delivery_order_foundation.sql`, `order-service/src/test/java/com/huynqb/laundrylocker/order/service/OrderServiceDroneDeliveryTest.java`; Mobile: `lib/features/locker_ops/data/locker_ops_service.dart`, `lib/features/drone_delivery/presentation/widgets/drone_booking_sheet.dart`, `test/features/locker_ops/locker_ops_service_test.dart`, `test/features/drone_delivery/drone_booking_sheet_test.dart`; Docs: `BUSINESS_FLOWS_CURRENT.md`, `PROJECT_PROGRESS_TRACKER.md`, `mobile/docs/merge-status.md`, mirror backend docs, spec/plan under `docs/superpowers/`. | Static diagnostics **PASS** backend + mobile; Maven compile/test **chưa chạy** vì môi trường không có `mvn`/`mvnw`; Flutter verify runtime chưa chạy ở bước này. |
| 2026-07-03 | Claude | **Cân bằng ĐẦY ĐỦ chức năng quản trị Admin giữa mobile ↔ web (bổ sung 2 chiều).** So sánh màn Admin Flutter vs React, đắp mọi khoảng trống chính — **không đổi backend/API/route/RBAC/DB/event**, chỉ surface endpoint sẵn có ra client thiếu. **Web ← Mobile:** thêm trang `/admin/drones` (list mã/trạng thái/pin/tủ gốc/lý do lỗi; thêm/sửa/ngừng; đổi trạng thái + pin) + sidebar + i18n vi/en/ja + tag RTK `Drones`. **Mobile ← Web (Admin mobile từ 6 → 10 tab, TabBar cuộn ngang):** (1) **Điều chỉnh ví** trong bottom-sheet user (mirror `WalletModal`): `GET`+`POST /api/admin/wallet/{userId}[/adjust]`. (2) **Thanh toán**: list + lọc trạng thái + đổi trạng thái `PUT /api/admin/payments/{id}/status`. (3) **Phản hồi**: list (sao + nội dung) + lọc chưa/đã xử lý + **Trả lời** `POST /api/admin/feedback/{id}/reply` + đổi trạng thái `PATCH /api/admin/feedback/{id}/status`. (4) **Thông báo**: list + **broadcast** `POST /api/admin/notifications/broadcast` (+stats). (5) **Lịch**: `GET /api/admin/scheduler/status` + chạy tay 3 job `POST /api/admin/scheduler/{auto-cancel\|release-boxes\|pickup-reminders}`. Helper `_list` nay xử lý cả Spring Page (`.content`). **Còn lại (không làm — đã cover bởi role-app riêng):** Lockers/Maintenance-admin (mobile có Manager + Maintenance home), Loyalty (thấp ưu tiên). | Mobile branch `feat/admin-parity-mobile`: `lib/features/locker_ops/data/locker_ops_service.dart` (+admin wallet/payments/feedback/notifications/scheduler methods, `_list` unwrap Page), `presentation/pages/admin_home_page.dart` (+4 tab build methods + flows), `docs/manual-test/kich-ban/04-admin.md`. Web branch `feat/admin-drone-management`: `fe/src/stores/apis/admin/drones.ts` (mới) + `admin/index.ts`, `fe/src/stores/baseAPi.ts` (+tag), `fe/src/pages/Admin/drones/index.tsx` (mới), `fe/src/constants/sidebar.ts`, `fe/src/routes/routes-config.tsx`, `fe/messages/{vi,en,ja}.json`. Docs branch `docs/admin-parity-mobile-web`: tracker này (+mirror). | Mobile `flutter analyze` toàn dự án **0 error** (264 info lint cũ); `flutter build apk --debug` verify. Web `npm run build` (tsc -b && vite build) **built** (chỉ warning chunk-size cũ). Merge qua PR vào nhánh chính mỗi repo. |
| 2026-07-03 | Claude | **PA5 — Gỡ nghiệp vụ giặt ủi + Giao hàng Courier + Đăng ký NV/Courier khỏi dự án; hoàn thiện tính năng dở.** BE `order-service`: gỡ 5 endpoint lifecycle giặt (collect/weight/process/ready/return) + 5 service method; trạng thái cũ COLLECTED/PROCESSING/READY/RETURNED vẫn được complete/checkout chấp nhận. Mobile: xóa 7 feature dirs (courier_delivery, courier_dispatch, courier_registration, staff_application, logistics_send, user_laundry, staff) + trang legacy (order_page, courier pages, locker_action_page, locker_otp_page mock-OTP, courier_detail_page, staff_application_page) + courier mode (Home/Profile/main.dart/FCM incoming-order) + courier use cases/models/route/constants + bộ lọc Giặt ủi; `user_gateway_client` chuyển về `core/network`; legacy cancel wire lại `PUT /api/orders/{id}/cancel`. **Bổ sung:** (a) khóa ứng dụng sinh trắc học — `local_auth ^2.3.0`, `BiometricService`, toggle ở Bảo mật, gate splash (thử lại/đăng xuất), `MainActivity`→`FlutterFragmentActivity`, quyền `USE_BIOMETRIC`; (b) nút "Mở tủ" parse `accepted/message` từ iot-service (backend vốn chờ MQTT 20s rồi mới trả) — báo "tủ đã mở"/lỗi thật thay vì thông báo chung; (c) Trợ giúp/Liên hệ bottom sheet nội dung thật (mailto/website). Quên mật khẩu đã có từ 2026-07-02 (không trùng lặp — bỏ bản nháp sheet). | BE branch `feat/order-remove-laundry-endpoints`: `order-service/.../controller/OrderController.java`, `service/OrderService.java`. Mobile branch `feat/mobile-remove-courier-laundry`: xóa `lib/features/{courier_*,staff_application,logistics_send,user_laundry,staff}/**` + legacy pages; sửa `main.dart`, `app_router.dart`, `home_page.dart`, `profile_page.dart`, `firebase_messaging_service.dart`, `my_locker_orders_page.dart`, `customer_order_detail_page.dart`, `order_provider/injection`, `order_repository(+impl)`, `order_remote_data_source(+impl)`, `store_service.dart`, `api_constants.dart`; mới `core/services/biometric_service.dart`; `security_page.dart`, `splash_screen.dart`, `pubspec.yaml`, `MainActivity.kt`, `AndroidManifest.xml`. Docs: 2 file sống + mirror. | BE `mvn -pl order-service -am compile` **BUILD SUCCESS**. Mobile `flutter analyze` **0 error/0 warning** (333 info lint cũ); `flutter build apk --debug` chạy nền. |
| 2026-07-02 | Claude | **Đóng gap G3/G4 + Manager thao tác đơn (file 04 mobile).** (1) **G3 — job dời kho khi quá hạn lấy hàng**: `OrderService.releaseOverdueOrders()` — đơn STORING/RETURNED quá `pickupDeadline` hơn `app.order.overdue-release-hours` (mặc định 24h, 0=tắt) → chốt phí quá hạn, chuyển **EXPIRED**, thu hồi PIN, **cắt tham chiếu sendBoxId/receiveBoxId** (chống double-release ô đã cấp cho đơn khác) + nhả ô + notify khách/người nhận; `checkout` chấp nhận thêm EXPIRED (nhân viên trao trả từ kho); `nextAction=CONTACT_STAFF`; cron `overdue-release-cron` (mặc định phút :05 mỗi giờ) + trigger tay `POST /api/admin/scheduler/release-overdue`. (2) **G4 — job đối soát ô ↔ đơn**: locker-service `GET /internal/boxes` (`listBoxesForReconcile`: id/lockerId/status/reservedUntil); order-service `reconcileBoxStates()` hai chiều — ô RESERVED/OCCUPIED mồ côi (không đơn INITIALIZED/STORING/RETURNED nào giữ) → release (RESERVED còn hạn giữ chỗ thì bỏ qua, tránh race đơn vừa tạo; hạn null coi như mồ côi); đơn hoạt động mà ô AVAILABLE → reserve/occupy lại (re-check trạng thái đơn ngay trước khi sửa); cron `reconcile-cron` (phút :35 mỗi giờ) + `POST /api/admin/scheduler/reconcile-boxes`. (3) **Manager đổi trạng thái đơn**: `PATCH /api/manage/orders/{id}/status` (RBAC `/api/manage/**` MANAGER/ADMIN sẵn có ở gateway — KHÔNG đổi gateway; manager ghi làm actor qua `X-User-Id` nếu request không có staffId). Mobile: `managerSetOrderStatus` + tab Đơn hàng Manager bấm đơn mở bottom sheet đổi trạng thái + filter EXPIRED. (4) **Mobile Quên mật khẩu**: trang `/forgot-password` (email → OTP 6 số → mật khẩu mới) wire vào `POST /api/auth/forgot-password`/`reset-password` sẵn có; nút "Quên mật khẩu?" ở `LoginScreen` + `AuthBottomSheet` hết toast chết. Không migration/DB mới, không đổi event. | BE branch `feat/order-overdue-reconcile`: `order-service/.../service/{OrderService,OrderScheduler}.java`, `controller/OrderController.java`, `client/LockerClient.java`; `locker-service/.../service/LockerService.java`, `controller/LockerController.java`. Mobile branch `feat/mobile-forgot-password-manager-orders`: `lib/features/auth/...` (data source/repository/use case `forgot_password_use_case` mới/provider `forgot_password_provider` mới/page `forgot_password_page` mới, `login_screen`, `auth_bottom_sheet`), `lib/core/routing/app_router.dart`, `lib/features/locker_ops/data/locker_ops_service.dart`, `lib/features/locker_ops/presentation/pages/manager_home_page.dart`. Docs: file 04 mobile + tracker này + `BUSINESS_FLOWS_CURRENT.md`. | BE `mvn compile` 2 module **BUILD SUCCESS**; mobile `flutter analyze` **0 error**. Chưa commit/push (user tự commit + merge). |
| 2026-06-30 | Claude | **Cảnh báo phần cứng toàn cục — slice mobile UI.** Wire `GET /api/maintenance/box-anomalies` (slice backend) vào `MaintenanceHomePage`. (1) `LockerOpsService.boxAnomalies()`. (2) State `_anomalies` + load best-effort trong `_load` (nuốt lỗi nếu BE/IoT chưa có). (3) `_boxAnomaliesSection` ở đầu tab "Sự cố" (`_buildQueue`): banner đỏ đếm số ô + mỗi ô card "tên tủ · Ô #n (cellType)" + `StatusChip` logic + "Cửa MỞ · báo lúc ..." + nút **Chỉ đường** (dùng lại `_openDirections` với `lockerLatitude/lockerLongitude/lockerAddress` từ response). Ẩn khi `_anomalies` rỗng. Không đổi backend/API/route/role. | Mobile branch `feat/maintenance-box-anomalies`: `lib/features/locker_ops/data/locker_ops_service.dart`, `lib/features/locker_ops/presentation/pages/maintenance_home_page.dart`. Docs BE branch `feat/maintenance-box-anomalies-mobile`: `BUSINESS_FLOWS_CURRENT.md` (mục 21) + `PROJECT_PROGRESS_TRACKER.md` (+mirror). | `flutter analyze` 2 file **No issues found**. Verify tĩnh; chưa smoke emulator. Không đổi backend/API/DB/event. Chưa commit/push (user tự commit). |
| 2026-06-30 | Claude | **Tổng quan cảnh báo phần cứng toàn cục (slice backend).** Box-health theo từng tủ → thêm endpoint gom mọi ô bất thường trên TẤT CẢ tủ. `LockerService.boxAnomalies()`: Feign `iotClient.boxStatus(null)` lấy toàn bộ hardware status (best-effort, try/catch → rỗng); lọc `hwState=OPEN` && box tồn tại && logic ≠ `OCCUPIED`; enrich locker (`lockerRepository.findById` → code/name/address/lat/long, giống `openFaults`) → `BoxAnomalyResponse`. Endpoint `GET /api/maintenance/box-anomalies` (MAINTENANCE/ADMIN). Read-only, **không** side-effect (không tạo report/notify). KHÔNG đổi state machine/DB/event. Slice mobile (section tổng quan ca trực) để sau. | BE branch `feat/maintenance-box-anomalies`: `locker-service/.../dto/BoxAnomalyResponse.java` (mới), `service/LockerService.java` (+`boxAnomalies`, +import ArrayList), `controller/LockerController.java` (+endpoint). Docs: `BUSINESS_FLOWS_CURRENT.md` (mục 2) + `PROJECT_PROGRESS_TRACKER.md` (+mirror). | `mvn -pl locker-service -am compile` **BUILD SUCCESS**. Verify tĩnh; chưa E2E. Chưa commit/push (user tự commit). Slice mobile chưa làm. |
| 2026-06-30 | Claude | **Box-health cho MAINTENANCE — slice mobile UI (follow-up GAP 2).** Wire endpoint `GET /api/maintenance/lockers/{lockerId}/box-health` (slice backend trước) vào `MaintenanceHomePage`. (1) `LockerOpsService.boxHealth(lockerId)`. (2) State `_boxHealth` + load best-effort trong `_selectLocker` (clear khi đổi tủ, guard `_selectedLockerId == lockerId`, nuốt lỗi nếu BE/IoT chưa có dữ liệu). (3) Card "Tình trạng phần cứng ô" trong tab "Kiểm tra tủ" (`_boxHealthCard`/`_boxHealthRow`): mỗi ô hiện trạng thái logic (`StatusChip`) cạnh trạng thái cửa phần cứng (MỞ/đóng/chưa có tín hiệu + "Báo lúc ..."); ô `needsAttention` tô đỏ + banner đếm số ô cần kiểm tra; rỗng → hint. Surface MAINTENANCE cho GAP 2 (trước chỉ Manager/Admin web). Không đổi backend/API/route/role. | Mobile branch `feat/maintenance-box-health`: `lib/features/locker_ops/data/locker_ops_service.dart`, `lib/features/locker_ops/presentation/pages/maintenance_home_page.dart`. Docs BE branch `feat/maintenance-box-health-mobile`: `BUSINESS_FLOWS_CURRENT.md` (mục 21) + `PROJECT_PROGRESS_TRACKER.md` (+mirror). | `flutter analyze` 2 file **No issues found**. Verify tĩnh; chưa smoke emulator. Không đổi backend/API/DB/event. Chưa commit/push (user tự commit). |
| 2026-06-30 | Claude | **Box-health cho MAINTENANCE (follow-up GAP 2, slice backend).** GAP 2 mới expose hardware box-status cho Manager/Admin; nay cho vai trò **bảo trì** xem. (1) iot-service `GET /internal/iot/box-status` (service-to-service, `?lockerId=`, delegate `listBoxHardwareStatuses`). (2) locker-service `IotClient.boxStatus` + record `BoxHardwareStatus`; `LockerService.boxHealth(lockerId)` lấy `LockerBox` (trạng thái logic theo đơn) + Feign hardware (best-effort, try/catch → Map rỗng nếu iot-service chết), ghép theo boxId → `BoxHealthResponse{boxId, boxNumber, cellType, logicalStatus, hwState, lastReportedAt, doorOpen, needsAttention}`; `doorOpen`=hwState OPEN, `needsAttention`=doorOpen && logic ≠ OCCUPIED (cửa mở trên ô không đang dùng). (3) `GET /api/maintenance/lockers/{lockerId}/box-health` (MAINTENANCE/ADMIN, route `/api/maintenance/**` sẵn có — KHÔNG đổi gateway). **KHÔNG** sửa `LockerBox.status`/state machine, không tự reconcile/ghi đè, không DB/migration/event. Slice mobile UI để task sau. | BE branch `feat/maintenance-box-health`: `iot-service/.../controller/IotController.java`; `locker-service/.../client/IotClient.java`, `dto/BoxHealthResponse.java` (mới), `service/LockerService.java`, `controller/LockerController.java`. Docs: `BUSINESS_FLOWS_CURRENT.md` (mục 2/19) + `PROJECT_PROGRESS_TRACKER.md` (+mirror). | `mvn -pl iot-service,locker-service -am compile` **BUILD SUCCESS**. Verify tĩnh; chưa E2E runtime (cần JWT MAINTENANCE + dữ liệu hardware). Chưa commit/push (user tự commit). Slice mobile UI chưa làm. |
| 2026-06-30 | Claude | **Cabinet → backend: trạng thái phần cứng ô (GAP 2, bản an toàn).** Đóng hướng IoT→backend còn lại: cabinet báo cửa/cảm biến thật, backend lưu lại để ops đối chiếu. Trước đây `IotService.updateBoxStatus` chỉ `publishRawDeviceStatus` (event `iot.device.status.changed`) rồi vứt — không persist gì về ô. Nay: (1) bảng mới `iot_schema.box_hardware_status` (V3, box_id PK, locker_id, hw_state, last_reported_at) **tách hẳn** `LockerBox.status` (locker-service/order-driven) ⇒ hardware truth KHÔNG bao giờ ghi đè trạng thái logic theo đơn; (2) `updateBoxStatus` upsert hàng này + vẫn publish event (backward-compatible); (3) `BoxStatusUpdateRequest` thêm `lockerId` (optional) — MQTT handler `cabinet/+/locker/+/status` lấy lockerId từ segment topic (`parseLongOrNull`), REST có thể bỏ trống; (4) `GET /api/manage/iot/box-status` (+`?lockerId=`, Manager/Admin, route `/api/manage/iot/**` sẵn có — KHÔNG đổi gateway) trả danh sách; (5) simulator sau khi mở thành công báo `cabinet/{lockerId}/locker/{boxId}/status` OPEN rồi CLOSED sau `SIM_DOOR_CLOSE_SECONDS`. **KHÔNG đổi** locker-service/`LockerBox.status`, không tự reconcile/ghi đè, không migration cũ, không `main.py`/serial/setup. | BE branch `feat/iot-hardware-status-reconcile`: `iot-service/.../db/migration/V3__box_hardware_status.sql` (mới), `model/BoxHardwareStatus.java` (mới), `repository/BoxHardwareStatusRepository.java` (mới), `dto/BoxHardwareStatusResponse.java` (mới), `dto/BoxStatusUpdateRequest.java`, `service/IotService.java`, `controller/IotController.java`, `service/LockerMqttService.java`. IoT branch `feat/iot-hardware-status-reconcile`: `smart-locker-iot/simulate_demo_cabinet.py`. Docs: `BUSINESS_FLOWS_CURRENT.md` (mục 19) + `PROJECT_PROGRESS_TRACKER.md` (+mirror). | BE `mvn -pl iot-service -am compile` **BUILD SUCCESS**; simulator `python -m py_compile` **OK** + smoke door-state **OK**. Verify tĩnh; chưa E2E runtime (cần deploy V3 + broker). Chưa surface cho MAINTENANCE (follow-up). Chưa commit/push (user tự commit). |
| 2026-06-30 | Claude | **Simulator phát heartbeat → device-health (GAP 3).** Đóng lỗ hổng IoT→backend: handler `cabinet/{id}/heartbeat` (iot-service `LockerMqttService`→`IotService.updateStatus`) + dashboard `GET /api/manage/iot/device-status` đã có sẵn nhưng KHÔNG ai gửi heartbeat ⇒ dashboard luôn rỗng, thiết bị "offline". Cho `simulate_demo_cabinet.py` (1) **học cabinet từ traffic**: mọi lệnh `cabinet/{id}/command/open|sync` → `_learn_cabinet` thêm id + heartbeat ngay (hiện ONLINE tức thì); (2) **seed up-front** qua env `SIM_HEARTBEAT_CABINETS=2,3` (announce khi connect/reconnect); (3) **thread định kỳ** `_heartbeat_loop` mỗi `SIM_HEARTBEAT_SECONDS` (mặc định 30s) publish `{status:ONLINE,timestamp}` cho mọi cabinet đã biết; guard `is_connected()` để không publish khi mất link. Thuần simulator — backend Java không đổi (đã hỗ trợ heartbeat). | IoT branch `feat/iot-simulator-heartbeat`: `smart-locker-iot/simulate_demo_cabinet.py` (thêm `_publish_heartbeat`/`_learn_cabinet`/`_heartbeat_loop` + hook trong `_on_connect`/`_on_message`/`main`, 2 env mới, docstring). Docs (cùng branch BE): `PROJECT_PROGRESS_TRACKER.md`, `BUSINESS_FLOWS_CURRENT.md` mục 19 (+mirror). | `python -m py_compile` **OK**; smoke logic stub client **OK** (learn→heartbeat ONLINE, no double-register, sync learns cabinet, disconnected→no publish). Chưa round-trip thật + dashboard phiên này. Không đổi backend/API/DB/event. Chưa commit/push (user tự commit). |
| 2026-06-29 | Claude | **Đồng bộ booking → IoT (GAP 1).** Đóng lỗ hổng: đặt đơn SEND/RENTAL trước đây chỉ đổi trạng thái ô trong DB, cabinet không hề biết. Nay mỗi lần ô đổi vòng đời, `locker-service` báo xuống tủ qua MQTT để "đặt đơn → tủ mô phỏng đồng bộ". (1) **iot-service**: `BoxStateSyncRequest` (record `lockerId/boxId/state/orderId`); `LockerMqttService.publishBoxStateSync` publish `cabinet/{lockerId}/command/sync` body `{boxId,state,orderId?}` — fire-and-forget, guard client null/disconnected, nuốt lỗi → trả `false`; `IotService.syncBoxState`; `IotController` `POST /internal/iot/box-sync` (service-to-service, chặn qua gateway). (2) **locker-service**: `IotClient.syncBoxState` (+record); helper best-effort `syncBoxStateQuietly(box,state)` (try/catch nuốt lỗi, không vỡ luồng đặt đơn) gọi trong `reserveBox`(RESERVED)/`occupyBox`(OCCUPIED)/`releaseBox`(AVAILABLE)/`markFault`(FAULT)/`clearFault`(AVAILABLE). (3) **simulator** `simulate_demo_cabinet.py`: subscribe thêm `cabinet/+/command/sync`, nhánh `_on_message` log `[SIM] SYNC: ... state=...` mô phỏng cabinet cập nhật sơ đồ ô. **Không đổi**: gateway route/RBAC, DB/migration, event RabbitMQ, API public, UI mobile/web, `main.py`/serial/setup hardware track. | BE branch `feat/iot-booking-state-sync`: `iot-service/.../dto/BoxStateSyncRequest.java` (mới), `service/LockerMqttService.java`, `service/IotService.java`, `controller/IotController.java`; `locker-service/.../client/IotClient.java`, `service/LockerService.java`. IoT branch `feat/iot-booking-state-sync`: `smart-locker-iot/simulate_demo_cabinet.py`. Docs: `BUSINESS_FLOWS_CURRENT.md` (mục 19), `PROJECT_PROGRESS_TRACKER.md` (+mirror). | BE `mvn -pl iot-service,locker-service -am compile` **BUILD SUCCESS**; simulator `python -m py_compile` **OK**. Verify tĩnh; chưa E2E runtime (cần deploy iot/locker + chạy simulator để thấy `[SIM] SYNC`). Chưa commit/push (user tự commit). |
| 2026-06-28 | Claude | **Push notification trạng thái giao hàng (drone delivery) — phase noti-only** (chưa live map/websocket/MQTT). Tái dùng hạ tầng FCM sẵn có. **Mobile** (mở rộng, không dựng lại): (1) `delivery_notification.dart` — enum `DeliveryStatus` 6 mốc + parse `message.data` `{orderId,status,eta,message}` + icon/màu/title/body tiếng Việt. (2) `FirebaseMessagingService` — thêm `onTokenRefresh` (tự đăng ký lại token), refactor `_registerToken`/lưu `ApiClient`; tap noti → deep-link `order_detail` theo `orderId` ở **cả 3 trạng thái app** (onMessageOpenedApp/getInitialMessage + tap banner foreground qua **payload JSON**); local-notif fallback title/body theo status; bỏ toast trùng cho noti giao hàng. (3) `notification_permission_helper.dart` — kiểm tra/xin quyền + hướng dẫn mở Settings khi từ chối vĩnh viễn. (4) `notification_list_page.dart` — lịch sử tap `ORDER_STATUS_CHANGED` → order detail + icon truck. **Backend** `notification-service`: (5) `DeliveryNotificationService` (enum 6 status + title/body VN + `notifyDeliveryStatus` + `notifyFromEvent`), `DeliveryStatusNotificationRequest`, `NotificationService.create(req, extraData)` overload (merge orderId/status/eta vào FCM data, backward-compatible), `POST /internal/notifications/delivery-status`, binding + listener cho event `delivery.status.changed`. Nhắm theo **userId người nhận** (reuse `fcm_tokens`+`sendToUser`, không đổi DB). | Mobile branch `feat/mobile-delivery-push-notifications`: `lib/features/notifications/domain/entities/delivery_notification.dart` (mới), `lib/core/services/notification_permission_helper.dart` (mới), `lib/core/services/firebase_messaging_service.dart`, `lib/features/notifications/presentation/pages/notification_list_page.dart`. BE branch `feat/notification-delivery-status`: `notification-service/.../service/{DeliveryNotificationService,NotificationService,NotificationEventListener}.java`, `dto/DeliveryStatusNotificationRequest.java`, `controller/NotificationController.java`, `config/RabbitConfig.java`; `common-lib/.../event/DomainEventNames.java`. | Mobile `flutter analyze` **No issues found**; BE `mvn -pl notification-service -am compile` **BUILD SUCCESS**. Chưa smoke push thật (cần Firebase credential + thiết bị); luồng giao drone bắn event thật vẫn TODO (test bằng internal endpoint). |
| 2026-06-28 | Claude | **Telemetry & điều khiển real-time MAVLink (Flight Data, Mảng 2) cho mobile.** Kết nối flight controller ArduPilot/PX4 thật (hoặc SITL) để xem drone live + điều khiển. (1) **Codec MAVLink** tự viết thuần Dart (không thêm dependency): `MavlinkCrc` (CRC-16/MCRF4XX + bảng `CRC_EXTRA` common.xml), `MavlinkProtocol` parse khung v1+v2 (buffer streaming, resync khi sai CRC, học sysid/compid từ heartbeat) + encode COMMAND_LONG/SET_MODE/REQUEST_DATA_STREAM/HEARTBEAT. (2) **Transport** `dart:io`: `UdpMavConnection` (học địa chỉ peer từ datagram đầu, hợp SITL/WiFi) + `TcpMavConnection`; abstraction `MavConnection` sẵn cho USB-serial/BT sau. (3) **Domain** message (HEARTBEAT/SYS_STATUS/GPS_RAW_INT/ATTITUDE/GLOBAL_POSITION_INT/VFR_HUD/COMMAND_ACK/STATUSTEXT), `DroneTelemetry` (fold message → state: vị trí/alt/heading/speed/attitude/pin/GPS/mode/armed/link), `flight_modes` (ArduCopter custom_mode → tên). (4) **UI** `FlightDataController` (ChangeNotifier: link→codec→state, heartbeat 1Hz, lệnh), `FlightDataPage` (bản đồ live + vệt bay + HUD `AttitudeIndicator` chân trời nhân tạo + readouts + lệnh ARM/DISARM/RTL/LOITER/GUIDED/AUTO/TAKEOFF + sheet kết nối UDP/TCP). Route `/drone/flight-data` + nút entry tab "Drone". **USB-serial/Bluetooth chưa làm.** | Mobile branch `feat/mobile-drone-mission-planner`: `lib/features/drone_telemetry/data/{mavlink_crc,mavlink_protocol,mav_connection}.dart` (mới), `domain/{mav_messages,drone_telemetry,flight_modes}.dart` (mới), `presentation/{flight_data_page,flight_data_controller}.dart` + `widgets/attitude_indicator.dart` (mới); `lib/core/routing/app_router.dart` (+import/+const/+GoRoute); `lib/features/locker_ops/.../maintenance_home_page.dart` (+nút "Flight Data" + helper `_droneToolCard`); `test/features/drone_telemetry/mavlink_protocol_test.dart` (mới). | `flutter analyze` **No issues found**; `flutter test mavlink_protocol_test.dart` **9/9 PASS** (gồm CRC khớp check-value chuẩn `0x6F91`). Chưa smoke SITL/drone thật phiên này. Không đổi backend/API/DB/event. |
| 2026-06-28 | Claude | **Mission Planner — Lập kế hoạch bay (Flight Plan, Mảng 1) cho mobile.** Thuần frontend + data model, không cần drone thật. Feature mới `lib/features/drone_mission`: (1) **domain** — `MavCommand` (enum lệnh MAVLink kèm `code` chuẩn ArduPilot/PX4 + metadata param), `MissionItem` (1 dòng mission: frame/command/lat/lng/alt/param1-4 + `extraValue`/`movedTo`/`withExtraValue`), `FlightMission` (home + items + cruiseSpeed + defaultAltitude; tính `pathPoints`/`totalDistanceMeters` haversine/`estimatedSeconds`/`waypointCount`; add/update/remove/reorder bất biến). (2) **data** — `MissionCodec` encode/decode **QGC WPL 110** (`.waypoints`, item 0 = HOME, tab-separated, tương thích Mission Planner/QGroundControl) + **JSON nội bộ** lossless; `MissionRepository` lưu library qua `shared_preferences` + export/import file qua `file_picker`. (3) **presentation** — `MissionPlannerPage`: `flutter_map`+OSM, chạm thêm waypoint, marker đánh số + HOME, polyline đường bay, chế độ "di chuyển", edit sheet (đổi lệnh/độ cao/param/xóa), reorder list, summary bar (số điểm/quãng đường/ETA), menu lưu/mở/xuất/nhập/đặt HOME/tạo mới/xóa, cài đặt độ cao-tốc độ. Route `/drone/mission-planner` + nút entry trong tab "Drone" của `MaintenanceHomePage`. **Mảng 2 (telemetry MAVLink real-time) chưa làm.** | Mobile branch `feat/mobile-drone-mission-planner`: `lib/features/drone_mission/domain/{mav_command,mission_item,flight_mission}.dart` (mới), `lib/features/drone_mission/data/{mission_codec,mission_repository}.dart` (mới), `lib/features/drone_mission/presentation/mission_planner_page.dart` (mới) + `presentation/widgets/waypoint_edit_sheet.dart` (mới); `lib/core/routing/app_router.dart` (+import/+const/+GoRoute); `lib/features/locker_ops/presentation/pages/maintenance_home_page.dart` (+import AppRouter, +OpsCard entry trong `_buildDroneFleet`); `test/features/drone_mission/mission_codec_test.dart` (mới). | `flutter analyze` (feature + 2 file sửa + test) **No issues found**; `flutter test mission_codec_test.dart` **7/7 PASS**. Chưa smoke trên emulator (verify tĩnh + unit test). Không đổi backend/API/DB/event. |
| 2026-06-21 | Claude | **Thanh toán đa hình thức (ví + VNPay + MoMo + tiền mặt).** (1) **Ví nội bộ** payment-service: bảng `wallets`+`wallet_transactions` (V3), `WalletService` (credit/debit idempotent theo `(source,referenceId)`, optimistic lock `@Version`), endpoint `GET /api/wallet`(+transactions) + admin `GET/POST /api/admin/wallet/{userId}*`. Nạp VNPay COMPLETED → cộng ví (sửa `handleVnPayReturn`). (2) **Checkout đơn** `POST /api/payments/checkout` (WALLET trừ ví tức thì / VNPAY / MOMO / CASH), amount lấy từ order-service, chặn double-pay. (3) **order-service** nghe `PAYMENT_COMPLETED` (queue `order.payment.events`, `OrderPaymentEventListener`) → đơn `payment_status=PAID` (V5). (4) **MoMo thật** `MomoService` (AIO v2 create + HMAC SHA256 + verify IPN), callback/IPN controller thật, gated env (chưa cấu hình → `MOMO_NOT_CONFIGURED`). (5) **Gateway** route `/api/wallet/**`+`/api/admin/wallet/**`; **compose** thêm env VNPAY_*/MOMO_*. (6) **Mobile** bật cờ ví + sửa endpoint `/api/wallet`. (7) **Web admin** ví: RTK `wallet.ts`, `WalletModal`, nút "Ví" trong Users. | BE: payment-service (`model/{Wallet,WalletTransaction}`,`repository/Wallet*`,`service/{WalletService,MomoService,PaymentService}`,`controller/{WalletController,PaymentController}`,`dto/{CheckoutRequest,Wallet*}`,`db/migration/V3__wallet.sql`); order-service (`model/LockerOrder`,`config/RabbitConfig`,`service/OrderPaymentEventListener`,`db/migration/V5__order_payment_status.sql`); `api-gateway/application.yml`; `docker-compose.yml`. Mobile: `core/config/feature_flags.dart`, `features/wallet/.../wallet_provider.dart`. Web: `constants/api-paths.ts`, `stores/apis/admin/{wallet.ts,index.ts}`, `stores/baseAPi.ts`, `pages/Admin/users/components/{WalletModal,UserTable}.tsx`. | BE `mvn compile` **BUILD SUCCESS**; mobile `flutter analyze` ví **0 issue**; web `npm run build` **PASS**. Chưa E2E sandbox; chưa wire nút trả tiền trong mobile locker_ops. |
| 2026-06-21 | Claude | **Hoàn tất + sửa lỗi WIP đăng ký user mobile qua Firebase + admin web cấp role khác** (theo `guides/MOBILE_SELF_REGISTER_PLAN.md`). Kiểm tra WIP dang dở trên `develop`/`main`: phần lớn đã code (self-register ép CUSTOMER, `firebaseLogin`+`/api/auth/firebase`, `social_identities` V2, `createAccount`+`/internal/auth/accounts`, `adminCreate` tạo auth_account, UI mobile Google/phone-OTP, web đổi role list). **Sửa các lỗi chặn build/boot**: (1) BE — duplicate import `lombok.extern.slf4j.Slf4j` trong `FirebaseConfig.java` (lỗi compile); duplicate key root `logging:` trong `auth-service application.yml` (YAML duplicate-key → fail boot). (2) Mobile — `FirebaseAuthService.signInWithGoogle()` dùng API google_sign_in **v6** (`GoogleSignIn()`+`.signIn()`+`await .authentication`) nhưng project dùng **v7.2.0** → 2 lỗi compile; viết lại theo v7 (`GoogleSignIn.instance.initialize()`+`authenticate()`, `.authentication` sync) **và** route Google qua `FirebaseAuth.signInWithCredential` để trả **Firebase** idToken (trước đó trả Google idToken thô → backend `verifyIdToken` sẽ từ chối). (3) Web — `RoleNameSchema` (Zod) còn stale `USER/STAFF/MODERATOR/PARTNER` → tạo MANAGER/MAINTENANCE bị chặn client-side; mở rộng thành superset gồm CUSTOMER/ADMIN/MANAGER/MAINTENANCE + giữ legacy. (4) Xóa file rác `payload.json` (curl test có password). | BE: `auth-service/.../config/FirebaseConfig.java`, `resources/application.yml`; (đã có sẵn từ WIP) `service/AuthService.java`, `controller/AuthController.java`, `model/SocialIdentity.java`, `repository/SocialIdentityRepository.java`, `dto/{CreateAccountRequest,FirebaseLoginRequest}.java`, `db/migration/V2__auth_social_identities.sql`, `pom.xml`; `user-service/.../controller/UserController.java`, `client/AuthClient.java`, `dto/AdminCreateUserRequest.java`. Mobile: `lib/core/services/firebase_auth_service.dart`, `lib/features/auth/application/use_cases/social_login_use_case.dart`, `presentation/widgets/auth_bottom_sheet.dart`, `presentation/providers/{login_provider,auth_injection}.dart`, data source/repository auth, `pubspec.yaml`, `android/app/build.gradle.kts`. Web: `fe/src/pages/Admin/users/components/{CreateUserModal,UserTable}.tsx`, `fe/src/schemas/admin.schemas.ts`. | BE `mvn -pl auth-service,user-service -am compile` **BUILD SUCCESS**; Mobile `flutter analyze` **0 error** (410 info cũ); Web `npm run build` **PASS**. Chưa E2E runtime (cần Firebase service account + google-services.json). Chưa commit/push (đang ở develop/main). Facebook chưa bật. |
| 2026-06-19 | Claude | **Mobile (web run): fix lỗi đỏ + ẩn thông tin nhạy cảm khi chạy trên Chrome.** User chạy app trên web (`flutter run -d chrome`) thấy nhiều lỗi đỏ + console đổ dữ liệu nhạy cảm. Sửa: (1) **Firebase web** — bọc init bằng `if (!kIsWeb)` (web chưa cấu hình `DefaultFirebaseOptions` → trước đó ném lỗi đỏ "have not been configured for web"); thêm `import foundation show kIsWeb`. (2) **Dio logger** — tắt `requestHeader/requestBody/responseBody` trong `PrettyDioLogger` (trước in cả accessToken/refreshToken + toàn bộ body ra console — "thông tin không cần thiết"), chỉ giữ dòng request + lỗi. (3) **RenderFlex overflow** màn đăng nhập — Row "Đăng nhập nhanh (DEV)" tràn 68px khi viewport hẹp; bọc Text giữa vào `Flexible`+`ellipsis` (2 Divider đã `Expanded`). Đã reproduce trên Chrome: overflow KHÔNG xảy ra ở width mặc định ⇒ chỉ ở viewport hẹp như màn DevTools chia đôi của user. (4) **Làm rõ logo launcher**: icon đã là logo navy đúng trong project (verify pixel góc), máy user còn hiện Flutter-default vì cài APK build cũ → cần build + cài lại. | Mobile: `lib/main.dart` (guard `!kIsWeb` + import), `lib/core/network/dio_client.dart` (logger gọn), `lib/features/auth/presentation/pages/login_screen.dart` (Flexible text). | `flutter analyze` **0 error/0 warning** (406 info cũ); `flutter build apk --debug` **√ Built**; reproduce web bằng `flutter run -d chrome`. |
| 2026-06-19 | Claude | **Mobile: kiểm tra & dọn lỗi đỏ + đổi logo + tạm ẩn tính năng backend chưa có.** (1) **Kiểm tra**: `flutter analyze` = 0 error / 0 warning (406 info lint cũ); `flutter build apk --debug` PASS. File `problems-report.html` user đang mở chỉ là 15 WARNING deprecation Groovy DSL của plugin (không phải lỗi project). (2) **Đổi logo**: copy logo tủ khóa navy người dùng cung cấp → `assets/images/logo.png`, đổi `pubspec.yaml` `flutter_launcher_icons.android: "launcher_icon"`→`true` (khớp manifest `@mipmap/ic_launcher`), chạy `dart run flutter_launcher_icons` regen icon Android (mọi mật độ) + iOS. Logo trong app (splash/onboarding/appbar) tự đổi vì cùng dùng `assets/images/logo.png`. (3) **Tạm ẩn tính năng không hoạt động** (backend chưa có endpoint → 404 + UI rỗng): tạo `lib/core/config/feature_flags.dart` (5 cờ, mặc định `false`); ví/số dư (`/wallet/balance`), nạp tiền & giao dịch (`/payments/transactions`), gói dịch vụ/subscription (`/plans/customer`,`/subscriptions/*`), kho voucher cá nhân (`/promotions/vouchers/my`), quảng cáo+blog trang chủ (`/advertisements`,`/blogs`). Short-circuit ngay tại `HomeRepository`/`WalletProvider` (không gọi mạng → hết lỗi đỏ), ẩn card số dư trên home + section ví courier, ẩn 3 menu Profile (Gói dịch vụ/Ưu đãi&Quà tặng/Giao dịch). **Không xóa route/page**, chỉ ẩn entry-point bằng cờ → bật lại dễ. Trang "Ưu đãi" (`/api/promotions/active`) vẫn chạy bình thường, không bị ẩn. Flow SEND/RENTAL thật (locker_ops) không phụ thuộc ví nên không ảnh hưởng. | Mobile branch `feat/mobile-hide-broken-and-rebrand-logo`: `lib/core/config/feature_flags.dart` (mới); `lib/features/home/data/repositories/home_repository.dart`; `lib/features/wallet/presentation/providers/wallet_provider.dart`; `lib/features/home/presentation/pages/home_page.dart`; `lib/features/profile/presentation/pages/profile_page.dart`; `pubspec.yaml`; `assets/images/logo.png`; `android/.../mipmap-*/ic_launcher.png`; `ios/.../AppIcon.appiconset/*`. | `flutter analyze` **0 error/0 warning** (406 info cũ, không tăng); `flutter build apk --debug` → **√ Built app-debug.apk**. Chưa chạy emulator phiên này (verify tĩnh + build). Chưa commit/push. |
| 2026-06-18 | Claude | **Redesign customer home page + Fix "Ưu đãi" chip dead API.** (1) **Flash Sale section**: Xoá `_buildTodayServicesSection`/`_buildServicePlanCard` (3 card cứng không có API thật); thêm `_buildFlashSaleSection` fetch `GET /api/promotions/active` (endpoint đã có sẵn trong `order-service`). Flash Sale card horizontal scroll 260×175px: gradient/image bg, badge cam "⚡ FLASH SALE" (top-left), badge đỏ discount label (top-right), tên promo + countdown expiry + copy-code chip (bottom). Empty state ẩn section hoàn toàn (`SizedBox.shrink()`). (2) **Popular Lockers section** (session trước): `_buildPopularLockersSection` horizontal scroll, xoá `_buildWelcomeCard`/`_buildStoreSection`. (3) **Fix chip "Ưu đãi" dead API**: `VoucherRepository.getMyVouchers()` gọi `/promotions/vouchers/my` → endpoint KHÔNG TỒN TẠI → 404 mỗi lần bấm. Fix: chip redirect sang `/promotions` (page mới dùng `GET /api/promotions/active` thật). (4) **Tạo Promotions feature** (mobile): `PromotionModel` (discountLabel/expiryLabel/shortDiscountLabel), `PromotionRepository` (`GET /api/promotions/active`), `PromotionProvider` (Riverpod legacy `ChangeNotifierProvider`), `PromotionsPage` (`ConsumerStatefulWidget`, full list promotions, copy-code, countdown, image/gradient). (5) **AppRouter**: thêm `AppRouter.promotions = '/promotions'` + `GoRoute`. (6) **Backend V4 migration**: thêm `image_url` + `description` nullable vào `order_schema.promotions` (admin có thể gán hình cho ưu đãi); update `Promotion.java` entity. (7) **initState + _onRefresh**: thêm `promotionNotifierProvider.load()` song song `lockerNotifierProvider.getLocations()`. | Mobile: `lib/features/promotions/data/models/promotion_model.dart` (mới), `lib/features/promotions/data/repositories/promotion_repository.dart` (mới), `lib/features/promotions/presentation/providers/promotion_provider.dart` (mới), `lib/features/promotions/presentation/pages/promotions_page.dart` (mới), `lib/features/home/presentation/pages/home_page.dart` (thay section + imports + chip), `lib/core/routing/app_router.dart` (+const +route +import). BE: `order-service/.../model/Promotion.java` (+imageUrl, +description), `order-service/.../resources/db/migration/V4__promotions_image_description.sql` (mới). | `flutter analyze lib/features/promotions lib/features/home/presentation/pages/home_page.dart lib/core/routing/app_router.dart` → **No issues found** (0 error, 0 warning). Backend: migration V4 idempotent (`IF NOT EXISTS`), không chạy Flyway offline (chạy tự động khi deploy). |
| 2026-06-18 | Claude | **Implement VNPay Wallet Topup flow + fix mobile path.** Điều tra: mobile gọi `POST /payments/topup/create` → 404 vì (1) api-gateway chỉ route `/api/payments/**`, không có `/payments/**`; (2) payment-service không có endpoint topup; (3) toàn bộ backend không có wallet/balance service. **Sửa backend (payment-service)**: tạo `CreateTopupRequest.java` (record: `amount @Min(1000)`, `returnUrl`, `bankCode`, `locale`) và `TopupResponse.java` (record: `paymentUrl`, `txnRef`); thêm `findByReferenceId()` vào `PaymentRepository` (thay `findAll().stream()` non-indexed trong `handleVnPayReturn`); thêm `createTopupUrl(userId, request)` vào `PaymentService` — generate `txnRef="TOPUP_{userId}_{timestamp}"`, build VNPay URL, lưu `PaymentRecord` với `orderId=0L` (sentinel, NOT NULL được giữ) và `method="VNPAY_TOPUP"`; thêm 2 endpoint vào `PaymentController`: `POST /api/payments/topup/create` (auth required, `X-User-Id` từ JWT gateway) và `GET /payments/vnpay/callback` (public alias cho mobile WebView detect). **Sửa api-gateway**: thêm `/payments/vnpay/callback` vào route predicates payment-service và vào `isPublic()` của `JwtGatewayFilter`. **Flyway**: `V2__allow_topup_payments.sql` (index `idx_payments_reference_id` trên `reference_id`). **Sửa mobile**: path `_path` trong `top_up_remote_data_source_impl.dart` từ `/payments/topup/create` → `/api/payments/topup/create`; thêm catch blocks riêng cho `NotFoundException/ServerException/NetworkException/ValidationException/AuthenticationException` (thay `UnknownFailure(e.toString())` generic) trong `top_up_repository_impl.dart`. **Lưu ý thiết kế**: `orderId=0L` là sentinel cho topup (không có order thật), `method="VNPAY_TOPUP"` để phân biệt; userId lấy từ `X-User-Id` header inject bởi `JwtGatewayFilter`; mobile WebView detect `url.contains('/payments/vnpay/callback')` để đóng và báo thành công. **Chưa làm**: wallet service để cộng balance thật sau topup (xem P1 ở trên + BUSINESS_FLOWS_CURRENT.md mục 26). | BE: `payment-service/.../dto/CreateTopupRequest.java` (mới), `TopupResponse.java` (mới), `repository/PaymentRepository.java` (+findByReferenceId), `service/PaymentService.java` (+createTopupUrl, fix handleVnPayReturn), `controller/PaymentController.java` (+2 endpoint), `resources/db/migration/V2__allow_topup_payments.sql` (mới); `api-gateway/.../resources/application.yml` (+route predicate), `java/.../JwtGatewayFilter.java` (+isPublic). Mobile: `lib/features/transactions/infrastructure/data_sources/top_up_remote_data_source_impl.dart` (path fix), `lib/features/transactions/infrastructure/repositories/top_up_repository_impl.dart` (exception handling). | Mobile: hết 404 "NotFoundExcepion: Not Found" khi bấm "Nạp ngay" (path đã đúng, endpoint đã có). Chưa E2E test với VNPay sandbox credential thật; chưa có wallet/balance service (số dư user không thay đổi sau topup); chưa tạo branch/commit; chưa deploy. |
| 2026-06-16 | Claude | **Làm lại UI `MaintenanceHomePage` (mobile, visual only)**. Lý do: đây là màn hình duy nhất chưa được đưa vào lần revamp design system trước (vẫn AppBar `#7F1D1D` cứng + `Card`/`Container` tự vẽ tay, không dùng `OpsCard`/`BrandHeroHeader`). Đọc toàn bộ 1771 dòng để lập bản đồ đầy đủ trước khi sửa (4 tab, state, mọi dialog/bottom sheet) — đảm bảo giữ nguyên 100% chức năng, chỉ đổi widget render. Thay đổi: (1) Header — bỏ AppBar màu mận đỏ, dùng `BrandHeroHeader` + `TabBar` riêng bên dưới (vì `BrandHeroHeader` chỉ có 1 slot `trailing` nên gộp refresh+logout vào 1 `Row`). (2) Tab Kiểm tra tủ — đổi `DropdownButtonFormField` sang `LockerPickerField` (dùng lại từ Gửi hàng/Thuê tủ), card thông tin tủ + lưới ô bọc `OpsCard`. (3) Tab Sự cố/Việc của tôi — `OpsSectionLabel`/`OpsCard`/`OpsEmptyState` thay cho `Text`/`Card` rời. (4) Tab Định kỳ — tương tự. (5) Cell-action bottom sheet — đổi `ListTile` cục bộ sang `OpsSheetAction` (widget mới, promote từ `_SheetAction` private trong `my_locker_orders_page.dart` lên `ops_widgets.dart`, thêm tham số `color` để giữ được 5 màu semantic khác nhau cho 5 loại hành động — nếu chỉ dùng `primary/danger` có sẵn sẽ mất phân biệt màu). (6) 3 dialog (force-open/report-fault/out-of-service) đồng bộ bo góc 20. (7) `_RepairLogSheet` đổi màu nền log/input sang `opsSurface`/`opsBorder`. **Không đổi tên method/state/API call/4-tab nào.** | Mobile branch `feat/maintenance-ui-redesign`: `lib/features/locker_ops/presentation/pages/maintenance_home_page.dart`, `lib/features/locker_ops/presentation/widgets/ops_widgets.dart` (+`OpsSheetAction`), `lib/features/locker_ops/presentation/pages/my_locker_orders_page.dart` (đổi tên dùng). | `flutter analyze --no-pub` 3 file PASS (0 issue). Chưa chạy emulator/thiết bị thật phiên này (không có sẵn + cần đăng nhập tương tác) — chỉ verify tĩnh. |
| 2026-06-16 | Claude | **Đóng backlog P0/P1/P2 từ audit "IoT↔User↔Maintenance còn thiếu/chưa đồng bộ"** (xem mục audit trước đó trong phiên). Phát hiện đáng chú ý: web admin có 2 UI "Mở tủ khẩn cấp" làm sẵn nhưng cả 2 không hoạt động thật (1 giả lập toast, 1 gọi route chết `/api/admin/lockers/boxes/{id}/force-open`); và action lifecycle ô (out-of-service/cleaning/return-to-service) **đã có sẵn** ở `Admin/lockers/layout-view.tsx` từ trước — audit ban đầu nhầm là thiếu vì chỉ xem `Admin/maintenance/index.tsx`. **(1) Force-open + audit log**: `iot-service` thêm bảng `box_access_logs`/`access_attempts` (V2), endpoint internal `/internal/iot/force-unlock`; `locker-service` thêm Feign `IotClient`, endpoint `/api/maintenance/boxes/{id}/force-open`; nối cả 2 UI web (sửa `BoxSettingModal.tsx`, thêm nút ở `layout-view.tsx`) + mobile (bottom-sheet hành động ô). Mọi lần mở (PIN/QR khách lẫn MASTER override) đều ghi audit log. **(2) PIN/QR lockout**: khóa box 15 phút sau 5 lần verify-access sai (`app.iot.lockout.*`). **(3) TTL backstop cho ô RESERVED**: cột `reserved_until` (V8, 24h, cùng cửa sổ auto-cancel order-service), job giờ `LockerScheduler` chỉ là lưới an toàn. **(4) Contact info + rating 2 chiều**: `LockerReportResponse` thêm `reporterName/reporterPhone` (tra `user-service` qua Feign mới, best-effort); khách đánh giá report sau khi RESOLVED (`locker_report_ratings` V9), maintenance xem điểm trung bình. **(5) Enforce size**: fallback sang size lớn hơn khi hết đúng size; mobile SEND thêm chọn kích thước. **(6) Device health dashboard**: `GET /api/manage/iot/device-status` (route gateway mới `/api/manage/iot/**`, RBAC MANAGER/ADMIN có sẵn) + section trong `Admin/maintenance`. **(7) Web admin "Lịch bảo trì định kỳ"**: tạo/đã-kiểm-tra/xóa — trước đó chỉ mobile có. Theo quyết định user: **bỏ qua** sync trạng thái ô→MQTT (chờ hardware) và payment gating (giữ miễn phí demo); không đụng `smart-locker-iot`. | BE branch `feat/maintenance-iot-sync-hardening`: `iot-service/.../{model,repository,dto,service,controller}/*`, `iot-service/.../V2__access_audit_and_lockout.sql`, `locker-service/.../{client,model,repository,dto,service,controller}/*`, `locker-service/.../V8__reserved_ttl.sql`, `V9__report_ratings.sql`, `api-gateway/.../application.yml`. Mobile branch cùng tên: `locker_ops_service.dart`, `maintenance_home_page.dart`, `my_reports_page.dart`, `send_parcel_page.dart`. Web FE branch cùng tên: `lockerOps.ts`, `layout-view.tsx`, `BoxSettingModal.tsx`, `Admin/maintenance/index.tsx`. | `mvn -B test` toàn reactor PASS (exit 0); `flutter analyze` 4 file PASS (0 issue); web FE `npm.cmd install` + `npm.cmd run build` PASS (0 lỗi TypeScript). Chưa chạy smoke runtime đầy đủ (docker-compose + emulator + browser thật) phiên này — xem ghi chú trong "Việc Còn Lại". |
| 2026-06-16 | Claude | **Email OTP trên prod: bypass chặn SMTP của DigitalOcean + tách địa chỉ From.** Tiếp theo dòng dưới: log droplet cho thấy (1) Gmail `smtp.gmail.com:587` → `MailConnectException timeout` vì **DigitalOcean chặn outbound 25/465/587** (chống spam, ở tầng nền tảng). Giải pháp: relay hỗ trợ **port 2525** (Brevo/SendGrid/Mailgun — DO không chặn 2525); đổi `SPRING_MAIL_HOST=smtp-relay.brevo.com`, `PORT=2525` thì kết nối thông (log chuyển "timeout"→"Authentication failed"). (2) Brevo bắt **From = sender đã verify** (≠ SMTP login) → sửa `SmtpEmailService` cho From cấu hình riêng `app.mail.from` (fallback SMTP username) + passthrough `APP_MAIL_FROM` trong compose. Runtime còn phụ thuộc user: kích hoạt tài khoản Brevo + đúng SMTP key (sửa "Authentication failed") + redeploy để có code From + đặt `APP_MAIL_FROM=<sender verify>`. | branch `fix/droplet-gateway-host-port-8080`: `auth-service/.../email/SmtpEmailService.java`, `docker-compose.yml`; living docs + mirror. | `mvn -pl auth-service -am compile` **PASS**; `docker compose config -q` **PASS**. Đã xác nhận trên droplet: 2525 vượt chặn DO (hết timeout). OTP luôn có trong log để demo. |
| 2026-06-16 | Claude | **Fix OTP admin-2FA không tới email.** Nguyên nhân: `docker-compose.yml` hardcode `SPRING_MAIL_HOST=localhost:1025` (mail-catcher dev) — droplet không có mail server ở đó nên `SmtpEmailService.sendSimpleEmail` ném exception → bị catch + warn-log → email không gửi (OTP chỉ ghi ra log auth-service `Development OTP: <code>`). Sửa: cho mail env **overridable từ `.env`/env droplet** (`${SPRING_MAIL_HOST:-localhost}`...) + thêm passthrough `SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH`/`STARTTLS_ENABLE` (mặc định false giữ hành vi cũ). Để gửi thật: đặt env Gmail (`smtp.gmail.com:587` + app password + auth/starttls=true) trong `.env` droplet rồi recreate auth-service. Không commit secret. | branch `fix/droplet-gateway-host-port-8080`: `docker-compose.yml` (auth-service mail env); living docs + mirror. | `docker compose config -q` **PASS** (syntax OK). Immediate unblock: lấy OTP từ `docker compose logs auth-service \| grep "Development OTP"`. Gửi email thật phụ thuộc user đặt SMTP creds trên droplet (agent không giữ secret). |
| 2026-06-16 | Claude | **Fix login/register 500 "Unexpected server error" + chống tái diễn Flyway.** Điều tra: login sai mật khẩu → 400 (auth OK), đúng mật khẩu → 500; register → 500 ⇒ chết ở Feign call sang **user-service** ([AuthService.login:82](../auth-service/src/main/java/com/huynqb/laundrylocker/auth/service/AuthService.java)). Log droplet: **user-service crash-loop vì Flyway "checksum mismatch migration version 1"** — `V1__create_user_tables.sql` đã bị sửa (commit `a6b5eb3`: đổi DEFAULT `roles` `USER`→`CUSTOMER` + seed bảng `roles`) sau khi đã apply lên DB droplet → Flyway validate fail → user-service không boot → auth-service gọi sang fail → 500. (Diff vô hại runtime: app luôn set roles tường minh; bảng `roles` bị drop ở V3.) **Fix tức thời (user chạy trên droplet):** `UPDATE flyway_schema_history SET checksum=1261895981 WHERE version='1'` trên `user_db` + recreate user-service. **Fix bền vững (code):** thêm shared `FlywayMigrationStrategy` (`repair()`→`migrate()`) ở common-lib (mọi service scan `...common`), bật mặc định, tắt bằng `app.flyway.repair-on-migrate=false` ⇒ migration cũ bị sửa sẽ tự repair thay vì crash-loop cả service lúc deploy. | branch `fix/droplet-gateway-host-port-8080`: `common-lib/pom.xml` (+flyway-core optional), `common-lib/.../common/config/FlywayRepairConfig.java` (mới); living docs + mirror. Không sửa migration cũ. | Cô lập lỗi bằng curl: sai-pass 400 / đúng-pass 500 / register 500 (PASS, chứng minh user-service). `mvn -pl common-lib -am compile` + `mvn -pl user-service -am compile` **PASS** (exit 0). **ĐÃ FIX + VERIFIED (user chạy repair trên droplet):** `UPDATE user_schema.flyway_schema_history SET checksum=1261895981 WHERE version='1'` (lưu ý bảng nằm trong `user_schema`, không phải `public`) → recreate user-service → boot OK. Probe ngoài: login/register/profile/notifications/admin-login **đều 200**. |
| 2026-06-16 | Claude | **Fix mobile "Lỗi server" (locker list không load).** Đọc logcat trên emulator: APK đang chạy là **bản cũ**, gọi `https://api-dev.aisl.io.vn/api/lockers` (domain chết) → `badResponse` → "Lỗi server". `aisl.io.vn` không còn trong source (env_config.g.dart + `.env` đều đã = server) ⇒ chỉ cần **rebuild**. Nhưng base URL mới là **http://** → Android API 28+ chặn cleartext (app không có network-security-config). Sửa: thêm `res/xml/network_security_config.xml` cho phép cleartext CHỈ tới `146.190.84.136` + host local (10.0.2.2/localhost/127.0.0.1), khai báo `networkSecurityConfig` + `INTERNET` trong manifest, rồi `flutter build apk --debug` + cài lại. | mobile branch `feat/point-clients-to-deployed-server`: `android/app/src/main/AndroidManifest.xml`, `android/app/src/main/res/xml/network_security_config.xml` (mới). | `flutter build apk --debug` **PASS** (104s). Cài lên emulator-5554: app gọi đúng `http://146.190.84.136:8080/api/lockers` (cleartext OK, không CLEARTEXT error), màn "Danh sách tủ" hiển thị tủ thật (TD demo FPT, TK AISL Quận 1/Bình Thạnh, TG Tủ Giặt 1). `/api/lockers`+`/api/stores` 200. Còn 1 call `/api/user/profile` dính **503 nhất thời** lúc user-service vừa restart trên droplet — probe lại ngoài đã 401 (service up, chỉ cần token) ⇒ blip server, không phải lỗi mobile. |
| 2026-06-16 | Claude | **Fix bền vững: gateway luôn publish host port 8080 khi deploy droplet.** Root cause "mobile không kết nối được": `docker compose ps` trên droplet cho thấy mọi service Up nhưng api-gateway map `0.0.0.0:18080->8080` (host 18080) trong khi firewall chỉ mở 8080 → `:8080` ngoài "connection refused". (Không phải OOM: RAM 7.8Gi/8GB, đã thêm swap 4GB.) Sửa: `deploy-from-artifact.sh` `export API_GATEWAY_PORT="${API_GATEWAY_PORT:-8080}"` để mỗi lần deploy gateway luôn ở host 8080 (env set tay trong `.env` bị deploy ghi đè); local giữ default 18080 vì dev chạy `docker compose` trực tiếp. | branch `fix/droplet-gateway-host-port-8080` (kế thừa `docs/point-clients-to-deployed-server`): `scripts/deploy-from-artifact.sh`; living docs + mirror. | `bash -n scripts/deploy-from-artifact.sh` **PASS** (syntax OK). Fix tức thời trên droplet (user chạy): thêm `API_GATEWAY_PORT=8080` vào `.env` + `docker compose up -d api-gateway` → `0.0.0.0:8080->8080`. Áp bền vững khi merge `develop` (tự redeploy). |
| 2026-06-16 | Claude | **Trỏ FE + mobile + DB về backend đã deploy trên DigitalOcean** (`http://146.190.84.136:8080`) và chốt chính sách cả nhóm dùng chung server, không cần docker local. (1) **FE**: `fe/.env` `VITE_API_BASE_URL`, default `api-paths.ts` (`http://localhost:8080`→server), và proxy `vite.config.ts` (`localhost:18080`→server). `fe/.env` được track nên áp cho cả nhóm. (2) **Mobile**: phát hiện `env_config.g.dart` (track) + `defaultValue` đã = server từ trước ⇒ chỉ cập nhật `.env.example` cho member copy; mobile `.env` private giữ nguyên không commit. (3) **DB**: client đi qua gateway server ⇒ trỏ FE/mobile về server là tự dùng DB server; nối DB trực tiếp cần mở port `15432` firewall (chưa mở). (4) **Auto-deploy**: xác nhận `deploy-droplet.yml` (push `develop`) đã tự build+ship+`docker compose up -d --build`, Flyway migrate DB lúc khởi động ⇒ "merge nhánh chính → tự deploy code+DB" có sẵn. Phát hiện gateway droplet `:8080` đang không listen (stack chưa chạy) — cần bật lại trên droplet. | FE branch `feat/point-clients-to-deployed-server`: `fe/.env`, `fe/vite.config.ts`, `fe/src/constants/api-paths.ts`. Mobile branch `feat/point-clients-to-deployed-server`: `.env.example`. BE branch `docs/point-clients-to-deployed-server`: living docs + mirror. | FE `npm run build` **PASS** (0 lỗi type, ~25s). Server probe: SSH `:22` OK, gateway `:8080` "connection refused" (**PARTIAL/FAIL** — stack droplet chưa chạy, cần bật lại). Không sửa code Dart/Java/migration nên không chạy flutter analyze/mvn. |
| 2026-06-16 | Claude | **Đồng bộ luồng booking tủ: IoT↔mobile mô phỏng + đặt lại đơn + customer↔maintenance qua lại.** (1) **Mô phỏng IoT**: phát hiện `main.py` không bao giờ trả lời lệnh mở vì chờ handshake `iot/{mac}/command/setup` mà Java không gửi, và payload Java gửi (`box_id`) khác field Python đọc (`lockerId`/`slotIndex`) — viết script độc lập mới `simulate_demo_cabinet.py` (không đụng `main.py`/hardware-track) trả lời đúng contract Java thật đang dùng; mobile thêm action "Mở tủ" (gọi `/api/iot/unlock`) trong chi tiết đơn. (2) **Đặt lại đơn**: phát hiện `OrderService.reorder()` gọi `create()` generic với `sendBoxId=null` và không có `cellType`/`hours` → đơn SEND reorder không có ô, đơn RENTAL sai giá/thiếu hạn thuê — sửa để branch theo type và gọi lại `createSend()`/`createRental()`; mobile thêm action "Đặt lại đơn". (3) **Customer↔Maintenance qua lại**: thêm event `locker.report.claimed`/`locker.report.resolved` (publish từ `LockerService.claimReport`/`resolveReportAndClearFault`, kèm binding RabbitMQ mới vì exchange dùng binding theo từng routing key, không phải wildcard); generalize `NotificationService.consumeDomainEvent` đọc `referenceId`/`referenceType`/`message` từ payload (fallback hành vi cũ); mobile thêm màn mới "Báo cáo của tôi" (`/locker/my-reports`) — không sửa/xoá `report_list_page.dart`/`create_report_page.dart` legacy (kiến trúc khác, gọi API chết) theo đúng tiền lệ repoint tab Đơn hàng trước đây. | BE branch `feat/booking-iot-reorder-maintenance-loop`: `order-service/.../OrderService.java`, `common-lib/.../DomainEventNames.java`, `locker-service/.../LockerService.java`, `notification-service/.../{NotificationService,config/RabbitConfig}.java`. Mobile branch cùng tên: `locker_ops_service.dart`, `my_locker_orders_page.dart`, `my_reports_page.dart` (mới), `app_router.dart`. IoT branch `feat/demo-cabinet-simulator` (repo riêng): `simulate_demo_cabinet.py` (mới), `README.md`. | `mvn -pl common-lib,order-service,locker-service,notification-service -am test` PASS (exit 0, gồm Testcontainers Postgres thật); `flutter analyze --no-pub` 4 file PASS (0 issue); MQTT round-trip thật qua `broker.hivemq.com` PASS (publish payload Java thật → nhận đúng `status:SUCCESS` trong ~1.5s). **Chưa chạy**: docker-compose full stack + emulator click-through + xác nhận push notification thật (máy có Docker RAM hạn chế ~6.8GB, không đủ an toàn cho 12+ JVM đồng thời trong phiên này). |
| 2026-06-15 | Claude | **Seed demo đầy đủ cho toàn bộ MS DB** (`scripts/seed-full-demo-ms.sql`, idempotent). Tạo 4 tài khoản đặt sẵn (pw `12345678`, hash bcrypt tái dùng từ `seed-test-user.sql`): ADMIN `baohuy2k12k4@gmail.com` (uid 9001), CUSTOMER `nqbhuy2004nt@gmail.com` (9002), MAINTENANCE `se180211nguyenquocbaohuy@gmail.com` (9003), MANAGER `huynqbse180211@fpt.edu.vn` (9004) + 100 khách bulk. Seed ~100+ bản ghi/bảng cho 22 bảng nghiệp vụ (stores/lockers/boxes/orders/details/history/ratings/complaints/promotions/payments/refunds/notifications/fcm/loyalty/point_tx/reports/schedules/repair_logs/device_status), tham chiếu chéo DB nhất quán bằng dải id riêng (≥9001 / ≥90001) + marker `*-DEMO-*`. Bỏ qua `email_otps`/`refresh_tokens` (runtime). | branch `chore/full-demo-seed`: `scripts/seed-full-demo-ms.sql`; living docs + mirror. | Local PASS ×2 (idempotent); 4 account login thật qua gateway OK đúng role. **Droplet chưa áp** (harness chặn ghi prod DB) — user chạy lệnh psql trong báo cáo. |
| 2026-06-15 | Claude | **Khắc phục lỗi/cảnh báo + build chậm khi `flutter run` mobile.** (1) **401 → auto-logout**: token access hết hạn (chỉ sống 24h) làm app auto-login bằng token cũ bị 401 rồi `AuthInterceptor` logout thẳng. Thêm **refresh-on-401**: gọi `POST /api/auth/refresh-token` bằng Dio sạch (không gắn token cũ), serialize 1 lần/đợt (backend xoay vòng refresh token), retry request gốc với token mới, chỉ logout khi refresh fail/không có refresh token. (2) **Firebase `duplicate-app`**: `main.dart` gọi `Firebase.initializeApp` trong khi đã auto-init native → throw → catch nuốt lỗi → `FirebaseMessagingService.init()` bị bỏ qua. Guard `Firebase.apps.isEmpty` để messaging vẫn init. (3) **Build chậm/giật**: Gradle heap `-Xmx8G` quá lớn cho máy ~16GB chạy kèm Docker (12 JVM) + emulator → swap; giảm `-Xmx3G` + `MaxMetaspaceSize=1G` + bật `parallel`/`caching`. KGP warning là forward-compat (chưa breaking) — không migrate vội (phụ thuộc plugin bên thứ ba). | branch `fix/mobile-token-refresh-and-startup` (repo `smart-laundry-locker-mobile`): `lib/core/network/auth_interceptor.dart`, `lib/main.dart`, `android/gradle.properties`; living docs + mirror (repo backend). | `flutter analyze --no-pub` 2 file Dart **PASS** (0 error; `auth_interceptor` 0 issue, `main.dart` chỉ info-level cũ). Chưa chạy lại `flutter run` E2E (cần thiết bị/emulator của user). |
| 2026-06-15 | TruongNguyenThaiBinh77 | **UI revamp tối 2026-06-15 (branch `refactor/unify-home-and-locker-data-flow`)**: (1) **Bottom nav bar**: frosted-glass pill không label, QR icon ngang bằng icon khác. (2) **Filter chips MyLockerOrdersPage**: nền trong suốt. (3) **Home page**: revert layout gốc + thêm wallet card (navy→blue gradient, balance, nút nạp tiền). (4) **ProfilePage**: cấu trúc layout đổi từ `CustomScrollView + SliverAppBar` sang `Column → [BrandHeroHeader, Expanded(SingleChildScrollView)]` giống LockerPage; header dùng `softHeaderGradient + navy text + BrandCircleIconButton`; loading state cũng dùng `BrandHeroHeader`; gỡ 2 import thừa. (5) **MyLockerOrdersPage**: header đổi từ white Container "Hoạt động" sang `BrandHeroHeader(title='Đơn tủ', subtitle=..., trailing=refresh)`. (6) **Navigation bug fix**: "Đơn tủ" quick action `LockerUtilitiesRow` đổi từ `context.push(AppRouter.myLockerOrders)` (route ngoài ShellRoute → mất nav bar + không có nút quay lại) sang `context.go(AppRouter.orders)` (tab trong ShellRoute → nav bar visible, hành vi tab chuẩn). | `smart-laundry-locker-mobile`: `lib/shared/widgets/custom_bottom_navigation_bar.dart`, `lib/features/home/presentation/pages/home_page.dart`, `lib/features/profile/presentation/pages/profile_page.dart`, `lib/features/locker_ops/presentation/pages/my_locker_orders_page.dart`, `lib/features/locker_ops/presentation/widgets/location_services.dart`. | `flutter analyze` 3 file đã sửa: **3 issue info/warning pre-existing** (lucide unnecessary_import + deprecated `activeColor` + `withOpacity` — không liên quan thay đổi), 0 error mới. |
| 2026-06-15 | Claude | **Fix crash trang `/admin/dashboard`** (`Cannot read properties of undefined (reading 'toString')` tại `dash-board.tsx:60`). **Nguyên nhân gốc:** lệch contract — Dashboard + OverviewSection đọc 11 field (`totalOrders, ordersToday, pendingOrders, totalRevenue, revenueToday, totalUsers, totalStores, totalLockers, activeServices, availableBoxes, occupiedBoxes`) nhưng `OrderService.statistics()` (endpoint `/api/admin/dashboard/overview`) chỉ trả `totalOrders`+`byStatus` → các field còn lại `undefined` → `.toString()`/`.toLocaleString()` nổ. **Sửa:** (1) **FE** `useDashboard` normalize `overview` về object đủ 11 field default `0` (không crash dù backend trả thiếu/partial/lỗi). (2) **BE** `order-service` bổ sung các metric tự tính từ dữ liệu order (`ordersToday`, `pendingOrders`, `totalRevenue`, `revenueToday`) — không gọi chéo service. KPI cross-service (users/stores/lockers/boxes/services) tạm hiển thị 0 (chưa wire aggregation). | FE branch `fix/admin-login-2fa-response-normalization`: `fe/src/pages/Admin/dashboard/hooks/useDashboard.ts`; BE branch `fix/gateway-cors-globalcors-prefix`: `order-service/.../service/OrderService.java`; living docs + mirror. | FE `npm.cmd run build` **PASS**; BE `mvn -pl order-service -am package` **SUCCESS**; rebuild `ll-ms-order-service` UP; endpoint trả `{totalOrders:11, ordersToday:0, pendingOrders:2, totalRevenue:230000.00, revenueToday:0, byStatus}`. |
| 2026-06-15 | Claude | **Fix crash sau khi nhập OTP ở web admin login** (`Cannot read properties of undefined (reading 'id')`). Sau khi CORS được sửa, request `verify-2fa` đi qua nhưng FE crash khi chuẩn hoá response. **Nguyên nhân gốc:** backend `/api/admin/auth/verify-2fa` (qua `AuthService.authMap`) trả payload **phẳng** `{accountId,userId,accessToken,refreshToken,tokenType,expiresAt,roles,name,...}`, **không có** key `user` cũng không có `id`; nhưng FE `adminLoginStep2` đọc `const raw = data.user; raw.id` → `data.user` `undefined` → TypeError. **Sửa (FE):** chuẩn hoá đọc các field phẳng (`id ← userId/accountId`, `role ← roles`, `fullName ← name`), giữ email người dùng nhập ở bước 1 để điền `User.email` (response không echo email), vẫn tương thích nếu sau này backend đổi sang dạng `user` lồng. Không đổi backend. | branch `fix/admin-login-2fa-response-normalization` (repo `laundry-locker-frontend`): `fe/src/context/auth-context.tsx`; living docs + mirror (repo backend). | `npm.cmd run build` (`tsc -b && vite build`) **PASS** 0 lỗi type; trace luồng tới `/admin/dashboard`. Chưa E2E browser (cần password admin của user). |
| 2026-06-15 | Claude | **Fix web admin không đăng nhập được (CORS preflight bị chặn)**. Triệu chứng: web `http://localhost:3000` gọi `POST /api/admin/auth/login` qua gateway `:18080` báo "Failed to fetch" + console `No 'Access-Control-Allow-Origin' header`. **Nguyên nhân gốc:** khối `globalcors` trong gateway nằm ở prefix cũ `spring.cloud.gateway.globalcors` (đã **deprecated từ Spring Cloud Gateway 4.3.0**, không còn bind vào `GlobalCorsProperties`) trong khi `routes`/`discovery` đã ở `spring.cloud.gateway.server.webflux.*` — nên cấu hình CORS bị bỏ qua hoàn toàn, gateway không trả header CORS. Mobile/curl không bị vì không enforce CORS. **Sửa:** chuyển `globalcors` về đúng `spring.cloud.gateway.server.webflux.globalcors`, thêm method `PATCH`. | branch `fix/gateway-cors-globalcors-prefix`: `api-gateway/src/main/resources/application.yml`; living docs + mirror. | `mvn -pl api-gateway -am package -DskipTests` SUCCESS; rebuild image + recreate `ll-ms-api-gateway`; curl preflight **403→200** kèm đầy đủ header CORS; POST thật trả ACAO + `AUTH_INVALID` (sai mật khẩu cố ý). |
| 2026-06-15 | Claude | **PA3 đợt 2 — drop `audit_logs` + bỏ hẳn `staff-service`** (theo yêu cầu). (1) **audit_logs**: migration `user-service/V4` DROP TABLE; xóa entity `AuditLog` + `AuditLogRepository`; gỡ 4 endpoint `/api/admin/audit-logs*` + field khỏi `UserController`; gỡ seed + verify. (2) **staff-service** (gỡ toàn bộ): bỏ module khỏi `pom.xml`, block khỏi `docker-compose.yml`, 3 mục route/docs/health khỏi `api-gateway`, `staff_db` khỏi `init-databases.sql`, xóa thư mục `staff-service/`; thêm `staff_db` vào script ops drop. Không service nào phụ thuộc (đã verify), fe/mobile không gọi `/api/staff`. Giải phóng 1 JVM trên droplet. | branch `chore/drop-legacy-tables`: `user-service/.../V4__drop_audit_logs.sql` + `UserController.java` (−AuditLog/Repo), `pom.xml`, `docker-compose.yml`, `api-gateway/.../application.yml`, `docker/postgres/{init-databases,seed-demo-data,verify-demo-data}.sql`, `scripts/drop-legacy-databases.sql`, xóa `staff-service/`; living docs + mirror. | `mvn -pl user-service -am compile` **SUCCESS**; `mvn -o validate` reactor OK (pom không còn staff); `docker compose config` hợp lệ. Drop bảng/DB áp khi redeploy + chạy script ops. |
| 2026-06-15 | Claude | **Tối ưu DB — thêm index cho truy vấn nóng** (chỉ thêm index, không đổi dữ liệu; `CREATE INDEX IF NOT EXISTS`). Đối chiếu repository để bù các cột bị query/sort mà chưa có index: `orders(pin_code)` (tra cứu mở tủ — nóng nhất, trước full-scan), `orders(user_id, created_at DESC)` (đơn của tôi), `order_complaints(user_id)`, `order_ratings(user_id)`, `notifications(user_id, is_read, created_at DESC)` (đếm/đọc thông báo chưa đọc — mỗi lần mở app), `locker_boxes(status)` (list ô lỗi cho bảo trì). Các lookup auth/loyalty/fcm/device đã được UNIQUE-constraint phủ nên bỏ qua. | branch `chore/drop-legacy-tables` (stack cùng PA3): `order-service/.../V3__performance_indexes.sql`, `notification-service/.../V2__unread_index.sql`, `locker-service/.../V7__box_status_index.sql`. | Đối chiếu tên cột với `CREATE TABLE` (khớp); migration additive `IF NOT EXISTS`. Áp khi redeploy (Flyway). |
| 2026-06-15 | Claude | **PA3 — dọn bảng/DB thừa (audit toàn backend: 28 bảng/10 schema)**. (1) Drop 3 bảng RBAC orphan `roles/permissions/role_permissions` (không entity/repo; role dùng cột `user_profiles.roles` VARCHAR + JWT) — migration `user-service/V3`; gỡ seed + verify tương ứng. (2) Gỡ cột `stores.partner_id` (di sản PARTNER) — migration `store-service/V2` + entity/DTO/service. (3) Gỡ DB rỗng `partner_db` + `laundry_db` khỏi `init-databases.sql` + script ops `scripts/drop-legacy-databases.sql` để drop trên DB đã chạy. (4) Đánh dấu **deprecated** (giữ tạm) `audit_logs` (không có writer runtime) + `staff_assignments`/staff-service (không flow nào gọi). | branch `chore/drop-legacy-tables`: `user-service/.../V3__drop_legacy_rbac_tables.sql`, `store-service/.../V2__drop_partner_id.sql` + `StoreLocation/StoreRequest/StoreResponse/StoreService`, `docker/postgres/{init-databases,seed-demo-data,verify-demo-data}.sql`, `scripts/drop-legacy-databases.sql`, living docs + mirror. | `mvn -pl store-service,user-service -am compile` **BUILD SUCCESS**. Migration/drop chưa chạy trên DB thật (cần redeploy + chạy script ops). |
| 2026-06-15 | Claude | **L5 (slice 4, KHÉP L5) — Bảo trì phòng ngừa (lịch định kỳ)**, BE + mobile + admin. BE: migration **V6** `maintenance_schedules` + entity/repo/DTO + `createSchedule/listSchedules/completeSchedule/deleteSchedule` (cờ `due`=now≥next_due_at); endpoint `GET /api/maintenance/schedules`, `POST /api/maintenance/schedules/{id}/complete`, `POST/DELETE /api/admin/lockers/schedules[/{id}]`. Mobile: tab **'Định kỳ'** (nhóm Đến hạn/Sắp tới + nút 'Đã kiểm tra'). Admin: section **'Bảo trì định kỳ'** (tạo/liệt kê/đã-kiểm-tra/xóa). | BE `feat/locker-maintenance-ops` (`V6__maintenance_schedules.sql`, `MaintenanceSchedule*.java`, `LockerService.java`, `LockerController.java`); mobile `feat/locker-maintenance-ops` (`maintenance_home_page.dart`, `locker_ops_service.dart`); fe `feat/locker-cell-status-admin` (`lockerOps.ts`, `maintenance/MaintenanceSchedules.tsx`, `maintenance/index.tsx`); living docs + mirror. | `mvn compile` **SUCCESS**; `flutter analyze` **0 error**; `tsc -b` **0 error**. Chưa redeploy/smoke. **Khép L5** (4 slice: vòng đời ô · work-log · SLA · phòng ngừa). |
| 2026-06-15 | TruongNguyenThaiBinh77 | **UX revamp lưới ô tủ + DRONE cell type (mobile + backend)**. (1) **Mobile** (branch `feat/mobile-user-ui-revamp`): `StoreLockerGridPage` mới — lưới ô 2D lazy-load theo store (`GET /api/lockers?storeId=X` + `GET /api/lockers/{id}/layout`), màu theo status, ô DRONE hiển thị icon máy bay indigo + không tappable, legend cập nhật. Booking `RentLockerPage`/`SendParcelPage`: bỏ picker + skip API load khi `initialLockerId` có sẵn, hiện card read-only tủ + loại ô có icon khóa. Bridge `LockerLocation`→`Store` trong `locker_page.dart`. (2) **Backend** (`locker-service`): thêm `CellType.java` constants class với Javadoc đầy đủ (STANDARD/DRONE/XL + sơ đồ ASCII + business rules); cập nhật Javadoc `LockerBox.java` (default `CellType.STANDARD`) và `CellResponse.java` (mô tả đầy đủ hành vi mobile). Không có migration mới (V3 đã seed 3 DRONE). | Mobile: `store_lockers_page.dart` (mới), `locker_page.dart`, `rent_locker_page.dart`, `send_parcel_page.dart`, `locker_ops_service.dart`, `app_router.dart`, `home_page.dart`; BE: `CellType.java` (mới), `LockerBox.java`, `CellResponse.java`; docs: `merge-status.md`, `PROJECT_PROGRESS_TRACKER.md`. | `flutter analyze` **0 error** trên các file đã chỉnh; `mvn compile` PASS (không đổi logic Java). |
| 2026-06-15 | Claude | **L5 (slice 3) — SLA phiếu bảo trì**, BE + mobile + admin. BE: `LockerReportResponse` thêm `slaHours/slaDueAt/overdue` (tính trong `toReport` từ `createdAt + app.maintenance.sla-hours` mặc định 4h; `overdue` khi quá hạn & chưa RESOLVED) — không migration. Mobile: pill "Quá hạn SLA" đỏ + tô đỏ tuổi phiếu theo `overdue`. Admin: badge "Quá hạn SLA" + đếm số phiếu quá hạn ở tiêu đề. Cờ authoritative từ BE thay cho heuristic client. | BE `feat/locker-maintenance-ops` (`LockerReportResponse.java`, `LockerService.java`); mobile `feat/locker-maintenance-ops` (`maintenance_home_page.dart`); fe `feat/locker-cell-status-admin` (`lockerOps.ts`, `maintenance/index.tsx`); living docs + mirror. | `mvn compile` **SUCCESS**; `flutter analyze` **0 error**; `tsc -b` **0 error**. Chưa redeploy/smoke. |
| 2026-06-15 | Claude | **L5 (slice 2) — Nhật ký xử lý phiếu bảo trì (work-log)**, BE + Maintenance mobile + Admin web. BE: migration **V5** bảng `repair_logs` + entity/repo/DTO + `addRepairLog/repairLogs` + endpoint `GET/POST /api/maintenance/reports/{id}/logs`. Mobile: nút "Nhật ký" trên thẻ phiếu → bottom sheet xem + thêm bước xử lý. Admin: nút "Nhật ký" → Dialog xem + thêm ghi chú. | BE branch `feat/locker-maintenance-ops` (`V5__repair_logs.sql`, `RepairLog.java`, `RepairLogRepository.java`, `RepairLogResponse.java`, `LockerService.java`, `LockerController.java`); mobile branch `feat/locker-maintenance-ops` (`maintenance_home_page.dart`, `locker_ops_service.dart`); fe branch `feat/locker-cell-status-admin` (`lockerOps.ts`, `maintenance/RepairLogDialog.tsx`, `maintenance/index.tsx`); living docs + mirror. | `mvn -pl locker-service -am compile` **SUCCESS**; `flutter analyze` **0 error**; `tsc -b` **0 error**. Chưa redeploy/smoke. |
| 2026-06-15 | Claude | **L5 — Bảo trì vận hành (vòng đời ô)**, cuốn chiếu BE + Maintenance mobile + Admin web. (1) **Backend** (`locker-service`): thêm trạng thái ô `OUT_OF_SERVICE`/`CLEANING` + `setOutOfService/setCleaning/returnToService` (guard ô đang OCCUPIED/RESERVED; `releaseBox` không lật ô ngưng-dùng); 3 endpoint `/api/maintenance/boxes/{id}/{out-of-service\|cleaning\|return-to-service}`. Không cần migration (status là String, không CHECK). (2) **Maintenance mobile**: dialog ô → bottom sheet hành động theo trạng thái (Báo hỏng/Ngưng dùng/Vệ sinh/Khôi phục/Đã sửa) + 3 method service + màu/nhãn 2 trạng thái mới. (3) **Admin web** (`layout-view`): action theo từng ô + badge OUT_OF_SERVICE/CLEANING + 3 RTK mutation. | BE branch `feat/locker-maintenance-ops` (`LockerService.java`, `LockerController.java`); mobile branch `feat/locker-maintenance-ops` (`maintenance_home_page.dart`, `locker_ops_service.dart`, `ops_widgets.dart`); fe branch `feat/locker-cell-status-admin` (`lockerOps.ts`, `lockers/layout-view.tsx`); living docs + mirror. | `mvn -pl locker-service -am compile` **BUILD SUCCESS**; `flutter analyze lib/features/locker_ops` **0 error**; `tsc -b` (fe) **0 error**. Chưa redeploy/smoke runtime. |
| 2026-06-15 | AI | - Khắc phục lỗi `500/503` (Cold Start timeout) khi Đăng nhập trên mobile bằng cách tăng `timeout-duration` của Resilience4j TimeLimiter từ `1s` mặc định lên `10s` trong `auth-service/application.yml` và restart container để API Gateway quét lại.<br>- Xây dựng SQL seed script (`scripts/seed-test-user.sql`) chèn đầy đủ dữ liệu cho tài khoản test (`binhtntse182370@fpt.edu.vn`, mk `12345678`) đồng bộ xuyên 4 DB (`auth`, `user`, `order`, `loyalty`) chạy trực tiếp trên Postgres container.<br>- Sửa lỗi build/compile ở Flutter app (`home_page.dart` missing brackets, `isFavorite` parameter trong `StoreCard`). | `auth-service/application.yml`, `scripts/seed-test-user.sql`, `smart-laundry-locker-mobile/...` | PASS |
| 2026-06-14 | User/AI | Hoàn tất giao diện mobile cho role Maintenance: xem tủ, báo hỏng, mở lại ô. Commit và push vào develop. | `smart-laundry-locker-mobile/...` | PASS trên emulator |
| 2026-06-14 | Claude | Mobile batch 2: repoint tab Đơn hàng sang đơn tủ thật (locker_ops) và làm chi tiết tủ thân thiện khi thiếu toạ độ. **Đã push + merge vào `develop`**: rebase branch `fix/mobile-api-endpoints-alignment` lên `origin/develop` mới nhất (đã có PR #4 locker-ui-map; rebase không conflict), push branch, merge `--no-ff` vào develop (merge commit `23dbf76`) và push `origin/develop`. `env_config.g.dart` (18080 local) và `.env` vẫn KHÔNG commit/push. | `smart-laundry-locker-mobile` branch `fix/mobile-api-endpoints-alignment` (2 commit fix) → `develop`; `lib/core/routing/app_router.dart`, `lib/features/locker/infrastructure/data_sources/locker_remote_data_source_impl.dart`; living docs + mirror. | `flutter analyze` toàn project **0 error** (412 info debt cũ) sau rebase; on-device verify tab Đơn hàng + chi tiết tủ; push `develop` thành công (`1cb716b..23dbf76`). |
| 2026-06-14 | Claude | Audit toàn bộ mobile + sửa lệch endpoint (app gốc Revoland chỉ migrate một phần sang backend microservices hiện tại). Sửa 3 màn hỏng người dùng gặp: tab Hồ sơ (profile 404 + kẹt spinner do face-status AI), Đăng ký (sai path+payload), tab Tủ (gọi `/locations/customer` chết). Lập inventory đầy đủ feature legacy chưa wire (wallet/topup, courier/staff-application, face/QR AI, delegations legacy, advertisements/blogs, tab Đơn hàng order model cũ). | branch `fix/mobile-api-endpoints-alignment`: `profile_remote_data_source_impl.dart`, `profile_provider.dart`, `auth_remote_data_source_impl.dart`, `auth_bottom_sheet.dart`, `locker_remote_data_source_impl.dart`; living docs + mirror. | `flutter analyze` 0 error; rebuild APK PASS; on-device verify Tủ/Hồ sơ render + register API `AUTH_REGISTERED`. Chưa commit/push (chờ user). |
| 2026-06-14 | Claude | Cấu hình + chạy mobile với backend để manual test luồng tủ Customer/Maintenance. Phát hiện gateway local chạy port `18080` (host 8080 bị process khác chiếm) nhưng mobile hard-code `10.0.2.2:8080` → tạo `.env` (private, gitignored, không commit) + cập nhật generated `env_config.g.dart` sang `10.0.2.2:18080`, rebuild + cài APK debug, login customer vào đúng Customer Home. Maintenance login role PASS. | `smart-laundry-locker-mobile/.env` (local/private), `lib/core/config/env_config.g.dart` (generated, local không commit), living docs. | gateway `:18080` health UP; customer+maintenance login PASS; `flutter pub get`/`build apk --debug`/install/login Customer Home PASS. |
| 2026-06-14 | Codex | Hoàn tất hardening runtime notification còn dang dở: tách STOMP JWT auth thành interceptor có test, thêm handshake principal handler để Spring user registry nhận `userId`, thêm smoke script PowerShell cho CONNECT/subscribe/trigger/receive, và cấu hình RabbitMQ converter allow-list cho `DomainEvent` để hết lỗi deserialize. | `notification-service/src/main/java/com/huynqb/laundrylocker/notification/config/**`, `notification-service/src/test/java/com/huynqb/laundrylocker/notification/config/**`, `scripts/smoke-notification-stomp.ps1`, living docs/mirror. | `mvn -pl notification-service -am test` PASS; `mvn -pl notification-service -am package -DskipTests` PASS; Docker rebuild notification-service PASS; runtime smoke qua gateway `18080` PASS: health/login/list/count/FCM token save-delete/private STOMP MESSAGE/Rabbit conversion log clean. |
| 2026-06-14 | Codex | Sau khi merge/push vertical slice maintenance/user vào nhánh chính, tiếp tục harden realtime notification: notification-service giờ xác thực STOMP `CONNECT` bằng JWT access token và set user principal cho `/user/queue/notifications`; Flutter thêm `stomp_dart_client` và `RealtimeNotificationService` để subscribe realtime, insert notification mới và cập nhật unread badge. Runtime smoke STOMP/FCM còn bị chặn bởi Docker/gateway local không ổn định và deploy health 404. | `notification-service/pom.xml`, `notification-service/.../WebSocketConfig.java`, `notification-service/application.yml`, `smart-laundry-locker-mobile/lib/features/notifications/**`, `pubspec.yaml`, `pubspec.lock`, living docs/mirror. | `mvn -pl notification-service -am test` PASS; `flutter analyze --no-pub lib/features/notifications lib/core/services/firebase_messaging_service.dart` PASS; runtime smoke PARTIAL/FAIL do môi trường. |
| 2026-06-14 | Codex | Nâng cấp vertical slice maintenance/user realtime-readiness: maintenance API trả thêm locker address/toạ độ/thông tin ô; notification-service thêm public JWT endpoint `/api/notifications/fcm-tokens`; mobile sửa notification client sang `/api/notifications/**`, sync FCM token đúng gateway, thêm chỉ đường trong đơn tủ customer và màn Maintenance Home có dashboard ca trực/SLA/chỉ đường; web admin maintenance thêm stats và nút chỉ đường. | `locker-service`, `notification-service`, `smart-laundry-locker-mobile/lib/features/locker_ops/**`, `smart-laundry-locker-mobile/lib/features/notifications/**`, `smart-laundry-locker-mobile/lib/core/services/firebase_messaging_service.dart`, `laundry-locker-frontend/fe/src/pages/Admin/maintenance`, `fe/src/stores/apis/admin/lockerOps.ts`, living docs/mirror. | Backend Maven targeted PASS; Flutter targeted analyze PASS; Web FE build PASS. |
| 2026-06-14 | Claude | Thêm `docs/ARCHITECTURE_DECISIONS.md` (ADR-001..005): hoãn/không-áp-dụng-now K8s/Helm/GitOps, Kafka (giữ RabbitMQ), CQRS/Event Sourcing, GraphQL (giữ REST), service mesh — mỗi ADR có bối cảnh, lý do, phương án nhẹ đang dùng, **điều kiện xem lại**, và phác thảo khi áp dụng (handoff CodeX). Pointer trong 2 file sống. | `docs/ARCHITECTURE_DECISIONS.md`; `docs/BUSINESS_FLOWS_CURRENT.md`; `docs/PROJECT_PROGRESS_TRACKER.md`; mirror. | `git diff --check` PASS; docs-only, không đụng code/CI/compose. |
| 2026-06-14 | Claude | Backend L1 (luồng tủ): vá lỗ hổng ô kẹt `RESERVED` (G1/G2). `OrderService.autoCancelUnconfirmedOrders` giờ release ô + transition đầy đủ (history/event/notify); thêm `OrderScheduler.sweepUnconfirmedReservations` `@Scheduled` (cron mặc định mỗi 15 phút); cửa sổ giữ chỗ cấu hình `app.order.auto-cancel-hours` (24h). | `laundry-locker-microservices` branch `fix/locker-reservation-ttl-and-release`: `order-service/.../service/OrderService.java`, `OrderScheduler.java`; living docs. | `mvn -pl order-service -am test` BUILD SUCCESS (8 test common-lib pass; order-service compile sạch). |
| 2026-06-14 | Claude | Mobile: revamp UI 3 màn locker customer (Gửi hàng/Thuê tủ/Đơn tủ của tôi) về design system shadcn navy; thêm design kit dùng chung (`ops_widgets`, `locker_picker`); format giá/ngày/countdown; gate action theo trạng thái+loại (confirm/complete/extend/end/delegate/report/cancel). Bắt đầu lộ trình luồng tủ phần mobile. | `smart-laundry-locker-mobile` branch `feat/locker-customer-ui-revamp`: `lib/features/locker_ops/presentation/{widgets/ops_widgets.dart,widgets/locker_picker.dart,pages/send_parcel_page.dart,pages/rent_locker_page.dart,pages/my_locker_orders_page.dart}`; living docs (backend repo). | `flutter analyze` PASS (0 error; 413 info/warning debt cũ không đổi). |
| 2026-06-14 | Claude | Phân tích luồng tủ as-is (đọc trực tiếp `LockerService`, `OrderService`, `IotService`, entities, migrations, scheduler, `QrTokenService`) và đặc tả toàn bộ luồng nghiệp vụ tủ khóa chuẩn thực tế (to-be) + 16 gap (G1–G16) + backlog L1–L7. Phát hiện lỗ hổng: auto-cancel không @Scheduled & không release ô (G1/G2), quá hạn không giải phóng ô (G3), thiếu luồng nhận hàng courier PARCEL_RECEIVE (G6). Chỉ tài liệu, không đụng code. | `docs/project-artifacts/guides/LOCKER_FLOWS_STANDARD_SPEC.md` (mới); `docs/BUSINESS_FLOWS_CURRENT.md`; `docs/PROJECT_PROGRESS_TRACKER.md`; mirror `docs/project-artifacts/markdown-by-project/backend/docs/*`. | `git diff --check` PASS; quét private-file trong artifacts PASS. |
| 2026-06-13 | Claude | Mobile: thêm feature `stores` (list + detail + ratings) cho customer + entry "Khám phá cửa hàng" trên home; xác minh `develop` đã merge `ThaiBinh_NewUI_v1` (PR #1) nên không cần merge thủ công; xác minh UI Đội bảo trì (MAINTENANCE) đã wire (`homeForRoles`). | `smart-laundry-locker-mobile`: `lib/features/stores/**`, `lib/core/routing/app_router.dart`, `lib/features/home/presentation/pages/home_page.dart`; living docs (backend repo). | `flutter analyze` targeted PASS (0 issue) + full PASS (0 error). |
| 2026-06-13 | Claude | Bỏ role `PARTNER` khỏi backend mức shallow: gỡ role/permission `PARTNER_MANAGE`/account `partner.seed`/seed `partner_db` khỏi seed-demo-data, gỡ check partner trong verify-demo-data, gỡ `partner-service` khỏi docker-compose + override. Giữ schema `partner_db`/`partner_schema`/`partner_id` cũ (không đụng migration). | `docker/postgres/seed-demo-data.sql`, `docker/postgres/verify-demo-data.sql`, `docker-compose.yml`, `docker-compose.override.yml`, `docs/BUSINESS_FLOWS_CURRENT.md`, `docs/PROJECT_PROGRESS_TRACKER.md`. | `git diff --check` PASS; review seed SQL comma/structure PASS; grep Java `PARTNER` = no match. |
| 2026-06-13 | Codex | Production hardening Phase 4 continuation: thêm GitHub artifact attestations cho deploy artifact, tag-based backend release workflow tạo tarball/SBOM/checksum/provenance và publish GitHub Release, thêm script verify release artifact. | `.github/workflows/deploy-droplet.yml`, `.github/workflows/backend-release.yml`, `scripts/verify-release-artifact.sh`, living docs. | `mvn -pl api-gateway -am test` PASS; Git Bash `bash -n` cho deploy/verify scripts PASS; `git diff --check` PASS. |
| 2026-06-13 | Codex | Production hardening Phase 3/4: thêm gateway RBAC/access-token unit tests, Swagger UI/OpenAPI aggregation qua gateway, build-info metadata, full 12-image Trivy matrix, deploy artifact checksum và checksum verify trong deploy script. | `api-gateway`, root `pom.xml`, `.github/workflows/backend-security.yml`, `.github/workflows/deploy-droplet.yml`, `scripts/deploy-from-artifact.sh`, living docs. | `mvn -pl api-gateway -am test` PASS; `mvn -B test` PASS/PARTIAL; `mvn -B clean verify` PASS/PARTIAL; Git Bash `bash -n scripts/deploy-from-artifact.sh` PASS. |
| 2026-06-13 | Codex | Production hardening Phase 2: bật Resilience4j circuit breaker/timeout cho OpenFeign, expose `/actuator/sbom`, generate CycloneDX SBOM, thêm Testcontainers smoke cho locker-service, và thêm SBOM/Trivy container scan gate trong backend security workflow. | Root `pom.xml`, service `pom.xml`/`application.yml`, `locker-service/src/test/**`, `.github/workflows/backend-security.yml`, living docs. | `mvn -pl locker-service -am test` PASS/PARTIAL; `mvn -B test` PASS/PARTIAL; `mvn -B clean verify` PASS/PARTIAL; Docker local không khả dụng nên Testcontainers skip 2 smoke tests. |
| 2026-06-13 | Codex | Production hardening Phase 1: correlation ID xuyên gateway/service, Prometheus metrics endpoint, OpenAPI runtime docs, backend security workflow, deploy bắt buộc `clean verify`. | `common-lib`, `api-gateway`, service `pom.xml`/`application.yml`, `.github/workflows/*`, living docs. | `mvn -pl common-lib,api-gateway -am test` PASS; `mvn test` PASS; `mvn -B clean verify` PASS. |
| 2026-06-13 | Codex | Hardening backend nền tảng: secret policy dùng chung, chặn refresh token ở gateway, guard config payment production, thêm backend CI. | `common-lib`, `api-gateway`, `auth-service`, `order-service`, `payment-service`, `.github/workflows/backend-ci.yml`. | `mvn test` PASS toàn bộ backend; `git diff --check` PASS. |
| 2026-06-13 | Codex | Đổi 2 file sống chính sang tiếng Việt có dấu và sync artifact mirror. | `docs/BUSINESS_FLOWS_CURRENT.md`, `docs/PROJECT_PROGRESS_TRACKER.md`, `docs/project-artifacts/markdown-by-project/backend/docs/*`. | Kiểm tra heading tiếng Anh cũ, link/copy artifact, và private txt exclusion. |
| 2026-06-13 | Codex | Tạo living business flow và progress tracker docs. | `docs/BUSINESS_FLOWS_CURRENT.md`, `docs/PROJECT_PROGRESS_TRACKER.md`, artifact copies. | Link đã verify; markdown artifacts đã sync; private local txt không bị copy. |
| 2026-06-13 | Codex | Cập nhật tài liệu cũ theo trạng thái hiện tại. | Backend README/run guide, mobile README, IoT README, root docs, artifact copies. | Markdown synced; private env/pro/Application excluded from artifacts. |
| 2026-06-13 | Codex | Verify lỗi kết nối deployed DB và hướng dẫn reset password postgres. | DigitalOcean/PostgreSQL. | Service users OK; postgres reset verified trong container. |
| 2026-06-13 | Codex | Hoàn tất wiring Flutter quick action route và verification. | `home_page.dart`, locker ops, auth routing. | `flutter analyze`, debug APK build, customer smoke passed. |
| 2026-06-13 | Codex | Fix blocker FE production build. | Examples, loyalty API, order timeline. | `npm.cmd run build` passed. |
| 2026-06-13 | Previous agent/Codex continuation | Implement backend Locker Phase 2. | order/locker/iot/gateway/user services. | Backend E2E recorded trong `RUN_RESULT.md`. |

## Mẫu Cập Nhật Cho Lần Sau

Khi hoàn tất một feature, thêm một dòng như sau:

```markdown
| YYYY-MM-DD | Agent/User | Tóm tắt thay đổi ngắn | File/khu vực chạm tới | PASS/FAIL/PARTIAL kèm lệnh hoặc lý do |
```

Sau đó cập nhật:

- `Bảng Tiến Độ Theo Component`
- `Việc Còn Lại`
- `Nhật Ký Verification`
- `docs/BUSINESS_FLOWS_CURRENT.md` nếu hành vi nghiệp vụ thay đổi
- `RUN_RESULT.md` nếu kết quả chạy/lịch sử verification thay đổi
